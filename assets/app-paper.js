const PAPER_REFRESH_MS=18e4,PAPER_LB_CLIENT_TTL_MS=18e4,PAPER_SHARE_PATH="/paper",PAPER_SHARE_REFERRAL_CLAIM_KEY="kg_paper_share_referral_claim_v1",PAPER_DEFAULT_NOTIFY_THRESHOLD=5,PAPER_RANK_WINDOWS=Object.freeze([{key:"d1",label:"1일",days:1},{key:"d3",label:"3일",days:3},{key:"d7",label:"7일",days:7},{key:"all",label:"전체",days:0}]);let paperData=null,paperQuoteMap={},paperLeaderboard=null,paperLeaderboardAt=0,paperError="",paperLoadInFlight=null,paperLoadingText="",paperTimer=null,paperRevealCode="",paperLinkInfo=null,paperBusy=!1,paperBusyText="",paperRankWindow="all",paperRankScope="all",paperParticipantSort="rank",paperShowParticipants=!1,paperParticipantsError="",paperNicknameEditing=!1,paperTicket={code:"",market:"",name:"",price:null,changePct:null,side:"buy",qty:""},paperSymbolAutocomplete=null,paperRankMotion=new Map,paperRankMotionUntil=0,paperLoadStartedAt=0,paperLoadSeq=0,paperLoadWatchdogTimer=null,paperOnboardDraft={nickname:"",recovery:""},paperMemberStage="";function paperResetMemberStage(){paperMemberStage=""}function paperIsLoggedIn(){try{return typeof authAvailable<"u"&&authAvailable===!0&&typeof currentAuthUser=="function"&&!!currentAuthUser()}catch{return!1}}let paperBoardShare=null,paperBadgeModalOpen=!1,paperSessionBadgeIds=[],paperPinDraft=null;const PAPER_BADGE_DEFS=[{id:"hall_of_fame",emoji:"👑",name:"명예의 전당",tier:"S",desc:"리더보드 1위를 한 번이라도 차지"},{id:"tenbagger",emoji:"💎",name:"텐배거",tier:"S",desc:"보유 종목 평가수익 +100% 달성"},{id:"whale",emoji:"🦈",name:"큰손",tier:"S",desc:"평가자산 1억원 돌파"},{id:"rocket",emoji:"🚀",name:"로켓",tier:"A",desc:"총수익률 +30% 달성"},{id:"hot_week",emoji:"🔥",name:"주간 불장",tier:"A",desc:"한 주 수익률 +20% 달성"},{id:"veteran",emoji:"🏃",name:"백전노장",tier:"A",desc:"누적 체결 200회"},{id:"climber",emoji:"📈",name:"상승가도",tier:"B",desc:"총수익률 +10% 달성"},{id:"trader",emoji:"⚡",name:"트레이더",tier:"B",desc:"누적 체결 50회"},{id:"diversified",emoji:"🎯",name:"분산투자",tier:"B",desc:"동시 5종목 이상 보유"},{id:"old_timer",emoji:"🐢",name:"고인물",tier:"B",desc:"계좌 개설 30일 경과"},{id:"in_profit",emoji:"💵",name:"수익권",tier:"C",desc:"5회 이상 체결 + 수익률 0% 이상"},{id:"first_buy",emoji:"🛒",name:"첫 매수",tier:"C",desc:"매수 주문 1회"},{id:"first_sell",emoji:"💸",name:"첫 매도",tier:"C",desc:"매도 주문 1회"},{id:"rookie",emoji:"🐣",name:"입문",tier:"C",desc:"첫 체결"},{id:"sprout",emoji:"🌱",name:"새싹",tier:"C",desc:"모의투자 계좌 개설"}],PAPER_BADGE_BY_ID=Object.fromEntries(PAPER_BADGE_DEFS.map(b=>[b.id,b])),PAPER_BADGE_BY_EMOJI=Object.fromEntries(PAPER_BADGE_DEFS.map(b=>[b.emoji,b]));function paperBadgeTipByEmoji(emoji){const def=PAPER_BADGE_BY_EMOJI[String(emoji||"")];return def?`모의투자 뱃지 : "${def.name}"
${def.desc} — 이 뱃지를 받았어요`:""}window.paperBadgeTipByEmoji=paperBadgeTipByEmoji;const PAPER_MAX_PINNED_BADGES=3,PAPER_SHARE_CHANNELS=[{key:"free",label:"자유"},{key:"kr",label:"국장"},{key:"us",label:"미장"},{key:"coin",label:"코인"}];function paperEarnedBadgeIds(){const earned=paperData?.badges?.earned;return Array.isArray(earned)?earned.filter(id=>PAPER_BADGE_BY_ID[id]):[]}function paperPinnedBadgeIds(){return Array.isArray(paperPinDraft)?paperPinDraft.filter(id=>PAPER_BADGE_BY_ID[id]):paperSessionBadgeIds.filter(id=>PAPER_BADGE_BY_ID[id]&&paperEarnedBadgeIds().includes(id)).slice(0,PAPER_MAX_PINNED_BADGES)}function paperBadgeEmoji(id){return PAPER_BADGE_BY_ID[id]?.emoji||""}function paperMyNickBadgeIds(){return paperPinnedBadgeIds().slice(0,3)}window.paperMyNickBadgeIds=paperMyNickBadgeIds;function paperMyNickBadgeEmojis(){return paperPinnedBadgeIds().map(paperBadgeEmoji).filter(Boolean).slice(0,3)}window.paperMyNickBadgeEmojis=paperMyNickBadgeEmojis;function paperMyRankPercentile(){const me=paperLeaderboard?.me||{},rank=Number(me.rank)||0,total=Number(paperLeaderboard?.total)||0;if(rank<1||total<10)return null;const topPct=Math.max(1,Math.round(rank/total*100)),bottomPct=Math.max(1,Math.round((total-rank+1)/total*100));return topPct<=20?{side:"top",pct:topPct}:bottomPct<=20?{side:"bottom",pct:bottomPct}:null}window.paperMyRankPercentile=paperMyRankPercentile;function paperApiUrl(path){try{if(typeof window.apiUrl=="function")return window.apiUrl(path);if(typeof apiUrl=="function")return apiUrl(path)}catch{}return path}function paperUserIdSafe(){try{if(typeof authAvailable<"u"&&authAvailable===!0&&typeof currentAuthUser=="function"){const u=currentAuthUser();if(u&&u.userId)return String(u.userId)}}catch{}try{return typeof chatUserId=="function"?chatUserId():""}catch{return""}}function paperAuthHeader(){try{if(typeof authAvailable<"u"&&authAvailable===!0&&typeof authSessionToken=="function"){const t=authSessionToken();if(t)return{"x-session-token":t}}}catch{}return{}}function paperNotificationSettings(){try{const key=typeof CHAT_NOTIFICATION_SETTINGS_KEY=="string"?CHAT_NOTIFICATION_SETTINGS_KEY:"kg_chat_notification_settings_v1";return JSON.parse(localStorage.getItem(key)||"{}")||{}}catch{return{}}}function writePaperPerformanceNotificationSetting(enabled){try{const key=typeof CHAT_NOTIFICATION_SETTINGS_KEY=="string"?CHAT_NOTIFICATION_SETTINGS_KEY:"kg_chat_notification_settings_v1",settings=paperNotificationSettings();return settings.notifyPaperPerformance=!!enabled,localStorage.setItem(key,JSON.stringify(settings)),typeof persistSet=="function"&&persistSet(key,JSON.stringify(settings)),settings}catch{return paperNotificationSettings()}}function paperPerformanceNotificationDefaultEnabled(){return paperNotificationSettings().notifyPaperPerformance!==!1}async function ensurePaperPerformanceNotificationDefault(options={}){if(!paperData?.account)return{ok:!1,skipped:"no-account"};if(!paperPerformanceNotificationDefaultEnabled())return{ok:!1,skipped:"disabled"};if(typeof Notification>"u"||Notification.permission!=="granted")return{ok:!1,skipped:"permission"};if(Number(paperData.account.notifyThreshold)>0)return{ok:!0,skipped:"already-on"};const data=await paperPost("notify-settings",{threshold:5}),threshold=Number(data?.notifyThreshold)||5;return paperData?.account&&(paperData.account.notifyThreshold=threshold),options.render!==!1&&paperTabActive()&&renderPaperTab(),options.silent||showToast?.(`모의투자 알림을 ±${threshold}%p 기준으로 켰어요`,"info"),{ok:!0,notifyThreshold:threshold}}function paperShareUsesQueryUrl(){try{return location.hostname==="excelkospi.github.io"}catch{return!1}}function paperShareUrl(ref=""){const suffix=ref?paperShareUsesQueryUrl()?`${PAPER_SHARE_PATH}/?p=${encodeURIComponent(ref)}`:`${PAPER_SHARE_PATH}/${encodeURIComponent(ref)}`:paperShareUsesQueryUrl()?`${PAPER_SHARE_PATH}/`:PAPER_SHARE_PATH;try{return new URL(suffix,location.origin).toString()}catch{return`${location.origin||"https://excelkospi.pages.dev"}${suffix}`}}function paperShareToken(value){const token=String(value||"").trim();return/^[A-Za-z0-9_-]{10,32}$/.test(token)?token:""}function paperSharePathToken(pathname){const path=String(pathname||"").replace(/\/+$/g,""),prefix=`${PAPER_SHARE_PATH}/`;if(!path.startsWith(prefix))return"";const encoded=path.slice(prefix.length);if(!encoded||encoded.includes("/"))return"";try{return paperShareToken(decodeURIComponent(encoded))}catch{return""}}function paperShareUrlToken(value){try{const url=new URL(String(value||""),location.origin),path=url.pathname.replace(/\/+$/g,""),pathToken=paperSharePathToken(path);return pathToken||(path!==PAPER_SHARE_PATH?"":paperShareToken(url.searchParams.get("p")))}catch{return""}}function paperContestShareUrl(token,candidate=""){const ref=paperShareToken(token)||paperShareUrlToken(candidate);if(!ref)return"";try{const url=new URL(String(candidate||""),location.origin),path=url.pathname.replace(/\/+$/g,"");if(paperSharePathToken(path)===ref||path===PAPER_SHARE_PATH&&url.searchParams.get("p")===ref)return url.toString()}catch{}return paperShareUrl(ref)}function paperCurrentShareRef(){try{const url=new URL(location.href),path=url.pathname.replace(/\/+$/g,""),pathToken=paperSharePathToken(path);return pathToken||(path!==PAPER_SHARE_PATH?"":paperShareToken(url.searchParams.get("p")))}catch{return""}}function paperKstDayKey(){return new Date(Date.now()+9*60*60*1e3).toISOString().slice(0,10)}function paperUseNativeShare(){try{if(!navigator.share)return!1;const ua=String(navigator.userAgent||"");return!!(window.matchMedia?.("(max-width: 760px)")?.matches||/Mobi|Android|iPhone|iPad|iPod/i.test(ua)||Number(navigator.maxTouchPoints)>1)}catch{return!1}}function paperSetBusy(text="처리 중..."){paperBusy=!0,paperBusyText=text,paperTabActive()&&renderPaperTab()}function paperClearBusy(){paperBusy=!1,paperBusyText="",paperTabActive()&&renderPaperTab()}function paperWon(value,withSign=!1){const n=Number(value);return Number.isFinite(n)?`${withSign&&n>0?"+":""}${Math.round(n).toLocaleString("ko-KR")}원`:"-"}function paperSignedPct(value,digits=2){const n=Number(value);return Number.isFinite(n)?`${n>0?"+":""}${n.toFixed(digits)}%`:"-"}function paperDirClass(value){const n=Number(value);return!Number.isFinite(n)||Math.abs(n)<1e-9?"flat":n>0?"up":"down"}function paperQtyText(market,qty){const n=Number(qty);return Number.isFinite(n)?market==="COIN"?String(Math.round(n*1e6)/1e6):n.toLocaleString("ko-KR"):"-"}function paperRecentShareTrades(limit=8){const max=Math.max(1,Math.min(20,Number(limit)||8));return(paperData?.orders||[]).filter(order=>String(order?.id||"").startsWith("o_")&&(order.side==="buy"||order.side==="sell"&&Number.isFinite(Number(order.realized)))).slice(0,max).map(order=>{const code=String(order.code||"").toUpperCase(),market=String(order.market||"").toUpperCase(),qty=Number(order.qty)||0,price=Number(order.price)||0,amount=Number(order.amount)||0,base={id:String(order.id),code,market,name:String(order.name||order.code||"").slice(0,40),side:order.side==="buy"?"buy":"sell",qty,price,amount,fee:Number(order.fee)||0,createdAt:order.created_at||order.createdAt||"",timeText:typeof relativeTimeKR=="function"?relativeTimeKR(order.created_at||order.createdAt):""};if(base.side==="sell"){const realizedRaw=Number(order.realized)||0,costBasis=amount-realizedRaw,returnPct=costBasis>0?Math.round(realizedRaw/costBasis*1e4)/100:null;return{...base,realized:Math.round(realizedRaw),returnPct}}const markKRW=paperLiveMarkKRW(code,market);return Number.isFinite(markKRW)&&markKRW>0&&price>0?{...base,markPrice:Math.round(markKRW),unrealized:Math.round((markKRW-price)*qty),returnPct:Math.round((markKRW/price-1)*1e4)/100}:base})}window.paperRecentShareTrades=paperRecentShareTrades;function paperTradePctText(order){const pct=Number(order?.returnPct);return Number.isFinite(pct)?`${pct>=0?"+":""}${pct.toFixed(2)}%`:""}window.paperTradePctText=paperTradePctText;function paperShareTradeOptionText(order){const name=order?.name||order?.code||"체결",time=order?.timeText?` · ${order.timeText}`:"",pct=paperTradePctText(order),pctSuffix=pct?` (${pct})`:"";if(order?.side==="buy"){const unrealized=Number(order?.unrealized),valText=Number.isFinite(unrealized)?`평가 ${paperWon(unrealized,!0)}`:"평가 집계 중";return`${name} 매수 · ${valText}${pctSuffix}${time}`}const profit=Number(order?.realized),profitText=Number.isFinite(profit)?paperWon(profit,!0):"손익 집계 중";return`${name} 매도 · ${profitText}${pctSuffix}${time}`}window.paperShareTradeOptionText=paperShareTradeOptionText;function paperShareTradeProofHtml(order,className="pp-trade-proof"){if(!order)return"";const isBuy=order.side==="buy",value=Number(isBuy?order.unrealized:order.realized),dir=paperDirClass(Number.isFinite(value)?value:0),market=paperTradeMarketLabel(order.market),qty=paperQtyText(order.market,order.qty),time=order.timeText||"",sideLabel=isBuy?"매수":"매도",cap=isBuy?"선택 매수 · 공유 시점 평가손익":"선택 매매 실현손익",aria=isBuy?"선택한 매수 평가손익":"선택한 매매 실현손익",pct=paperTradePctText(order),valueText=Number.isFinite(value)?`${paperWon(value,!0)}${pct?` (${pct})`:""}`:isBuy?"평가 집계 중":"손익 집계 중";return`<div class="${className} ${dir}" role="group" aria-label="${aria}">
    <span class="${className}-cap">${esc(cap)}</span>
    <b class="${className}-profit">${esc(valueText)}</b>
    <span class="${className}-meta">${esc([market,order.name||order.code,`${qty} ${sideLabel}`,time].filter(Boolean).join(" · "))}</span>
  </div>`}const PAPER_FETCH_TIMEOUT_MS=15*1e3,PAPER_LOAD_WATCHDOG_MS=18*1e3;function paperFetch(url,init={}){const controller=new AbortController;let timedOut=!1;const timeoutError=new Error("요청이 너무 오래 걸려요. 네트워크 상태를 확인해 주세요.");let timer=null;const timeout=new Promise((_,reject)=>{timer=setTimeout(()=>{timedOut=!0;try{controller.abort(timeoutError)}catch{}reject(timeoutError)},PAPER_FETCH_TIMEOUT_MS)});return Promise.race([fetch(url,{...init,signal:controller.signal}),timeout]).catch(error=>{throw timedOut||error?.name==="AbortError"?timeoutError:error}).finally(()=>clearTimeout(timer))}function paperIdentityError(data){const code=String(data?.code||data?.error||"");return code==="chat_identity_required"||code==="identity_mismatch"||code==="identity_owner_mismatch"}async function paperIdentityToken(force=!1){try{if(typeof ensureChatIdentityToken=="function")return await ensureChatIdentityToken(force?{force:!0}:{})}catch{}return""}function paperClearIdentityToken(){try{typeof clearChatIdentityToken=="function"&&clearChatIdentityToken()}catch{}}function paperCachedIdentityToken(){try{if(typeof readChatIdentityToken=="function")return readChatIdentityToken()}catch{}return""}async function paperFetchJsonWithIdentity(url,init={},retry=!0,token=paperCachedIdentityToken()){const headers={...init.headers||{},...token?{"x-chat-identity-token":token}:{},...paperAuthHeader()},res=await paperFetch(url,{...init,headers}),data=await res.json().catch(()=>({ok:!1,error:"응답을 읽지 못했어요"}));if((!res.ok||data?.ok===!1)&&paperIdentityError(data)&&retry){paperClearIdentityToken();const freshToken=await paperIdentityToken(!0);return paperFetchJsonWithIdentity(url,init,!1,freshToken)}if(!res.ok||data?.ok===!1)throw new Error(data?.error||`요청 실패 (${res.status})`);return data}async function paperPostJsonWithIdentity(url,payload={},retry=!0){const token=await paperIdentityToken(!1),body={...payload,...token?{identity_token:token}:{}},res=await paperFetch(url,{method:"POST",headers:{"content-type":"application/json",...token?{"x-chat-identity-token":token}:{},...paperAuthHeader()},body:JSON.stringify(body)}),data=await res.json().catch(()=>({ok:!1,error:"응답을 읽지 못했어요"}));if((!res.ok||data?.ok===!1)&&paperIdentityError(data)&&retry){paperClearIdentityToken();const freshToken=await paperIdentityToken(!0);return paperPostJsonWithIdentity(url,{...payload,...freshToken?{identity_token:freshToken}:{}},!1)}if(!res.ok||data?.ok===!1)throw new Error(data?.error||`요청 실패 (${res.status})`);return data}async function paperPost(action,payload={}){const userId=paperUserIdSafe();if(action!=="create")return paperPostJsonWithIdentity(paperApiUrl("/api/paper"),{action,user_id:userId,...payload});await paperIdentityToken(!1).catch(()=>"");const attempt=async(fresh,forceIdentity=!1)=>{const identityToken=await paperIdentityToken(forceIdentity),ttok=typeof chatTurnstileToken=="function"?await chatTurnstileToken(2e4,fresh?{fresh:!0}:{nonBlocking:!0}).catch(()=>""):"",res2=await paperFetch(paperApiUrl("/api/paper"),{method:"POST",headers:{"content-type":"application/json",...identityToken?{"x-chat-identity-token":identityToken}:{},...paperAuthHeader()},body:JSON.stringify({action,user_id:userId,...ttok?{turnstile_token:ttok}:{},...identityToken?{identity_token:identityToken}:{},...payload})}),data2=await res2.json().catch(()=>({ok:!1,error:"응답을 읽지 못했어요"}));return{res:res2,data:data2}};let{res,data}=await attempt(!1);if((res.status===403||data?.code==="turnstile_required"||data?.error==="turnstile_required")&&({res,data}=await attempt(!0)),(!res.ok||data?.ok===!1)&&paperIdentityError(data)&&(paperClearIdentityToken(),{res,data}=await attempt(!1,!0)),!res.ok||data?.ok===!1)throw new Error(data?.error||`요청 실패 (${res.status})`);return data}async function paperFetchPortfolio(){const userId=paperUserIdSafe();return paperFetchJsonWithIdentity(paperApiUrl(`/api/paper?user_id=${encodeURIComponent(userId)}`),{cache:"no-store"})}async function paperMaybeClaimShareReferral(){const ref=paperCurrentShareRef();if(!ref||!paperData?.account)return;const key=`${PAPER_SHARE_REFERRAL_CLAIM_KEY}:${ref}:${paperKstDayKey()}`;try{if(localStorage.getItem(key))return}catch{}try{const data=await paperPost("share-referral-claim",{ref});if(data?.ok!==!1&&data?.reason!=="no_visitor_account")try{localStorage.setItem(key,String(Date.now()))}catch{}data?.rewarded&&showToast?.("공유자에게 보너스 시드 200만원이 지급됐어요","info")}catch(e){debugWarn("paper referral claim failed",e)}}function paperQuoteTtlSeconds(){try{const kind=typeof pollProfileKind=="function"?pollProfileKind():"regular";if(kind==="weekend")return 300;if(kind==="extended")return 120}catch{}return 60}async function paperFetchQuotes(positions){const codes=(positions||[]).map(p=>`${p.code}:${p.market}`);paperTicket.code&&codes.push(`${paperTicket.code}:${paperTicket.market}`),codes.some(c=>c.endsWith(":US"))&&codes.push("KRW=X:US");const unique=[...new Set(codes)].slice(0,30);if(!unique.length)return{};const map={},misses=[];for(const c of unique){const[code,market]=c.split(":"),shared=typeof window.sharedQuoteForPaper=="function"?window.sharedQuoteForPaper(code,market):null;shared&&Number.isFinite(Number(shared.price))?map[`${String(code).toUpperCase()}:${String(market).toUpperCase()}`]={price:Number(shared.price),changePct:Number(shared.changePct)}:misses.push(c)}if(!misses.length)return map;try{const data=await(await paperFetch(paperApiUrl(`/api/quote?codes=${encodeURIComponent(misses.join(","))}&ttl=${paperQuoteTtlSeconds()}&coinSource=upbit`))).json();for(const row of data?.results||[])row?.ok&&Number.isFinite(Number(row.price))&&(map[`${String(row.code||"").toUpperCase()}:${String(row.market||"").toUpperCase()}`]={price:Number(row.price),changePct:Number(row.changePct??row.changePercent)});return map}catch{return map}}async function paperFetchLeaderboard(force=!1,options={}){const needsParticipants=!!options.participants,hasParticipants=Array.isArray(paperLeaderboard?.participants);if(!force&&paperLeaderboard&&Date.now()-paperLeaderboardAt<18e4&&(!needsParticipants||hasParticipants))return paperLeaderboard;const userId=paperUserIdSafe(),participantParam=needsParticipants?"&participants=1":"",data=await paperFetchJsonWithIdentity(paperApiUrl(`/api/paper?action=leaderboard&user_id=${encodeURIComponent(userId)}${participantParam}`),{cache:"no-store"}).catch(()=>null);if(data?.ok){const nextLeaderboard=needsParticipants?data:{...paperLeaderboard||{},...data,participants:paperLeaderboard?.participants};paperPrepareRankMotion(paperLeaderboard,nextLeaderboard),paperLeaderboard=nextLeaderboard,paperLeaderboardAt=Date.now()}return paperLeaderboard}const PAPER_SNAPSHOT_KEY="kg_paper_snapshot_v1",PAPER_SNAPSHOT_MAX_AGE_MS=24*60*60*1e3;function clearPaperSnapshot(){try{localStorage.removeItem(PAPER_SNAPSHOT_KEY)}catch{}}function writePaperSnapshot(){try{if(!paperData?.account){clearPaperSnapshot();return}const leaderboard=paperLeaderboard?{...paperLeaderboard,participants:void 0}:null;localStorage.setItem(PAPER_SNAPSHOT_KEY,JSON.stringify({at:Date.now(),userId:paperUserIdSafe(),data:paperData,quotes:paperQuoteMap,leaderboard}))}catch{}}function hydratePaperSnapshot(){if(paperData)return!1;try{const snap=JSON.parse(localStorage.getItem(PAPER_SNAPSHOT_KEY)||"null");return!snap?.data?.account||String(snap.userId||"")!==String(paperUserIdSafe()||"")||Date.now()-Number(snap.at||0)>PAPER_SNAPSHOT_MAX_AGE_MS?!1:(paperData=snap.data,paperQuoteMap=snap.quotes&&typeof snap.quotes=="object"?snap.quotes:{},snap.leaderboard&&(paperLeaderboard=snap.leaderboard,paperLeaderboardAt=0),!0)}catch{return!1}}async function loadPaperTab(options={}){if(paperLoadInFlight){if(Date.now()-paperLoadStartedAt<PAPER_LOAD_WATCHDOG_MS)return paperLoadInFlight;paperLoadSeq+=1,paperLoadInFlight=null,paperLoadingText="",paperError="모의투자 로딩이 오래 걸려 다시 시도할게요.",paperTabActive()&&renderPaperTab()}const seq=paperLoadSeq+1;paperLoadSeq=seq,paperLoadStartedAt=Date.now(),paperLoadWatchdogTimer&&clearTimeout(paperLoadWatchdogTimer),paperLoadWatchdogTimer=setTimeout(()=>{seq!==paperLoadSeq||!paperLoadInFlight||(paperLoadSeq+=1,paperLoadInFlight=null,paperLoadingText="",paperError="모의투자 로딩이 오래 걸려 중단했어요. 다시 불러오기를 눌러 주세요.",paperTabActive()&&renderPaperTab(),paperTabActive()&&schedulePaperRefresh())},PAPER_LOAD_WATCHDOG_MS),paperError="";const hydrated=!paperData&&!options.silent&&hydratePaperSnapshot();return paperLoadingText=options.silent||hydrated?"":options.forceLb?"랭킹을 다시 불러오는 중...":paperData?"모의투자 현황을 새로고침 중...":"모의투자 데이터를 불러오는 중...",(paperLoadingText||hydrated)&&paperTabActive()&&renderPaperTab(),paperLoadInFlight=(async()=>{const data=await paperFetchPortfolio();if(seq!==paperLoadSeq)return data;if(paperData=data,data?.deleted==="moderation")try{showToast?.("운영정책 위반으로 계정이 삭제되었습니다.","warn")}catch{}if(data?.account&&paperMaybeClaimShareReferral(),data?.leaderboard){const next={...paperLeaderboard||{},...data.leaderboard,participants:paperLeaderboard?.participants};paperPrepareRankMotion(paperLeaderboard,next),paperLeaderboard=next,paperLeaderboardAt=Date.now()}const quotesPromise=paperFetchQuotes(data?.positions).then(map=>(map&&Object.keys(map).length&&(paperQuoteMap={...paperQuoteMap,...map}),map));return await Promise.race([quotesPromise,new Promise(resolve=>setTimeout(()=>resolve("__slow__"),2500))])==="__slow__"&&(paperTabActive()&&renderPaperTab(),await quotesPromise.catch(()=>null)),paperShowParticipants&&await paperFetchLeaderboard(!!options.forceLb,{participants:!0}).catch(()=>null),await ensurePaperPerformanceNotificationDefault({silent:!0,render:!1}).catch(()=>null),seq!==paperLoadSeq||(paperTabActive()&&renderPaperTab(),writePaperSnapshot()),data})().catch(error=>{throw seq===paperLoadSeq&&(paperError=error?.message||"paper load failed",clearPaperSnapshot(),paperTabActive()&&renderPaperTab()),error}).finally(()=>{if(seq===paperLoadSeq){paperLoadInFlight=null,paperLoadWatchdogTimer&&(clearTimeout(paperLoadWatchdogTimer),paperLoadWatchdogTimer=null);const hadLoadingText=!!paperLoadingText;paperLoadingText="",hadLoadingText&&paperTabActive()&&renderPaperTab(),paperTabActive()&&schedulePaperRefresh()}}),paperLoadInFlight}function paperTabActive(){try{return typeof timelineIsPaper=="function"&&timelineIsPaper()}catch{return!1}}function clearPaperRefresh(){paperTimer&&(clearTimeout(paperTimer),paperTimer=null)}function paperEquityIsStatic(){const positions=paperData?.positions||[];if(!positions.length)return!0;const policy=paperData?.policy||{};return positions.every(p=>{const m=String(p.market||"").toUpperCase();return m==="COIN"?!1:m==="KR"?policy.krOpen===!1&&policy.krEtfOpen===!1:m==="US"?policy.usOpen===!1:!0})}const PAPER_STATIC_REFRESH_MS=15*60*1e3;function paperRefreshDelayMs(){try{if(paperEquityIsStatic())return PAPER_STATIC_REFRESH_MS;const kind=typeof pollProfileKind=="function"?pollProfileKind():"regular";if(kind==="weekend")return 5*60*1e3;if(kind==="extended")return 3*60*1e3}catch{}return 18e4}function schedulePaperRefresh(){if(clearPaperRefresh(),window.CHAT_POPUP_MODE||!paperTabActive()||typeof shouldPauseDataRefreshForHidden=="function"&&shouldPauseDataRefreshForHidden())return;const delay=!paperData&&paperError?10*1e3:paperRefreshDelayMs();paperTimer=setTimeout(()=>{paperTimer=null,paperTabActive()&&(typeof shouldPauseDataRefreshForHidden=="function"&&shouldPauseDataRefreshForHidden()||loadPaperTab({silent:!0}).catch(e=>debugWarn("paper refresh failed",e)))},delay)}function paperFx(){const fx=Number(paperQuoteMap["KRW=X:US"]?.price);return Number.isFinite(fx)&&fx>=800&&fx<=3e3?fx:null}function paperPositionMark(pos){const quote=paperQuoteMap[`${pos.code}:${pos.market}`];let price=Number(quote?.price);if(String(pos.market)==="US"){const fx=paperFx();price=Number.isFinite(price)&&fx?price*fx:NaN}return Number.isFinite(price)&&price>0?price:Number(pos.avg_price)||0}function paperLiveMarkKRW(code,market){const quote=paperQuoteMap[`${String(code).toUpperCase()}:${String(market).toUpperCase()}`];let price=Number(quote?.price);if(!Number.isFinite(price)||price<=0)return NaN;if(String(market).toUpperCase()==="US"){const fx=paperFx();if(!fx)return NaN;price=price*fx}return price}function paperEquity(){const account=paperData?.account;if(!account)return null;let equity=Number(account.cash)||0;for(const pos of paperData?.positions||[])equity+=(Number(pos.qty)||0)*paperPositionMark(pos);return Math.round(equity)}function paperDashWrap(inner){return`<tr class="paper-dash-row"><td class="forecast-dash-cell">${inner}</td></tr>`}function paperPageHeaderHtml(){if(paperData?.account)return"";const total=Number(paperLeaderboard?.total)||0,meta=paperData?.account?.nickname?paperData.account.nickname:total?`참가 ${total.toLocaleString("ko-KR")}명`:"가상 시드 5,000만원";return`<div class="pp-page-head"><strong>excelkospi 모의투자</strong><em>${esc(meta)}</em></div>`}function paperProgressHtml(){const text=paperBusyText||paperLoadingText;return text?`<div class="pp-progress" role="status"><span class="news-loading-spin"></span><b>${esc(text)}</b><em>백그라운드에서 자동으로 갱신돼요. 다른 화면을 봐도 돼요.</em></div>`:""}const PAPER_RECOVERY_ACK_KEY="kg_paper_recovery_ack_v1";function paperRecoveryBannerHtml(){return paperData?.account?`<div class="pp-recovery-banner" role="alert">
    <div><strong>복구코드를 꼭 저장해 두세요.</strong><span>브라우저 데이터가 지워지면 복구코드나 연결된 다른 기기로만 계좌를 되찾을 수 있어요. 휴대폰을 미리 연결해 두는 게 가장 안전해요.</span></div>
    <div class="pp-banner-actions">
      <button class="pp-btn pp-btn-primary" type="button" data-pp-act="qr" ${paperBusy?"disabled":""}>📱 다른 기기 연결<em class="pp-reco-chip">강추</em></button>
      <button class="pp-btn" type="button" data-pp-act="reissue" ${paperBusy?"disabled":""}>복구코드 보기·재발급</button>
    </div>
  </div>`:""}function paperNoticeHtml(){const rankDays=Number(paperData?.policy?.inactiveRankDays)||7,deleteDays=Number(paperData?.policy?.inactiveDeleteDays??paperData?.policy?.inactiveDays)||10;return`<details class="pp-help">
    <summary class="pp-help-summary" aria-expanded="false">이용 안내 · 거래 시간 · 휴면 정책</summary>
    <p class="pp-disc pp-beta-disc"><b>베타 기능</b><br>거래내역과 계좌가 예고 없이 초기화될 수 있어요.</p>
    <p class="pp-disc">가상 자금으로 하는 연습 투자예요. 체결가·잔고·순위는 모두 서버가 계산해요.
    ${rankDays}일 동안 접속하지 않으면 랭킹에서 빠지고, ${deleteDays}일이 지나면 계좌가 삭제돼요.</p>
    <p class="pp-disc"><b>운영정책</b><br>부적절한 닉네임·활동(욕설·사칭·도배·어뷰징 등)이 확인되면 사전 통지 없이
    닉네임이 임의로 변경되거나, 계정이 제한·삭제될 수 있어요. 가상 자산이라 보상 대상이 아니에요.</p>
    <p class="pp-disc"><b>거래 시간</b><br>
    국내주식: 거래일 08:00~20:00 (정규장 앞뒤 시간외 포함, ETF는 09:00~15:30만)<br>
    해외주식: 미국 프리장~애프터장 (달러 시세에 환율을 곱해 원화로 체결)<br>
    코인: 24시간</p>
    <p class="pp-disc"><b>수수료</b> (실제 한국 표준 요율과 같아요)<br>
    국내 0.015% (매도 시 증권거래세 0.2% 추가) · 해외 0.25% · 코인 0.05%</p>
    ${paperData?.account?`<div class="pp-notify-row">
      <span>🔔 모의투자 알림</span>
      <select id="ppNotifySelect" ${paperBusy?"disabled":""}>
        ${[0,5].map(v=>`<option value="${v}" ${(Number(paperData.account.notifyThreshold)>0?5:0)===v?"selected":""}>${v===0?"끄기":"켜기"}</option>`).join("")}
      </select>
      <small>순위가 전체 참가자의 10% 이상 움직이거나 수익률이 5%p 이상 변하면 알려드려요(하루 최대 3회).</small>
    </div>`:""}
    ${paperData?.account?`<div class="pp-help-danger">
      <span>처음부터 다시 하고 싶다면 계좌를 삭제할 수 있어요. 기록은 복구되지 않아요.</span>
      <button class="pp-btn pp-btn-danger" type="button" data-pp-act="close-account" ${paperBusy?"disabled":""}>계좌 삭제하고 처음부터</button>
    </div>`:""}
  </details>`}function paperAccountSeedCash(){const accountSeed=Number(paperData?.account?.seedCash??paperData?.account?.seed_cash);if(Number.isFinite(accountSeed)&&accountSeed>0)return accountSeed;const policySeed=Number(paperData?.policy?.seedCash);return Number.isFinite(policySeed)&&policySeed>0?policySeed:5e7}function paperMyRankSnapshot(){const account=paperData?.account;if(!account)return null;const equity=paperEquity(),seed=paperAccountSeedCash(),returnPct=Number.isFinite(equity)?Math.round((equity/seed-1)*1e4)/100:null,me=paperLeaderboard?.me||{};return{nickname:account.nickname||"월급루팡",rank:Number(me.rank)||null,total:Number(paperLeaderboard?.total)||null,equity,profit:Number.isFinite(equity)?equity-seed:null,returnPct,ordersCount:Number(account.orders_count??account.ordersCount)||0,buyOrdersCount:Number(account.buy_orders_count??account.buyOrdersCount)||0,sellOrdersCount:Number(account.sell_orders_count??account.sellOrdersCount)||0,badges:paperMyNickBadgeEmojis()}}function paperNumberAttr(value){const number=Number(value);return Number.isFinite(number)?String(Math.round(number*100)/100):""}function paperShareDomNumber(name){try{const root=document.querySelector(".pp-summary");if(!root)return null;const raw=root.getAttribute(`data-pp-${name}`);if(raw===null||raw==="")return null;const number=Number(raw);return Number.isFinite(number)?number:null}catch{return null}}function paperShareVisibleSnapshot(base=null){if(!document.querySelector(".pp-summary"))return null;const number=(name,fallback=null)=>{const value=paperShareDomNumber(name);return value===null?fallback:value},snap=base||{},equity=number("equity",snap.equity),profit=number("profit",snap.profit),returnPct=number("return-pct",snap.returnPct),rank=number("rank",snap.rank),total=number("total",snap.total),ordersCount=number("orders",snap.ordersCount);return{...snap,equity,profit,returnPct,rank:Number.isFinite(rank)&&rank>0?Math.round(rank):null,total:Number.isFinite(total)&&total>0?Math.round(total):null,ordersCount:Number.isFinite(ordersCount)&&ordersCount>=0?Math.round(ordersCount):snap.ordersCount||0}}function paperProfitText(profit){if(!Number.isFinite(Number(profit)))return"손익 집계 중";const value=paperWon(profit,!0);return Number(profit)>0?`수익 ${value}`:Number(profit)<0?`손실 ${value}`:"손익 0원"}function paperShareText(input=null){const snap=input||paperShareVisibleSnapshot(paperMyRankSnapshot())||paperMyRankSnapshot();if(!snap)return"엑셀코스피 모의투자 대회 같이 해보세요!";const rankText=snap.rank&&snap.total?`${snap.total}명 중 ${snap.rank}등`:snap.rank?`${snap.rank}등`:"순위 집계 중",profitText=paperProfitText(snap.profit),retText=Number.isFinite(snap.returnPct)?`수익률 ${paperSignedPct(snap.returnPct)}`:"수익률 집계 중";return`모의투자 ${rankText} · ${profitText} · ${retText}`}function paperShareSnapshot(){const snap=paperShareVisibleSnapshot(paperMyRankSnapshot());return snap?{...snap,seedCash:paperAccountSeedCash(),topSymbols:paperTopShareSymbols(3)}:null}function paperShareSnapshotReady(snapshot){return!!snapshot&&Number.isFinite(Number(snapshot.equity))&&Number.isFinite(Number(snapshot.profit))&&Number.isFinite(Number(snapshot.returnPct))}function paperRemainingShort(ms){const n=Math.max(0,Number(ms)||0);if(n<=0)return"";const days=Math.ceil(n/(24*60*60*1e3));if(days>=2)return`${days}일`;const hours=Math.ceil(n/(60*60*1e3));return hours>=2?`${hours}시간`:`${Math.ceil(n/(60*1e3))}분`}function paperNicknameNextChangeMs(account=paperData?.account){const ms=Date.parse(account?.nicknameNextChangeAt||"");return Number.isFinite(ms)?ms:0}function paperNicknameCanChange(account=paperData?.account){const nextMs=paperNicknameNextChangeMs(account);return!nextMs||nextMs<=Date.now()}function paperNicknameChangeButtonText(account=paperData?.account){return paperNicknameCanChange(account)?"닉네임 변경":`${paperRemainingShort(paperNicknameNextChangeMs(account)-Date.now())} 후 변경`}function paperNicknameEditorHtml(nickname){return paperNicknameEditing?`<div class="pp-nick-editor">
    <input id="ppRenameInput" type="text" maxlength="12" value="${esc(nickname||"")}" autocomplete="off" ${paperBusy?"disabled":""} aria-label="모의투자 닉네임">
    <button class="pp-btn pp-btn-primary" type="button" data-pp-act="nickname-save" ${paperBusy?"disabled":""}>저장</button>
    <button class="pp-btn" type="button" data-pp-act="nickname-cancel" ${paperBusy?"disabled":""}>취소</button>
    <small>닉네임은 7일에 한 번 변경할 수 있어요.</small>
  </div>`:""}function paperTopHoldingNames(limit=3){return(paperData?.positions||[]).map(pos=>({name:String(pos.name||pos.code||"").trim(),value:(Number(pos.qty)||0)*paperPositionMark(pos)})).filter(item=>item.name&&item.value>0).sort((a,b)=>b.value-a.value).slice(0,limit).map(item=>item.name.slice(0,12))}function paperTopShareSymbols(limit=3){const map=new Map,add=(item,weight)=>{const code=String(item?.code||"").trim().toUpperCase(),market=String(item?.market||"").trim().toUpperCase(),key=`${code}:${market}`;if(!code||!market)return;const prev=map.get(key)||{code,market,name:String(item?.name||code).trim(),score:0};prev.name=prev.name||String(item?.name||code).trim(),prev.score+=Math.max(0,Number(weight)||0),map.set(key,prev)};for(const pos of paperData?.positions||[])add(pos,(Number(pos.qty)||0)*paperPositionMark(pos)*2);for(const order of paperData?.orders||[])add(order,Number(order.amount)||(Number(order.qty)||0)*(Number(order.price)||0));return[...map.values()].filter(item=>item.score>0).sort((a,b)=>b.score-a.score).slice(0,limit).map(item=>({code:item.code.slice(0,16),market:item.market,name:item.name.slice(0,20)}))}function paperBoastMessage(){const snap=paperMyRankSnapshot(),text=paperShareText();if(!snap)return text;const ordersCount=Math.max(0,Math.floor(Number(snap.ordersCount)||0)),meta={r:snap.rank||0,t:snap.total||0,ret:Number.isFinite(snap.returnPct)?snap.returnPct:null,p:Number.isFinite(snap.profit)?Math.round(snap.profit):null,o:ordersCount,h:paperTopHoldingNames(),b:snap.badges||[]},token=`⁣p${JSON.stringify(meta)}⁣`;return text.length+token.length<=278?`${text} ${token}`:text}function paperBoastEligibility(){const snap=paperMyRankSnapshot();return!paperData?.account||!snap?{ok:!1,reason:"no-account"}:(Number(snap.ordersCount)||0)<=0?{ok:!1,reason:"no-order"}:!Number.isFinite(snap.profit)||Math.round(snap.profit)===0?{ok:!1,reason:"zero-profit"}:{ok:!0,snap}}function paperSyncOnboardDraftFromDom(){const nick=document.getElementById("ppNickInput"),recovery=document.getElementById("ppRecoverInput");nick&&(paperOnboardDraft.nickname=String(nick.value||"")),recovery&&(paperOnboardDraft.recovery=String(recovery.value||""))}function paperOnboardFocusState(){const el=document.activeElement;return!el||el.id!=="ppNickInput"&&el.id!=="ppRecoverInput"?null:{id:el.id,start:Number.isFinite(el.selectionStart)?el.selectionStart:null,end:Number.isFinite(el.selectionEnd)?el.selectionEnd:null}}function paperRestoreOnboardFocus(state){state?.id&&setTimeout(()=>{const next=document.getElementById(state.id);if(!(!next||next.disabled)&&(next.focus({preventScroll:!0}),state.start!==null&&typeof next.setSelectionRange=="function"))try{next.setSelectionRange(state.start,state.end??state.start)}catch{}},0)}function paperMarketPulseHtml(){const stats=paperLeaderboard?.stats,total=Number(paperLeaderboard?.total)||0;if(!stats&&!total)return"";const popular=(stats?.popular||[]).slice(0,4).map(row=>`<em>${esc(row.name)}<i>${Number(row.holders)||0}명</i></em>`).join("");return`<div class="pp-pulse">
    <span><b>${total.toLocaleString("ko-KR")}</b>명 참가 중</span>
    ${Number(stats?.ordersToday)>0?`<span>오늘 체결 <b>${Number(stats.ordersToday).toLocaleString("ko-KR")}</b>건</span>`:""}
    ${popular?`<span class="pp-pulse-popular">인기 보유 ${popular}</span>`:""}
  </div>`}function paperMemberRecoverBlock(){return`<div class="pp-onboard-recover">
    <span>다른 기기에 계좌가 있나요? 캡처해 둔 복구코드로 불러오세요</span>
    <input id="ppRecoverInput" type="text" placeholder="복구코드 (KOSPI-XXXX-XXXX)" autocomplete="one-time-code" autocapitalize="characters" spellcheck="false" value="${esc(paperOnboardDraft.recovery)}" ${paperBusy?"disabled":""}>
    <button class="pp-btn" type="button" data-pp-act="recover" ${paperBusy?"disabled":""}>계좌 불러오기</button>
  </div>`}function paperMemberOnboardingHtml(){const u=typeof currentAuthUser=="function"?currentAuthUser():null,name=esc(String(u?.nickname||u?.loginId||"회원").slice(0,24));return paperMemberStage==="new"?`<div class="pp-card pp-onboard">
      <div class="pp-onboard-head">
        <strong>새 모의투자 계좌 만들기 <em class="pp-beta-chip">BETA</em></strong>
        <span class="pp-onboard-hero pp-onboard-warn">⚠️ 새 계좌를 만들면, 예전에 다른 기기·복구코드로 쓰던 계좌와는 <b>${name}</b> 계정에서 <b>영영 다시 연결할 수 없어요.</b></span>
        <span>예전에 쓰던 계좌가 있다면 먼저 아래 복구코드로 불러오세요.</span>
      </div>
      <div class="pp-onboard-form">
        <label class="pp-field"><span>닉네임 (최대 12자)</span>
          <input id="ppNickInput" type="text" maxlength="12" placeholder="예: 단타의신" autocomplete="nickname" autocapitalize="off" spellcheck="false" value="${esc(paperOnboardDraft.nickname)}" ${paperBusy?"disabled":""}>
        </label>
        <button class="pp-btn pp-btn-primary" type="button" data-pp-act="member-create" ${paperBusy?"disabled":""}>${paperBusyText||"새 계좌로 시작"}</button>
        <button class="pp-btn" type="button" data-pp-act="member-cancel" ${paperBusy?"disabled":""}>뒤로</button>
      </div>
      ${paperMemberRecoverBlock()}
      ${paperNoticeHtml()}
    </div>`:`<div class="pp-card pp-onboard">
    <div class="pp-onboard-head">
      <strong>모의투자 시작하기 <em class="pp-beta-chip">BETA</em></strong>
      <span class="pp-onboard-hero"><b>${name}</b> 계정으로 모의투자를 시작해요.</span>
      <span>가상 자금 <b>5,000만원</b>으로 국내·해외주식·코인을 사고팔며 수익률을 겨뤄요. 시작할 때 이 기기에 쓰던 계좌가 있으면 자동으로 이 계정에 연결돼요.</span>
    </div>
    ${paperMarketPulseHtml()}
    <div class="pp-onboard-form">
      <button class="pp-btn pp-btn-primary" type="button" data-pp-act="member-start" ${paperBusy?"disabled":""}>${paperBusyText||"모의투자 시작하기"}</button>
    </div>
    ${paperMemberRecoverBlock()}
    ${paperNoticeHtml()}
  </div>`}function paperOnboardingHtml(){return paperIsLoggedIn()?paperMemberOnboardingHtml():`<div class="pp-card pp-onboard">
    <div class="pp-onboard-head">
      <strong>모의투자 시작하기 <em class="pp-beta-chip">BETA</em></strong>
      <span class="pp-onboard-hero">로그인 없이 닉네임만 정하면 바로 시작돼요.</span>
      <span>가상 자금 <b>5,000만원</b>으로 국내주식·해외주식·코인을 사고팔며 수익률 순위를 겨루는 연습 투자예요.</span>
    </div>
    ${paperMarketPulseHtml()}
    <div class="pp-onboard-steps">
      <span><i>1</i>닉네임을 입력하면 가상 5,000만원이 들어와요</span>
      <span><i>2</i>실제 시세로 주문하면 자동으로 체결돼요</span>
      <span><i>3</i>복구코드가 있으면 폰·PC 어디서든 이어서 할 수 있어요</span>
    </div>
    <div class="pp-onboard-form">
      <label class="pp-field"><span>닉네임 (최대 12자)</span>
        <input id="ppNickInput" type="text" maxlength="12" placeholder="예: 단타의신" autocomplete="nickname" autocapitalize="off" spellcheck="false" value="${esc(paperOnboardDraft.nickname)}" ${paperBusy?"disabled":""}>
      </label>
      <button class="pp-btn pp-btn-primary" type="button" data-pp-act="create" ${paperBusy?"disabled":""}>${paperBusyText||"계좌 만들기"}</button>
    </div>
    <div class="pp-onboard-recover">
      <span>이미 계좌가 있다면</span>
      <input id="ppRecoverInput" type="text" placeholder="복구코드 (KOSPI-XXXX-XXXX)" autocomplete="one-time-code" autocapitalize="characters" spellcheck="false" value="${esc(paperOnboardDraft.recovery)}" ${paperBusy?"disabled":""}>
      <button class="pp-btn" type="button" data-pp-act="recover" ${paperBusy?"disabled":""}>계좌 복구</button>
    </div>
    ${paperNoticeHtml()}
  </div>`}function paperModNoticeHtml(){const acct=paperData?.account;if(!acct)return"";const parts=[];if(acct.deleteAfter){const days=Math.max(0,Math.ceil((Date.parse(acct.deleteAfter)-Date.now())/864e5));parts.push(`<div class="pp-card pp-mod-warn pp-mod-danger" role="alert">
      <strong>⚠️ 계정 삭제 예정</strong>
      <span>계정에서 부적절한 활동이 감지되어 <b>${days}일 후 계정이 삭제</b>됩니다. 문의: <a href="mailto:excelkospi@outlook.com">excelkospi@outlook.com</a></span>
    </div>`)}return acct.modNotice==="nickname_changed"&&parts.push(`<div class="pp-card pp-mod-warn" role="alert">
      <span>부적절한 닉네임이 감지되어 <b>닉네임이 임의로 변경</b>되었습니다.</span>
      <button class="pp-btn pp-btn-sm" type="button" data-pp-act="ack-notice" ${paperBusy?"disabled":""}>확인</button>
    </div>`),parts.join("")}function paperCodeRevealHtml(){return paperRevealCode?`<div class="pp-card pp-code" role="alert">
    <div class="pp-card-head"><span class="pp-card-title">복구코드 저장</span></div>
    <div class="pp-code-row"><code>${esc(paperRevealCode)}</code>
      <button class="pp-btn" type="button" data-pp-act="copy-code" data-code="${esc(paperRevealCode)}" ${paperBusy?"disabled":""}>복사</button>
    </div>
    <span>이 코드가 계좌를 되찾는 유일한 방법이에요. 화면을 캡처하거나 메모에 적어 두세요.</span>
    <button class="pp-btn pp-btn-primary" type="button" data-pp-act="code-done" ${paperBusy?"disabled":""}>저장했어요</button>
  </div>`:""}function paperLinkHtml(){if(!paperLinkInfo)return"";const shareButton=paperUseNativeShare()?`<button class="pp-btn pp-btn-primary" type="button" data-pp-act="link-share" ${paperBusy?"disabled":""}>공유창 열기</button>`:"";return`<div class="pp-card pp-link">
    <div class="pp-card-head"><span class="pp-card-title">다른 기기 연결</span></div>
    <div class="pp-link-qr" id="ppLinkQr" aria-label="기기 연결 QR"></div>
    <span>휴대폰 카메라로 QR을 찍으면 같은 계좌가 열려요. 링크는 1회용이고 5분이 지나면 만료돼요.</span>
    <div class="pp-code-row"><code class="pp-link-url">${esc(paperLinkInfo.url)}</code>
      ${shareButton}
      <button class="pp-btn" type="button" data-pp-act="copy-code" data-code="${esc(paperLinkInfo.url)}" ${paperBusy?"disabled":""}>주소 복사</button>
      <button class="pp-btn" type="button" data-pp-act="link-close" ${paperBusy?"disabled":""}>닫기</button>
    </div>
  </div>`}function paperSummaryHtml(){const account=paperData.account,equity=paperEquity(),seed=paperAccountSeedCash(),returnPct=Number.isFinite(equity)?(equity/seed-1)*100:null,dir=paperDirClass(returnPct),profit=Number.isFinite(equity)?equity-seed:null,profitDir=paperDirClass(profit),invested=Math.max(0,equity-(Number(account.cash)||0)),me=paperLeaderboard?.me,ordersCount=paperOrderBreakdown(account).total,rankText=me?.rank?`${me.rank}위 / ${paperLeaderboard.total}명`:ordersCount>0?"순위 집계 중":"첫 주문하면 랭킹 진입",nickname=account.nickname||"내 계좌",nicknameCanChange=paperNicknameCanChange(account),nicknameButtonText=paperNicknameChangeButtonText(account);return`<div class="pp-summary pp-${dir}" data-pp-equity="${esc(paperNumberAttr(equity))}" data-pp-profit="${esc(paperNumberAttr(profit))}" data-pp-return-pct="${esc(paperNumberAttr(returnPct))}" data-pp-rank="${esc(paperNumberAttr(me?.rank))}" data-pp-total="${esc(paperNumberAttr(paperLeaderboard?.total))}" data-pp-orders="${esc(paperNumberAttr(ordersCount))}">
    <div class="pp-summary-head">
      <span class="pp-summary-title"><strong>excelkospi 모의투자</strong><span class="pp-summary-nickline"><em>${esc(nickname)}</em><button class="pp-nick-btn" type="button" data-pp-act="nickname-edit" ${paperBusy||!nicknameCanChange?"disabled":""} title="닉네임은 7일에 한 번 변경할 수 있어요">${esc(nicknameButtonText)}</button></span></span>
      <div class="pp-summary-head-tools">
        <span class="pp-summary-rank-badge"><em>랭킹</em><b>${esc(rankText)}</b></span>
      </div>
    </div>
    ${paperNicknameEditorHtml(nickname)}
    <div class="pp-summary-main">
      <span class="pp-cap">총 평가자산</span>
      <strong class="pp-summary-num">${paperWon(equity)}</strong>
      <span class="pp-summary-retline">
        <em class="pp-summary-ret ${dir}">수익률 ${paperSignedPct(returnPct)}</em>
        <em class="pp-summary-profit ${profitDir}">평가손익 ${paperWon(profit,!0)}</em>
        <small>시드 ${paperWon(seed)}</small>
      </span>
    </div>
    <div class="pp-summary-side" aria-label="계좌 구성">
      <span><em>현금</em><b>${paperWon(account.cash)}</b></span>
      <span><em>투자중</em><b>${paperWon(invested)}</b></span>
      <span><em>체결</em><b class="pp-trade-breakdown">${paperOrderBreakdownHtml(account)}</b></span>
    </div>
    ${paperBoardSharePanelHtml()}
    ${paperActionBarHtml()}
  </div>`}function paperActionBarHtml(){if(!paperData?.account)return"";const earned=paperEarnedBadgeIds(),pinnedSet=new Set(paperPinnedBadgeIds()),ordered=PAPER_BADGE_DEFS.filter(b=>earned.includes(b.id));return`<div class="pp-actionbar" aria-label="내 뱃지와 자랑 버튼">
    <span class="pp-actionbar-label">내 뱃지</span>
    <span class="pp-badge-chips" aria-label="획득한 뱃지">${ordered.length?ordered.map(b=>`<span class="pp-badge-chip${pinnedSet.has(b.id)?" pp-badge-pinned":""}" title="${esc(b.name)} · ${esc(b.desc)}${pinnedSet.has(b.id)?" · 닉네임 표시 중":""}">${esc(b.emoji)}</span>`).join(""):'<span class="pp-badge-empty">아직 뱃지가 없어요 — 첫 주문부터!</span>'}</span>
    <div class="pp-actionbar-btns">
      <button class="pp-btn pp-btn-sm" type="button" data-pp-act="badge-modal-open" ${paperBusy?"disabled":""}>내 닉네임에 뱃지 달기</button>
      <button class="pp-btn pp-btn-sm" type="button" data-pp-act="boast-chat" title="1시간에 2번까지 보낼 수 있어요" ${paperBusy?"disabled":""}>채팅에 자랑</button>
      <button class="pp-btn pp-btn-sm" type="button" data-pp-act="share-board-open" title="1시간에 2번까지 올릴 수 있어요" ${paperBusy?"disabled":""}>게시판에 자랑</button>
      <button class="pp-btn pp-btn-sm" type="button" data-pp-act="share-contest" title="친구가 이 링크로 들어오면 내 시드머니 +200만원 · 하루 1회" ${paperBusy?"disabled":""}>링크로 수익 자랑하기</button>
    </div>
    <small class="pp-share-reward-note">'링크로 수익 자랑하기' 링크를 공유해 그 사람이 방문하면 시드머니 200만원이 추가 지급됩니다. (일 1회)</small>
  </div>`}function paperBoardShareTradePickerHtml(){const trades=paperRecentShareTrades(12),selectedId=trades.some(order=>order.id===paperBoardShare?.tradeId)?paperBoardShare.tradeId:"",selected=selectedId?trades.find(order=>order.id===selectedId):null,options=['<option value="">전체 수익률 카드만 첨부</option>',...trades.map(order=>`<option value="${esc(order.id)}" ${order.id===selectedId?"selected":""}>${esc(paperShareTradeOptionText(order))}</option>`)].join(""),detail=selected?paperShareTradeProofHtml(selected,"pp-trade-proof"):trades.length?"":'<span class="pp-board-trade-empty">매수·매도 체결이 생기면 그 매매 손익 카드를 붙일 수 있어요.</span>';return`<div class="pp-board-trade-picker">
    <div class="pp-board-trade-control">
      <label for="ppBoardShareTrade">최근 매매 손익</label>
      <select id="ppBoardShareTrade" ${paperBusy||!trades.length?"disabled":""}>${options}</select>
    </div>
    ${detail?`<div class="pp-board-trade-detail">${detail}</div>`:""}
  </div>`}function paperBoardSharePanelHtml(){if(!paperBoardShare)return"";const channel=paperBoardShare.channel||"free";return`<div class="pp-board-share" role="group" aria-label="게시판에 자랑">
    <div class="pp-board-share-head"><span class="pp-cap">게시판에 자랑</span><span class="pp-board-share-hint">내 순위·수익률 카드가 함께 올라가요</span></div>
    <div class="pp-share-channels">${PAPER_SHARE_CHANNELS.map(c=>`<button class="pp-share-ch${c.key===channel?" is-on":""}" type="button" data-pp-act="share-board-channel" data-ch="${esc(c.key)}" ${paperBusy?"disabled":""}>${esc(c.label)}</button>`).join("")}</div>
    <input id="ppBoardShareTitle" class="pp-board-share-title" maxlength="60" placeholder="제목 (선택)" value="${esc(paperBoardShare.title||"")}" ${paperBusy?"disabled":""} />
    <textarea id="ppBoardShareComment" class="pp-board-share-comment" maxlength="500" rows="2" placeholder="내용 (선택)" ${paperBusy?"disabled":""}>${esc(paperBoardShare.body||paperBoardShare.comment||"")}</textarea>
    ${paperBoardShareTradePickerHtml()}
    <div class="pp-board-share-actions">
      <button class="pp-btn pp-btn-primary" type="button" data-pp-act="share-board-submit" ${paperBusy?"disabled":""}>자랑하기</button>
      <button class="pp-btn" type="button" data-pp-act="share-board-cancel" ${paperBusy?"disabled":""}>취소</button>
    </div>
  </div>`}function paperBadgeModalHtml(){if(!paperBadgeModalOpen)return"";const earned=new Set(paperEarnedBadgeIds()),pinned=paperPinnedBadgeIds(),pinnedSet=new Set(pinned),rows=PAPER_BADGE_DEFS.map(b=>{const got=earned.has(b.id),isPinned=pinnedSet.has(b.id),pinBtn=got?`<button class="pp-badge-pin-btn${isPinned?" is-on":""}" type="button" data-pp-act="badge-pin-toggle" data-badge="${esc(b.id)}" aria-pressed="${isPinned?"true":"false"}">${isPinned?"선택됨":"선택"}</button>`:'<span class="pp-badge-lock">미획득</span>';return`<li class="pp-badge-item${got?" is-earned":" is-locked"}">
      <span class="pp-badge-item-emoji">${esc(b.emoji)}</span>
      <span class="pp-badge-item-main"><b>${esc(b.name)}<i class="pp-badge-tier pp-badge-tier-${esc(b.tier)}" title="난이도 ${esc(b.tier)}">${esc(b.tier)}</i></b><small>${esc(b.desc)}</small></span>
      ${pinBtn}
    </li>`}).join(""),dirty=Array.isArray(paperPinDraft);return`<div class="pp-modal-backdrop" data-pp-act="badge-modal-close">
    <div class="pp-modal pp-badge-modal" role="dialog" aria-modal="true" aria-label="뱃지 도감" data-pp-stop>
      <div class="pp-modal-head"><strong>뱃지 도감</strong><span class="pp-badge-count">${earned.size}/${PAPER_BADGE_DEFS.length} 획득 · 이번 세션 ${pinned.length}/${PAPER_MAX_PINNED_BADGES}</span><button class="pp-modal-x" type="button" data-pp-act="badge-modal-close" aria-label="닫기">×</button></div>
      <ul class="pp-badge-list">${rows}</ul>
      <div class="pp-modal-foot">
        <button class="pp-btn pp-btn-primary" type="button" data-pp-act="badge-pin-save" ${paperBusy||!dirty?"disabled":""}>닉네임에 달기</button>
        <button class="pp-btn" type="button" data-pp-act="badge-modal-close" ${paperBusy?"disabled":""}>닫기</button>
        <small class="pp-badge-foot-note">최대 ${PAPER_MAX_PINNED_BADGES}개까지 이번 세션의 채팅·게시판 닉네임 옆에 표시돼요. 새로고침하면 다시 선택해 주세요.</small>
      </div>
    </div>
  </div>`}function paperTicketKrwPrice(){const t=paperTicket,quote=t.code?paperQuoteMap[`${t.code}:${t.market}`]:null,live=Number(quote?.price??t.price);if(!Number.isFinite(live)||live<=0)return null;if(t.market!=="US")return live;const fx=paperFx();return fx?live*fx:null}function paperQtyValueText(market,qty){const n=Number(qty);if(!Number.isFinite(n)||n<=0)return"";if(market==="COIN"){const rounded=Math.floor(n*1e6)/1e6;return rounded>0?String(rounded):""}const whole=Math.floor(n);return whole>0?String(whole):""}function paperHeldQty(){const pos=(paperData?.positions||[]).find(p=>p.code===paperTicket.code&&p.market===paperTicket.market),qty=Number(pos?.qty);return Number.isFinite(qty)&&qty>0?qty:0}function paperBuyQtyForCashPct(pct){const price=Number(paperTicketKrwPrice());if(!Number.isFinite(price)||price<=0||!paperData?.account)return"";const cash=Math.max(0,Number(paperData.account.cash)||0),feeRate=paperFeeRate(paperTicket.market).fee,max=cash*pct/(price*(1+feeRate));return paperQtyValueText(paperTicket.market,max)}function paperSellQtyForHoldingPct(pct){const held=paperHeldQty();if(!held)return"";if(paperTicket.market==="COIN")return paperQtyValueText("COIN",held*pct);const qty=Math.floor(held*pct);return String(Math.min(held,Math.max(1,qty)))}function paperQtyForPct(pct){return paperTicket.side==="sell"?paperSellQtyForHoldingPct(pct):paperBuyQtyForCashPct(pct)}function paperTicketMarketClosed(){const t=paperTicket,policy=paperData?.policy||{};return t.market==="KR"?policy.krOpen===!1:t.market==="US"?policy.usOpen===!1:!1}function paperTicketClosedCopy(market){return market==="US"?"미국주식은 프리장부터 애프터장까지만 주문할 수 있어요. 지금은 주문 시간이 아니에요.":market==="KR"?"국내주식은 거래일 08:00~20:00에만 주문할 수 있어요. ETF는 정규장만 가능해요.":"선택한 시장은 지금 주문할 수 없어요."}function paperTicketDisabledReason(){const t=paperTicket,hasQuote=t.code&&Number.isFinite(Number(t.price)),krwPrice=paperTicketKrwPrice(),qty=Number(t.qty),validQty=Number.isFinite(qty)&&qty>0;return paperBusy?{type:"busy",title:paperBusyText||"처리 중",detail:"잠시만 기다려 주세요."}:t.code?hasQuote?paperTicketMarketClosed()?{type:"closed",title:"거래시간 아님",detail:paperTicketClosedCopy(t.market)}:t.market==="US"&&krwPrice===null?{type:"fx",title:"환율 확인 중",detail:"원/달러 환율을 불러온 뒤 주문할 수 있어요."}:validQty?null:{type:"qty",title:"수량 입력 필요",detail:"주문할 수량을 입력하거나 빠른 수량 버튼을 눌러 주세요."}:{type:"quote",title:"시세 확인 필요",detail:"현재가를 먼저 확인해야 주문 금액을 계산할 수 있어요."}:{type:"symbol",title:"종목 선택 필요",detail:"종목을 검색하거나 자동완성 목록에서 선택해 주세요."}}const PAPER_FEE_FALLBACK=Object.freeze({KR:{fee:15e-5,sellTax:.002},US:{fee:.0025,sellTax:0},COIN:{fee:5e-4,sellTax:0}});function paperFeeRate(market){return(paperData?.policy?.fees||{})[market]||PAPER_FEE_FALLBACK[market]||PAPER_FEE_FALLBACK.KR}function paperPctText(rate){const pct=(Number(rate)||0)*100;return`${(Math.round(pct*1e3)/1e3).toLocaleString("ko-KR",{maximumFractionDigits:3})}%`}function paperFeeTooltip(market){const rate=paperFeeRate(market);return market==="COIN"?`수수료 ${paperPctText(rate.fee)} — 업비트 KRW 마켓 기준`:market==="US"?`수수료 ${paperPctText(rate.fee)} — 국내 증권사 해외주식 온라인 표준`:`매수·매도 수수료 ${paperPctText(rate.fee)} + 매도 시 증권거래세 ${paperPctText(rate.sellTax)} — 2026년 국내 표준 요율`}function paperTicketFootHtml(){const t=paperTicket,hasQuote=t.code&&Number.isFinite(Number(t.price)),krwPrice=paperTicketKrwPrice(),qty=Number(t.qty),validQty=Number.isFinite(qty)&&qty>0,feeRate=paperFeeRate(t.market),gross=krwPrice!==null&&validQty?krwPrice*qty:null,commission=gross!==null?gross*feeRate.fee:null,tax=gross!==null&&t.side==="sell"?gross*feeRate.sellTax:0,fee=gross!==null?commission+tax:null,estimate=gross!==null?t.side==="buy"?Math.ceil(gross+fee):Math.floor(gross-fee):null,disabledReason=paperTicketDisabledReason(),marketClosed=disabledReason?.type==="closed",disabled=!!disabledReason,buttonText=paperBusy?paperBusyText||"처리 중...":marketClosed?"거래시간 아님 · 주문 불가":estimate!==null?`${paperWon(estimate)} ${t.side==="buy"?"매수":"매도"}`:disabledReason?.title||`${t.side==="buy"?"매수":"매도"} 주문`;let idleText="종목과 수량을 입력하면 예상 체결 금액이 표시돼요";return disabledReason&&(idleText=disabledReason.detail),`<div class="pp-ticket-foot">
      ${estimate!==null?`<div class="pp-ticket-summary" aria-live="polite">
        <div class="pp-ts-line"><span>주문금액</span><b>${paperWon(Math.round(gross))}</b></div>
        <div class="pp-ts-line pp-ts-fee"><span>수수료 <i>${paperPctText(feeRate.fee)}</i></span><b>${paperWon(Math.round(commission))}</b></div>
        ${t.side==="sell"&&feeRate.sellTax>0?`<div class="pp-ts-line pp-ts-fee"><span>증권거래세 <i>${paperPctText(feeRate.sellTax)}</i></span><b>${paperWon(Math.round(tax))}</b></div>`:""}
        ${t.market==="US"?`<div class="pp-ts-line pp-ts-fee"><span>환율 적용</span><b>${paperTicketFxText()}</b></div>`:""}
        <div class="pp-ts-line pp-ts-total"><span>${t.side==="buy"?"총 매수금액":"예상 수령액"}</span><b>${paperWon(estimate)}</b></div>
      </div>`:`<div class="pp-ticket-summary pp-ts-idle ${marketClosed?"pp-ts-blocked":""}" aria-live="polite"><b>${esc(disabledReason?.title||"주문 준비")}</b><span>${esc(idleText)}</span></div>`}
      <button class="pp-btn pp-btn-primary pp-btn-${t.side} pp-order-submit" type="button" data-pp-act="submit-order" title="${esc(disabledReason?.detail||buttonText)}" ${disabled?"disabled":""}>${esc(buttonText)}</button>
    </div>`}function paperTicketFxText(){const fx=paperFx();return fx?`1$ = ${Math.round(fx).toLocaleString("ko-KR")}원`:"불러오는 중..."}function paperUpdateTicketFoot(){const foot=document.querySelector(".pp-ticket .pp-ticket-foot");foot&&(foot.outerHTML=paperTicketFootHtml())}function paperSessionLabel(state){const st=String(state||"").toUpperCase();return st==="PRE"?"프리장":st==="POST"?"애프터장":st==="REGULAR"?"정규장":st==="CLOSED"?"장 닫힘":""}function paperTicketHtml(){const t=paperTicket,hasQuote=t.code&&Number.isFinite(Number(t.price)),quote=t.code?paperQuoteMap[`${t.code}:${t.market}`]:null,livePrice=Number(quote?.price??t.price),krwPrice=paperTicketKrwPrice(),marketName=t.market==="COIN"?"코인 · 24시간":t.market==="US"?"해외주식":"국내주식",session=t.market==="COIN"?"":paperSessionLabel(t.session)||(paperTicketMarketClosed()?"장 닫힘":"거래 가능"),marketTag=[marketName,session].filter(Boolean).join(" · "),priceText=t.market==="US"?`$${livePrice.toLocaleString("en-US",{maximumFractionDigits:2})}${krwPrice?` ≈ ${Math.round(krwPrice).toLocaleString("ko-KR")}원`:""}`:`${livePrice.toLocaleString("ko-KR")}원`,cash=Number(paperData?.account?.cash)||0,held=paperHeldQty(),cashLine=t.side==="sell"&&t.code?`보유 <b>${paperQtyText(t.market,held)}${t.market==="COIN"?"":"주"}</b>`:`주문 가능 <b>${paperWon(cash)}</b>`;return`<div class="pp-card pp-ticket">
    <div class="pp-card-head"><span class="pp-card-title">주문하기</span>
      <button type="button" class="pp-fee-info" data-tip="${esc(paperFeeTooltip(t.market))}" aria-label="수수료 안내">수수료 안내</button>
      <span class="pp-ticket-cash">${cashLine}</span>
    </div>
    <div class="pp-side-seg" role="tablist" aria-label="매수/매도 선택">
      <button type="button" role="tab" aria-selected="${t.side==="buy"}" class="pp-side-btn buy ${t.side==="buy"?"on":""}" data-pp-act="side-buy" ${paperBusy?"disabled":""}>매수</button>
      <button type="button" role="tab" aria-selected="${t.side==="sell"}" class="pp-side-btn sell ${t.side==="sell"?"on":""}" data-pp-act="side-sell" ${paperBusy?"disabled":""}>매도</button>
    </div>
    <div class="pp-ticket-row">
      <input id="ppSymbolInput" type="text" placeholder="예: 삼성전자, 005930, TSLA, BTC" value="${esc(t.code||"")}" autocomplete="off" ${paperBusy?"disabled":""}>
      <button class="pp-btn" type="button" data-pp-act="lookup" ${paperBusy?"disabled":""}>${paperBusyText==="시세 확인 중..."?"확인 중...":"시세 확인"}</button>
    </div>
    <small class="pp-ticket-hint">목록에 없어도 종목명·코드로 검색할 수 있어요.</small>
    ${hasQuote?`<div class="pp-ticket-quote ${paperDirClass(quote?.changePct??t.changePct)}">
      <span class="pp-ticket-quote-id"><b>${esc(t.name||t.code)}</b><em>${esc(marketTag)}</em></span>
      <span class="pp-ticket-price"><strong>${esc(priceText)}</strong><em>${paperSignedPct(Number(quote?.changePct??t.changePct))}</em></span>
    </div>`:'<div class="pp-ticket-quote pp-muted">종목을 검색하면 현재가가 표시돼요. 국내주식·해외주식·코인을 지원해요.</div>'}
    <div class="pp-ticket-row pp-ticket-qty">
      <input id="ppQtyInput" type="number" min="0" step="${t.market==="COIN"?"0.000001":"1"}" placeholder="수량" value="${esc(String(t.qty||""))}" ${paperBusy?"disabled":""}>
      <button class="pp-btn" type="button" data-pp-act="qty-add" data-n="1" ${paperBusy?"disabled":""}>+1</button>
      <button class="pp-btn" type="button" data-pp-act="qty-add" data-n="10" ${paperBusy?"disabled":""}>+10</button>
      <button class="pp-btn" type="button" data-pp-act="qty-clear" ${paperBusy?"disabled":""}>초기화</button>
    </div>
    <div class="pp-ticket-quick" role="group" aria-label="주문 수량 빠른 선택">
      <span>${t.side==="sell"?"보유수량":"현금"} 기준</span>
      <button class="pp-btn" type="button" data-pp-act="qty-percent" data-pct="0.1" ${paperBusy?"disabled":""}>10%</button>
      <button class="pp-btn" type="button" data-pp-act="qty-percent" data-pct="0.25" ${paperBusy?"disabled":""}>25%</button>
      <button class="pp-btn" type="button" data-pp-act="qty-percent" data-pct="0.5" ${paperBusy?"disabled":""}>50%</button>
      <button class="pp-btn" type="button" data-pp-act="qty-percent" data-pct="1" ${paperBusy?"disabled":""}>최대</button>
    </div>
    ${paperTicketFootHtml()}
  </div>`}function paperPositionsHtml(){const positions=paperData?.positions||[];if(!positions.length)return`<div class="pp-card"><div class="pp-card-head"><span class="pp-card-title">보유 종목</span></div>
      <div class="pp-empty">아직 보유 종목이 없어요. 위 주문 카드에서 첫 종목을 매수해 보세요.</div></div>`;const rows=positions.map(pos=>{const mark=paperPositionMark(pos),qty=Number(pos.qty)||0,cost=qty*(Number(pos.avg_price)||0),value=qty*mark,pnl=value-cost,pnlPct=cost>0?pnl/cost*100:0,dir=paperDirClass(pnl);return`<div class="pp-pos-row">
      <div class="pp-pos-id"><b>${esc(pos.name||pos.code)}</b><em>${esc(pos.market==="COIN"?"코인":pos.market==="US"?"해외":"국내")} · ${paperQtyText(pos.market,qty)}${pos.market==="COIN"?"":"주"} · 평단 ${Math.round(pos.avg_price).toLocaleString("ko-KR")}</em></div>
      <div class="pp-pos-val"><strong>${paperWon(value)}</strong><em class="${dir}">${paperWon(pnl,!0)} (${paperSignedPct(pnlPct)})</em></div>
      <div class="pp-pos-act">
        <button class="pp-btn" type="button" data-pp-act="prefill" data-code="${esc(pos.code)}" data-market="${esc(pos.market)}" data-side="buy" ${paperBusy?"disabled":""}>추가</button>
        <button class="pp-btn" type="button" data-pp-act="prefill" data-code="${esc(pos.code)}" data-market="${esc(pos.market)}" data-side="sell" data-qty="${esc(String(qty))}" ${paperBusy?"disabled":""}>매도</button>
      </div>
    </div>`}).join("");return`<div class="pp-card"><div class="pp-card-head"><span class="pp-card-title">보유 종목</span><em>${positions.length}종목</em></div>${rows}</div>`}function paperRankHoldingsText(row){const names=paperRankHoldingItems(row).map(item=>String(item?.name||item?.code||"").trim()).filter(Boolean).slice(0,5);return names.length?`보유 ${names.join(" · ")}`:"현재 현금 보유"}const PAPER_RANK_HOLDINGS_INLINE_LIMIT=5;function paperRankHoldingItem(item){const name=String(item?.name||item?.code||"").trim();if(!name)return null;const code=String(item?.code||"").trim(),market=String(item?.market||"").trim(),weightRaw=item?.weightPct,hasWeight=weightRaw!=null&&String(weightRaw).trim()!==""&&Number.isFinite(Number(weightRaw));return!!(code||market||hasWeight)?{...item&&typeof item=="object"?item:{},name,code,market}:null}function paperRankHoldingItems(row){return(Array.isArray(row?.holdings)?row.holdings:[]).map(paperRankHoldingItem).filter(Boolean)}function paperRankRecentNames(row,fallback=[]){const out=[],add=value=>{const name=String(value||"").trim();name&&!out.includes(name)&&out.push(name)};return(Array.isArray(row?.recent)?row.recent:[]).forEach(add),fallback.forEach(add),out.slice(0,3)}function paperRankMetaHtml(row){const badges=(row?.badges||[]).join("");return badges?`<span class="pp-lb-meta"><i class="pp-badges" title="🐣 첫 체결 · ⚡ 거래 50회 · 🚀 수익률 10% · 👑 1위 경험">${esc(badges)}</i></span>`:""}function paperRankOrdersHtml(row){return`<i class="pp-lb-orders">${paperOrderBreakdownText(row)}</i>`}function paperRankOrdersCount(row){return paperOrderBreakdown(row).total}function paperPortfolioPctText(value){const n=Number(value);return Number.isFinite(n)?`${(Math.round(n*10)/10).toLocaleString("ko-KR",{maximumFractionDigits:1})}%`:""}function paperOrderBreakdown(row){const total=Math.max(0,Math.floor(Number(row?.ordersCount??row?.orders_count)||0)),buy=Math.max(0,Math.floor(Number(row?.buyOrdersCount??row?.buy_orders_count)||0)),sell=Math.max(0,Math.floor(Number(row?.sellOrdersCount??row?.sell_orders_count)||0));return{total:Math.max(total,buy+sell),buy,sell,hasSplit:buy+sell>0||total===0}}function paperOrderBreakdownText(row){const stats=paperOrderBreakdown(row);return stats.hasSplit?`매수 ${stats.buy.toLocaleString("ko-KR")}회 · 매도 ${stats.sell.toLocaleString("ko-KR")}회`:`거래 ${stats.total.toLocaleString("ko-KR")}회`}function paperOrderBreakdownHtml(row){const stats=paperOrderBreakdown(row);return stats.hasSplit?`<span class="pp-trade-cell buy"><i>매수</i><b>${stats.buy.toLocaleString("ko-KR")}회</b></span><span class="pp-trade-cell sell"><i>매도</i><b>${stats.sell.toLocaleString("ko-KR")}회</b></span>`:`<span class="pp-trade-cell"><i>거래</i><b>${stats.total.toLocaleString("ko-KR")}회</b></span>`}function paperRankHoldingsHtml(row){const rawHoldings=Array.isArray(row?.holdings)?row.holdings:[],holdings=paperRankHoldingItems(row);if(!holdings.length){const legacyRecent=rawHoldings.filter(item=>!paperRankHoldingItem(item)).map(item=>String(item?.name||item?.code||item||"").trim()).filter(Boolean),recent=paperRankRecentNames(row,legacyRecent);return`<span class="pp-lb-sub"><span class="pp-lb-holdings is-cash"><i>현금 보유</i>${recent.length?`<em class="pp-lb-recent">최근 거래 ${esc(recent.join(" · "))}</em>`:""}</span>${paperRankOrdersHtml(row)}</span>`}return`<span class="pp-lb-sub"><span class="pp-lb-holdings"><i>보유</i>${holdings.slice(0,PAPER_RANK_HOLDINGS_INLINE_LIMIT).map(item=>{const name=String(item?.name||item?.code||"").trim();return name?`<button type="button" class="pp-lb-pill" data-pp-act="pill-quote" data-code="${esc(item?.code||"")}" data-market="${esc(item?.market||"")}" title="주문창에서 이 종목 시세 보기">${esc(name)}</button>`:""}).join("")}</span>${paperRankOrdersHtml(row)}</span>`}function paperRankPortfolioPreviewHtml(row){const weights=paperRankHoldingItems(row).filter(item=>Number.isFinite(Number(item?.weightPct))&&Number(item.weightPct)>0).sort((a,b)=>Number(b.weightPct)-Number(a.weightPct));if(!weights.length){const recent=paperRankRecentNames(row),suffix=recent.length?` · 최근 ${recent.slice(0,2).join(" · ")}`:"";return`<span class="pp-lb-portfolio is-cash"><i>포트폴리오</i><em>현금 100%${esc(suffix)}</em></span>`}const sum=weights.reduce((total,item)=>total+(Number(item.weightPct)||0),0),cashPct=Math.max(0,Math.round((100-sum)*10)/10),fullParts=weights.map(item=>`${String(item.name||item.code)} ${paperPortfolioPctText(item.weightPct)}`);cashPct>.1&&fullParts.push(`현금 ${paperPortfolioPctText(cashPct)}`);const visibleParts=weights.slice(0,3).map(item=>`${String(item.name||item.code)} ${paperPortfolioPctText(item.weightPct)}`);return cashPct>.1&&visibleParts.push(`현금 ${paperPortfolioPctText(cashPct)}`),`<span class="pp-lb-portfolio" title="${esc(fullParts.join(" · "))}"><i>포트폴리오</i>${visibleParts.map(part=>`<em>${esc(part)}</em>`).join("")}</span>`}function paperRankMoneyHtml(row,pct,dir,options={}){const weekly=options.weekly?`<small class="pp-lb-week">주간 ${paperSignedPct(row.weeklyReturnPct,1)}</small>`:"";return`<span class="pp-lb-money"><em class="pp-lb-ret ${dir}">${paperSignedPct(pct)}${weekly}</em><span class="pp-lb-equity">${paperWon(row.equity)}</span></span>`}function paperRankMainHtml(row,isMe=!1){return`<span class="pp-lb-main"><span class="pp-lb-head"><b class="pp-lb-nick">${esc(row.nickname)}${isMe?" (나)":""}</b>${paperRankMetaHtml(row)}</span>${paperRankHoldingsHtml(row)}${paperRankPortfolioPreviewHtml(row)}</span>`}let paperExpandedRankKey="";function paperRankDetailHtml(row){if(!paperExpandedRankKey||paperRankRowKey(row)!==paperExpandedRankKey)return"";const weights=(row?.holdings||[]).filter(h=>Number.isFinite(Number(h?.weightPct)));if(!weights.length){const recent=(row?.recent||[]).slice(0,3).join(" · ");return`<div class="pp-lb-detail">현금 100%${recent?` · 최근 거래 ${esc(recent)}`:""}</div>`}const sum=weights.reduce((s,h)=>s+Number(h.weightPct),0),cashPct=Math.max(0,Math.round((100-sum)*10)/10),parts=weights.map(h=>`${String(h.name||h.code)} ${Number(h.weightPct)}%`);return cashPct>.1&&parts.push(`현금 ${cashPct}%`),`<div class="pp-lb-detail">포트폴리오 — ${esc(parts.join(" · "))}</div>`}function paperRankRowKey(row){return`${String(row?.nickname||"")}|${String(row?.createdAt||"")}`}function paperRankComparableRows(lb){const out=[...Array.isArray(lb?.participants)&&lb.participants.length?lb.participants:lb?.top||[]];return lb?.me?.rank&&out.push(lb.me),out.filter(row=>paperRankRowKey(row).trim()!=="|")}function paperPrepareRankMotion(prev,next){if(paperRankMotion=new Map,paperRankMotionUntil=0,!prev?.updatedAt||!next?.updatedAt||prev.updatedAt===next.updatedAt)return;const before=new Map;for(const row of paperRankComparableRows(prev))before.set(paperRankRowKey(row),{rank:Number(row.rank),equity:Math.round(Number(row.equity)||0),returnPct:Math.round((Number(row.returnPct)||0)*100)/100});for(const row of paperRankComparableRows(next)){const key=paperRankRowKey(row),old=before.get(key);if(!old)continue;const rank=Number(row.rank),equity=Math.round(Number(row.equity)||0),returnPct=Math.round((Number(row.returnPct)||0)*100)/100;Number.isFinite(rank)&&Number.isFinite(old.rank)&&rank<old.rank?paperRankMotion.set(key,"pp-rank-up"):Number.isFinite(rank)&&Number.isFinite(old.rank)&&rank>old.rank?paperRankMotion.set(key,"pp-rank-down"):(equity!==old.equity||returnPct!==old.returnPct)&&paperRankMotion.set(key,"pp-rank-change")}paperRankMotion.size&&(paperRankMotionUntil=Date.now()+2400)}function paperRankMotionClass(row){return!paperRankMotion.size||Date.now()>paperRankMotionUntil?(paperRankMotion=new Map,paperRankMotionUntil=0,""):paperRankMotion.get(paperRankRowKey(row))||""}function paperParticipantRows(lb=paperLeaderboard){const rows=Array.isArray(lb?.participants)&&lb.participants.length?lb.participants:lb?.top||[],cfg=PAPER_RANK_WINDOWS.find(item=>item.key===paperRankWindow)||PAPER_RANK_WINDOWS[PAPER_RANK_WINDOWS.length-1],cutoff=cfg.days?Date.now()-cfg.days*24*60*60*1e3:0,byRank=(a,b)=>(Number(a.rank)||999999)-(Number(b.rank)||999999);return rows.filter(row=>{if(!cutoff)return!0;const activeAt=Date.parse(row.lastActiveAt||row.createdAt||"");return Number.isFinite(activeAt)&&activeAt>=cutoff}).sort((a,b)=>{if(paperParticipantSort==="orders"){const diff=paperRankOrdersCount(b)-paperRankOrdersCount(a);if(diff)return diff}return byRank(a,b)})}function paperParticipantFiltersHtml(){const sortOptions=[{key:"rank",label:"수익률순"},{key:"orders",label:"거래횟수순"}];return`<div class="pp-lb-filters" role="group" aria-label="참가자 최근 활동 기준">
    <span>최근 활동</span>
    ${PAPER_RANK_WINDOWS.map(item=>`<button class="pp-btn ${paperRankWindow===item.key?"on":""}" type="button" data-pp-act="rank-window" data-window="${esc(item.key)}">${esc(item.label)}</button>`).join("")}
    <span>정렬</span>
    ${sortOptions.map(item=>`<button class="pp-btn ${paperParticipantSort===item.key?"on":""}" type="button" data-pp-act="participant-sort" data-sort="${esc(item.key)}">${esc(item.label)}</button>`).join("")}
  </div>`}function paperParticipantListHtml(){return paperShowParticipants?paperParticipantsInlineHtml():`<div class="pp-participants pp-participants-collapsed">
      <button class="pp-btn pp-btn-ghost" type="button" data-pp-act="toggle-participants">전체 참가자 목록 보기 ›</button>
    </div>`}function paperParticipantSortLabel(){return paperParticipantSort==="orders"?"거래횟수순":"수익률순"}function paperParticipantRankLabel(row,index){if(paperParticipantSort==="orders")return`${index+1}위`;const rank=Number(row?.rank);return Number.isFinite(rank)&&rank>0?`${rank}위`:"-"}function paperParticipantsInlineHtml(lb=paperLeaderboard){const rows=paperParticipantRows(lb),headText=Array.isArray(lb?.participants)?`${rows.length}명 표시 · ${paperParticipantSortLabel()}`:`상위 ${rows.length}명 먼저 표시 · ${paperParticipantsError||"불러오는 중"}`,body=rows.length?rows.map((row,index)=>{const dir=paperDirClass(row.returnPct),isMe=lb?.me?.rank===row.rank&&lb?.me?.nickname===row.nickname,rankTitle=paperParticipantSort==="orders"?`거래횟수순 ${index+1}위 · 수익률 ${Number(row.rank)||"-"}위`:`수익률 ${Number(row.rank)||"-"}위`;return`<div class="pp-lb-row pp-participant-row ${isMe?"is-me":""} ${paperRankMotionClass(row)}" data-pp-act="rank-expand" data-rankkey="${esc(paperRankRowKey(row))}" role="button" tabindex="0">
      <span class="pp-lb-rank" title="${esc(rankTitle)}">${paperParticipantRankLabel(row,index)}</span>
      ${paperRankMainHtml(row,isMe)}
      ${paperRankMoneyHtml(row,row.returnPct,dir,{weekly:!0})}
    </div>${paperRankDetailHtml(row)}`}).join(""):'<div class="pp-empty">이 기간에 활동한 참가자가 아직 없어요.</div>';return`<div class="pp-participants pp-participants-expanded">
    <div class="pp-participants-head"><span class="pp-card-title">참가자 전체 목록</span><em>${esc(headText)} · <button class="pp-link-btn" type="button" data-pp-act="toggle-participants">접기</button></em></div>
    ${paperParticipantFiltersHtml()}
    <div class="pp-participant-list">${body}</div>
  </div>`}function paperParticipantListScrollState(){const list=document.querySelector(".pp-participant-list");return list?{top:list.scrollTop||0,left:list.scrollLeft||0}:null}function paperRestoreParticipantListScroll(state){if(!state)return;const list=document.querySelector(".pp-participant-list");list&&(list.scrollTop=state.top||0,list.scrollLeft=state.left||0)}function renderPaperTabKeepParticipantScroll(){const scrollState=paperParticipantListScrollState();renderPaperTab(),paperRestoreParticipantListScroll(scrollState)}function paperRecentTradeRows(limit=50){return(paperLeaderboard?.stats?.recentTrades||[]).slice(0,Math.max(0,Math.min(50,Number(limit)||50)))}function paperLeagueTradeStats(){const trades=paperRecentTradeRows(),nickMap=new Map,symbolMap=new Map;for(const trade of trades){const nickname=String(trade?.nickname||"참가자").trim()||"참가자",nick=nickMap.get(nickname)||{nickname,count:0};nick.count+=1,nickMap.set(nickname,nick);const name=String(trade?.name||trade?.code||"").trim();if(!name)continue;const market=String(trade?.market||"").toUpperCase(),key=`${name}|${market}`,symbol=symbolMap.get(key)||{name,market,count:0};symbol.count+=1,symbolMap.set(key,symbol)}const byCount=(a,b)=>b.count-a.count||String(a.nickname||a.name||"").localeCompare(String(b.nickname||b.name||""),"ko");return{trades,tradeKings:[...nickMap.values()].sort(byCount),hotSymbols:[...symbolMap.values()].sort(byCount)}}function paperSpotlightItemHtml(kind,kicker,title,meta,body=""){return`<div class="pp-spotlight-item pp-spotlight-${esc(kind)}">
    <span class="pp-spot-kicker">${esc(kicker)}</span>
    <b class="pp-spot-title">${esc(title)}</b>
    <em class="pp-spot-meta">${esc(meta)}</em>
    ${body}
  </div>`}function paperTradeKingSpotlightHtml(stats){const king=stats.tradeKings[0];return king?paperSpotlightItemHtml("trade-king","오늘의 거래왕",king.nickname,`최근 체결 ${king.count.toLocaleString("ko-KR")}건`):paperSpotlightItemHtml("quiet","오늘의 거래왕","집계 대기","최근 체결이 쌓이면 표시돼요")}function paperRankRiserSpotlightHtml(){let row=(paperLeaderboard?.stats?.rankRisers||[]).filter(row2=>Number(row2?.dailyRankRise)>0||Number(row2?.weeklyRankRise)>0)[0]||null,meta="";if(row){const daily=Number(row.dailyRankRise)||0,weekly=Number(row.weeklyRankRise)||0;meta=daily>0?`오늘 ${daily.toLocaleString("ko-KR")}계단 상승 · ${Number(row.rank)||"-"}위`:`주간 ${weekly.toLocaleString("ko-KR")}계단 상승 · ${Number(row.rank)||"-"}위`}else row=paperParticipantRows(paperLeaderboard).filter(item=>Number(item?.weeklyReturnPct)>0).sort((a,b)=>(Number(b.weeklyReturnPct)||0)-(Number(a.weeklyReturnPct)||0))[0]||null,row&&(meta=`주간 ${paperSignedPct(row.weeklyReturnPct,1)} · ${Number(row.rank)||"-"}위`);return row?paperSpotlightItemHtml("riser","역전왕",row.nickname||"참가자",meta):paperSpotlightItemHtml("quiet","역전왕","순위 변동 대기","일·주 시작 순위와 비교해요")}function paperHotSymbolSpotlightHtml(stats){const symbols=stats.hotSymbols.slice(0,3);if(!symbols.length)return paperSpotlightItemHtml("quiet","종목 유행","집계 대기","최근 체결 종목이 쌓이면 표시돼요");const top=symbols[0],tags=`<span class="pp-spot-tags">${symbols.map(item=>`<i>${esc(item.name)} ${item.count.toLocaleString("ko-KR")}</i>`).join("")}</span>`,market=paperTradeMarketLabel(top.market);return paperSpotlightItemHtml("hot-symbol","종목 유행",top.name,`${market?`${market} · `:""}최근 ${top.count.toLocaleString("ko-KR")}건`,tags)}function paperWeeklyMissionItems(){const account=paperData?.account;if(!account)return[{label:"첫 해외주식",done:!1,value:"대기"},{label:"현금 20%",done:!1,value:"대기"}];const hasUs=(paperData?.positions||[]).some(pos=>String(pos.market).toUpperCase()==="US")||(paperData?.orders||[]).some(order=>String(order.market).toUpperCase()==="US"),equity=paperEquity(),cashPct=equity>0?Math.max(0,Math.round((Number(account.cash)||0)/equity*100)):0;return[{label:"첫 해외주식",done:hasUs,value:hasUs?"완료":"대기"},{label:"현금 20%",done:cashPct>=20,value:`${cashPct}%`}]}function paperWeeklyMissionSpotlightHtml(){const missions=paperWeeklyMissionItems(),done=missions.filter(item=>item.done).length,title=paperData?.account?`${done}/${missions.length} 완료`:"계좌 만들면 시작",meta=paperData?.account?missions.map(item=>`${item.label} ${item.done?"완료":item.value}`).join(" · "):"이번 주 가벼운 목표",tags=`<span class="pp-spot-tags pp-mission-tags">${missions.map(item=>`<i class="${item.done?"done":""}">${esc(item.label)} ${esc(item.done?"완료":item.value)}</i>`).join("")}</span>`;return paperSpotlightItemHtml("mission","주간 미션",title,meta,tags)}function paperLeagueSpotlightHtml(){const stats=paperLeagueTradeStats(),items=[paperTradeKingSpotlightHtml(stats),paperRankRiserSpotlightHtml(),paperHotSymbolSpotlightHtml(stats),paperWeeklyMissionSpotlightHtml()].filter(Boolean);return items.length?`<div class="pp-card pp-spotlight">
    <div class="pp-card-head"><span class="pp-card-title">리그 관전</span><em>리더보드 캐시 기준</em></div>
    <div class="pp-spotlight-grid">${items.join("")}</div>
  </div>`:""}function paperLeagueDeckHtml(options={}){const spotlight=paperLeagueSpotlightHtml(),ranking=paperLeaderboardHtml(!!options.rankingCompact),trades=paperPublicRecentTradesHtml(!!options.tradesCompact);return`<div class="pp-league-wrap">${spotlight}<div class="pp-league-grid">${ranking}${trades}</div></div>`}function paperLeaderboardHtml(compact=!1){const lb=paperLeaderboard;if(!lb?.top?.length)return`<div class="pp-card"><div class="pp-card-head"><span class="pp-card-title">수익률 랭킹</span></div>
      <div class="pp-empty">아직 랭커가 없어요. 지금 시작하면 1위!</div>
      ${compact?"":paperParticipantListHtml(lb)}</div>`;const medals=["🥇","🥈","🥉"],meNick=lb.me?.rank?lb.me.nickname:"",week=paperRankScope==="week",baseRows=week&&Array.isArray(lb.participants)&&lb.participants.length?[...lb.participants]:[...lb.top];week&&baseRows.sort((a,b)=>(Number(b.weeklyReturnPct)||0)-(Number(a.weeklyReturnPct)||0));const rows=baseRows.slice(0,compact?5:10).map((row,index)=>{const pct=week?row.weeklyReturnPct:row.returnPct,dir=paperDirClass(pct),isMe=lb.me?.rank===row.rank&&row.nickname===meNick,rankNum=week?index+1:Number(row.rank);return`<div class="pp-lb-row ${isMe?"is-me":""} ${paperRankMotionClass(row)}" data-pp-act="rank-expand" data-rankkey="${esc(paperRankRowKey(row))}" role="button" tabindex="0" title="클릭하면 포트폴리오 비중을 볼 수 있어요">
      <span class="pp-lb-rank">${rankNum<=3?medals[rankNum-1]:`${rankNum}위`}</span>
      ${paperRankMainHtml(row,isMe)}
      ${paperRankMoneyHtml(row,pct,dir)}
    </div>${paperRankDetailHtml(row)}`}).join(""),meRow=!week&&lb.me?.rank&&lb.me.rank>(compact?5:10)?`<div class="pp-lb-row is-me ${paperRankMotionClass(lb.me)}" data-pp-act="rank-expand" data-rankkey="${esc(paperRankRowKey(lb.me))}" role="button" tabindex="0"><span class="pp-lb-rank">${lb.me.rank}위</span>${paperRankMainHtml(lb.me,!0)}${paperRankMoneyHtml(lb.me,lb.me.returnPct,paperDirClass(lb.me.returnPct))}</div>${paperRankDetailHtml(lb.me)}`:"",updated=typeof relativeTimeKR=="function"&&lb.updatedAt?relativeTimeKR(lb.updatedAt):"";return`<div class="pp-card">
    <div class="pp-card-head"><span class="pp-card-title">수익률 랭킹${week?" · 주간(월요일 리셋)":""}</span><em>${compact?"":`<span class="pp-scope-toggle" role="group" aria-label="랭킹 기간">
      <button class="pp-btn ${week?"":"on"}" type="button" data-pp-act="rank-scope" data-scope="all">누적</button>
      <button class="pp-btn ${week?"on":""}" type="button" data-pp-act="rank-scope" data-scope="week">주간</button>
    </span>`}참가 ${lb.total}명${updated?` · ${esc(updated)} 집계`:""}</em></div>
    ${rows}${meRow}
    <p class="pp-lb-note">순위는 약 1분마다 자동으로 다시 계산돼요. 참가자를 클릭하면 보유 종목 비중을 볼 수 있어요.</p>
    ${compact?"":paperParticipantListHtml(lb)}
  </div>`}function paperOrdersHtml(){const orders=paperData?.orders||[];return orders.length?`<div class="pp-card"><div class="pp-card-head"><span class="pp-card-title">최근 체결</span></div>${orders.slice(0,8).map(order=>{const sideText=order.side==="sell"?"매도":"매수",realized=Number(order.realized),time=typeof relativeTimeKR=="function"?relativeTimeKR(order.created_at):"";return`<div class="pp-order-row ${order.side}">
      <span class="pp-order-side">${sideText}</span>
      <b>${esc(order.name||order.code)}</b>
      <em>${paperQtyText(order.market,order.qty)} × ${Math.round(order.price).toLocaleString("ko-KR")}</em>
      <span class="pp-order-amt">${paperWon(order.amount)}${Number.isFinite(realized)&&order.side==="sell"?` <i class="${paperDirClass(realized)}">${paperWon(realized,!0)}</i>`:""}</span>
      <small>${esc(time)}</small>
    </div>`}).join("")}</div>`:""}function paperTradeMarketLabel(market){return market==="COIN"?"코인":market==="US"?"해외":market==="KR"?"국내":""}function paperPublicRecentTradesHtml(compact=!1){const trades=(paperLeaderboard?.stats?.recentTrades||[]).slice(0,compact?8:14);return trades.length?`<div class="pp-card pp-public-trades">
    <div class="pp-card-head"><span class="pp-card-title">전체 참가자 최근 체결</span><em>리더보드 캐시</em></div>
    <div class="pp-public-trade-list">${trades.map(trade=>{const side=trade.side==="sell"?"sell":"buy",sideText=side==="sell"?"매도":"매수",market=String(trade.market||"").toUpperCase(),marketText=paperTradeMarketLabel(market),qty=Number(trade.qty),qtyText=Number.isFinite(qty)&&qty>0?` · ${paperQtyText(market,qty)}${market==="COIN"?"":"주"}`:"",time=typeof relativeTimeKR=="function"&&trade.createdAt?relativeTimeKR(trade.createdAt):"";return`<div class="pp-public-trade-row ${side}">
      <span class="pp-public-trade-side">${sideText}</span>
      <b>${esc(trade.name||trade.code||"종목")}</b>
      <em>${esc(trade.nickname||"참가자")} · ${esc(marketText)}${esc(qtyText)}</em>
      <small>${esc(time)}</small>
    </div>`}).join("")}</div>
  </div>`:""}function paperFootHtml(){return`<div class="pp-foot">
    <button class="pp-btn" type="button" data-pp-act="qr" ${paperBusy?"disabled":""}>QR/공유로 기기 연결</button>
    <button class="pp-btn" type="button" data-pp-act="reissue" ${paperBusy?"disabled":""}>복구코드 재발급</button>
    <button class="pp-btn" type="button" data-pp-act="refresh" ${paperLoadInFlight||paperBusy?"disabled":""}>${paperLoadingText?"새로고침 중...":"새로고침"}</button>
  </div>${paperNoticeHtml()}`}function paperTableHtml(){return paperError&&!paperData?paperDashWrap('<div class="pp pp-state">모의투자 정보를 불러오지 못했어요. 잠시 후 자동으로 다시 시도해요.<button class="pp-btn" type="button" data-pp-act="refresh">다시 불러오기</button></div>'):paperData?paperData.account?paperDashWrap(`<div class="pp">
    ${paperPageHeaderHtml()}
    ${paperModNoticeHtml()}
    ${paperProgressHtml()}
    ${paperCodeRevealHtml()}
    ${paperRecoveryBannerHtml()}
    ${paperSummaryHtml()}
    <div class="pp-grid">
      <div class="pp-col">${paperTicketHtml()}${paperPositionsHtml()}${paperOrdersHtml()}</div>
      <div class="pp-col pp-league-col">${paperLeagueDeckHtml({tradesCompact:!0})}</div>
    </div>
    ${paperLinkHtml()}
    ${paperFootHtml()}
    ${paperBadgeModalHtml()}
  </div>`):paperDashWrap(`<div class="pp">${paperPageHeaderHtml()}${paperProgressHtml()}${paperCodeRevealHtml()}${paperOnboardingHtml()}${paperLeagueDeckHtml({tradesCompact:!1})}</div>`):paperDashWrap('<div class="pp pp-state"><span class="news-loading-spin"></span><span>모의투자 데이터를 불러오는 중...</span></div>')}function renderPaperTab(){const table=document.getElementById("timelineTable");if(!table)return;paperSyncOnboardDraftFromDom();const onboardFocus=paperOnboardFocusState();setTimelineTableMode(table,"forecast");try{table.innerHTML=paperTableHtml()}catch(error){try{debugWarn("paper render failed",error)}catch{}table.innerHTML=paperDashWrap('<div class="pp pp-state">화면 표시 중 오류가 났어요. 잠시 후 자동으로 다시 시도해요.</div>');return}const hint=document.getElementById("timelineHint");hint&&paperTabActive()&&(hint.textContent=""),paperInitSymbolAutocomplete(),paperLinkInfo&&paperRenderLinkQr(),paperRestoreOnboardFocus(onboardFocus)}let paperQrLibPromise=null;function paperLoadQrLib(){return typeof window.qrcode=="function"?Promise.resolve(window.qrcode):paperQrLibPromise||(paperQrLibPromise=new Promise((resolve,reject)=>{const script=document.createElement("script");script.src="/assets/vendor/qrcode.js?v=20260613-2584",script.async=!0,script.onload=()=>{typeof window.qrcode=="function"?resolve(window.qrcode):reject(new Error("QR unavailable"))},script.onerror=reject,document.head.appendChild(script)}).catch(error=>{throw paperQrLibPromise=null,error}),paperQrLibPromise)}function paperRenderLinkQr(){const box=document.getElementById("ppLinkQr");if(!box||!paperLinkInfo)return;const url=paperLinkInfo.url;paperLoadQrLib().then(qrFactory=>{const qr=qrFactory(0,"M");qr.addData(url),qr.make(),paperLinkInfo?.url===url&&(box.innerHTML=qr.createSvgTag(4,2,"모의투자 기기 연결 QR","excelkospi 모의투자 기기 연결"))}).catch(()=>{box.innerHTML='<span class="pp-muted">QR 생성 실패 — 아래 주소를 복사해 주세요.</span>'})}function paperInitSymbolAutocomplete(){paperSymbolAutocomplete?.destroy?.(),paperSymbolAutocomplete=null;const input=document.getElementById("ppSymbolInput"),panel=input?.closest?.(".pp-ticket");!input||!panel||!window.StockAutocomplete?.create||(paperSymbolAutocomplete=window.StockAutocomplete.create({input,panel,id:"paperStockAcList",sources:{KR:"/data/kr-stocks.json?v=20260613-2584",US:"/data/us-stocks.json?v=20260613-2584",COIN:"/data/coin-stocks.json?v=20260614-2645"},getActiveMarket:()=>"KR+US+COIN",filterItem:item=>item.market==="KR"||item.market==="US"||item.market==="COIN",onPick:paperPickSymbolSuggestion}))}function paperPickSymbolSuggestion(code,market,item={}){const nextCode=String(code||"").toUpperCase(),nextMarket=String(market||"KR").toUpperCase();nextCode&&(paperReadTicketInputs(),paperTicket={...paperTicket,code:nextCode,market:nextMarket,name:String(item?.name||nextCode),price:null,changePct:null,session:""},paperLookupSymbol(nextCode,nextMarket).catch(e=>debugWarn("paper autocomplete lookup failed",e)))}function paperRankStripHtml(){const lb=paperLeaderboard;if(!lb?.top?.length)return"";const medals=["🥇","🥈","🥉"],top=lb.top.slice(0,3).map(row=>`<em>${medals[row.rank-1]||`${row.rank}위`} ${esc(row.nickname)} <i class="${paperDirClass(row.returnPct)}">${paperSignedPct(row.returnPct,1)}</i></em>`).join(""),me=lb.me?.rank?`<em class="pp-strip-me">나 ${lb.me.rank}위</em>`:"";return`<button type="button" class="pp-strip" data-pp-act="open-tab" aria-label="모의투자 랭킹 보기">
    <b>모의투자 랭킹</b>${top}${me}<span>참여하기 ›</span>
  </button>`}document.addEventListener("timeline-tab-updated",ev=>{String(ev?.detail?.key||"")==="forecast"&&!paperLeaderboard&&paperFetchLeaderboard().then(()=>{paperLeaderboard?.top?.length&&typeof timelineIsForecast=="function"&&timelineIsForecast()&&typeof renderMarketForecast=="function"&&renderMarketForecast()}).catch(()=>null)});function paperReadTicketInputs(){const symbolInput=document.getElementById("ppSymbolInput"),qtyInput=document.getElementById("ppQtyInput");return qtyInput&&(paperTicket.qty=qtyInput.value),{symbol:symbolInput?.value?.trim()||"",qty:qtyInput?.value||""}}async function paperLookupSymbol(symbolOverride="",marketOverride=""){if(paperBusy)return;const{symbol:typedSymbol}=paperReadTicketInputs(),symbol=String(symbolOverride||typedSymbol||"").trim();if(!symbol){showToast?.("종목명이나 코드를 입력해 주세요","err");return}let focusQty=!1;paperSetBusy("시세 확인 중...");try{const marketParam=String(marketOverride||"AUTO").toUpperCase(),data=await(await fetch(paperApiUrl(`/api/quote?code=${encodeURIComponent(symbol)}&market=${encodeURIComponent(marketParam)}&ttl=60&coinSource=upbit`))).json();if(!data?.ok||!Number.isFinite(Number(data.price)))throw new Error(data?.error||"시세를 찾지 못했어요");const market=String(data.market||"").toUpperCase();if(market!=="KR"&&market!=="COIN"&&market!=="US"){showToast?.("지금은 국내주식·해외주식·코인만 모의투자할 수 있어요","err");return}if(paperTicket={...paperTicket,code:String(data.code||symbol).toUpperCase(),market,name:String(data.name||symbol),price:Number(data.price),changePct:Number(data.changePct??data.changePercent),session:String(data.marketState||"")},paperQuoteMap[`${paperTicket.code}:${market}`]={price:Number(data.price),changePct:paperTicket.changePct},market==="US"&&!paperFx())try{const fx=await(await fetch(paperApiUrl("/api/quote?code=KRW%3DX&market=US&ttl=60"))).json();fx?.ok&&Number.isFinite(Number(fx.price))&&(paperQuoteMap["KRW=X:US"]={price:Number(fx.price),changePct:Number(fx.changePct)})}catch{}focusQty=!0}catch(e){showToast?.(e?.message||"시세 조회에 실패했어요","err")}finally{paperClearBusy(),focusQty&&setTimeout(()=>document.getElementById("ppQtyInput")?.focus(),0)}}async function paperSubmitOrder(){if(paperBusy)return;paperReadTicketInputs();const disabledReason=paperTicketDisabledReason();if(disabledReason){showToast?.(disabledReason.detail||disabledReason.title||"주문 정보를 확인해 주세요",disabledReason.type==="closed"?"warn":"err");return}const qty=Number(paperTicket.qty);if(!paperTicket.code||!Number.isFinite(qty)||qty<=0){showToast?.("종목과 수량을 확인해 주세요","err");return}paperSetBusy(`${paperTicket.side==="buy"?"매수":"매도"} 주문 처리 중...`);try{const order=(await paperPost("order",{code:paperTicket.code,market:paperTicket.market,side:paperTicket.side,qty}))?.order;showToast?.(`${order?.name||paperTicket.code} ${paperTicket.side==="buy"?"매수":"매도"} 체결 — ${paperWon(order?.amount)}`,"info"),paperTicket.qty="",paperLeaderboardAt=0,await loadPaperTab({forceLb:!0,silent:!0}).catch(()=>null)}catch(e){showToast?.(e?.message||"주문에 실패했어요","err")}finally{paperClearBusy()}}async function paperCopyText(text,options={}){let copied=!1;try{navigator.clipboard?.writeText&&window.isSecureContext!==!1&&(await navigator.clipboard.writeText(text),copied=!0)}catch{}if(!copied)try{const ta=document.createElement("textarea");ta.value=text,ta.setAttribute("readonly",""),ta.style.position="fixed",ta.style.left="-9999px",document.body.appendChild(ta),ta.select(),copied=document.execCommand("copy"),ta.remove()}catch{}if(!copied&&options.prompt!==!1)try{window.prompt(options.promptLabel||"복사할 내용을 선택하세요",text)}catch{}return options.silent||showToast?.(copied?"복사했어요":"복사창을 열었어요","info"),copied}async function paperShareLink(){const url=paperLinkInfo?.url||"";if(url){if(paperUseNativeShare())try{await navigator.share({title:"excelkospi 모의투자 계좌 연결",text:"이 링크로 모의투자 계좌를 연결하세요. 5분 뒤 만료됩니다.",url});return}catch(e){if(e?.name==="AbortError")return}await paperCopyText(url,{promptLabel:"계좌 연결 주소를 복사하세요"})}}async function paperCreateContestShare(){const snapshot=paperShareSnapshot();if(!paperShareSnapshotReady(snapshot))throw new Error("share_snapshot_not_ready");const data=await paperPostJsonWithIdentity(paperApiUrl("/api/paper-share"),{user_id:paperUserIdSafe(),origin:location.origin,snapshot}),token=paperShareToken(data.token)||paperShareUrlToken(data.url),url=paperContestShareUrl(token,data.url);if(!token||!url)throw new Error("share_token_missing");return{url,title:data.title||"엑셀코스피 모의투자 대회",text:data.text||paperShareText(snapshot)}}async function paperShareContest(){if(paperBusy)return;let share;paperSetBusy("공유 링크 만드는 중...");try{share=await paperCreateContestShare()}catch(e){debugWarn("paper share link failed",e);const message=e?.message==="share_snapshot_not_ready"?"수익 집계가 화면에 뜬 뒤 다시 공유해 주세요":"공유 이미지 링크를 만들지 못했어요. 잠시 후 다시 눌러 주세요";showToast?.(message,"warn");return}finally{paperClearBusy()}const text=share.text||paperShareText(),url=paperContestShareUrl("",share.url);if(!url){showToast?.("공유 이미지 링크를 만들지 못했어요. 잠시 후 다시 눌러 주세요","warn");return}const copyText=`${text}
${url}`,nativeShare=paperUseNativeShare(),copied=await paperCopyText(copyText,{silent:!0,prompt:!nativeShare,promptLabel:"공유 문구를 복사하세요"});if(nativeShare)try{await navigator.share({title:share.title||"엑셀코스피 모의투자 대회",text,url}),showToast?.("공유 문구도 복사해 뒀어요","info");return}catch(e){if(e?.name==="AbortError")return}showToast?.(copied?"공유 문구와 주소를 복사했어요":"복사창을 열었어요","info")}async function paperRenameNickname(){if(paperBusy)return;const input=document.getElementById("ppRenameInput"),nickname=String(input?.value||"").trim();if(!nickname){showToast?.("닉네임을 입력해 주세요","err"),input?.focus?.();return}paperSetBusy("닉네임을 변경하는 중...");try{const previous=paperData?.account?.nickname||"",data=await paperPost("rename",{nickname});if(data?.account&&paperData?.account&&(paperData.account={...paperData.account,...data.account}),paperLeaderboard?.me&&data?.account?.nickname&&(paperLeaderboard.me.nickname=data.account.nickname),previous&&data?.account?.nickname)for(const key of["top","participants"])Array.isArray(paperLeaderboard?.[key])&&(paperLeaderboard[key]=paperLeaderboard[key].map(row=>row?.nickname===previous&&row?.rank===paperLeaderboard?.me?.rank?{...row,nickname:data.account.nickname}:row));paperLeaderboardAt=0,paperNicknameEditing=!1,showToast?.(data?.unchanged?"이미 같은 닉네임이에요":"모의투자 닉네임을 변경했어요","info"),await loadPaperTab({forceLb:!0,silent:!0}).catch(()=>null)}catch(e){showToast?.(e?.message||"닉네임 변경에 실패했어요","err"),setTimeout(()=>document.getElementById("ppRenameInput")?.focus?.(),0)}finally{paperClearBusy()}}const PAPER_SHARE_WINDOW_MS=60*60*1e3,PAPER_SHARE_LIMIT=2,PAPER_BOAST_AT_KEY="kg_paper_boast_at_v1",PAPER_BOARD_SHARE_AT_KEY="kg_paper_board_share_at_v1";function paperShareReadStamps(key){try{const raw=JSON.parse(localStorage.getItem(key)||"[]"),now=Date.now();return(Array.isArray(raw)?raw:[]).map(Number).filter(t=>Number.isFinite(t)&&t<=now&&t>now-PAPER_SHARE_WINDOW_MS).sort((a,b)=>a-b)}catch{return[]}}function paperShareWriteStamps(key,stamps){try{localStorage.setItem(key,JSON.stringify(stamps.slice(-PAPER_SHARE_LIMIT)))}catch{}}function paperShareRemainMs(key){const stamps=paperShareReadStamps(key);return stamps.length<PAPER_SHARE_LIMIT?0:Math.max(0,stamps[0]+PAPER_SHARE_WINDOW_MS-Date.now())}function paperShareMark(key){const stamps=paperShareReadStamps(key);stamps.push(Date.now()),paperShareWriteStamps(key,stamps)}function paperShareForce(key,ms){const remain=Math.max(0,Math.min(PAPER_SHARE_WINDOW_MS,Number(ms)||0)),base=Date.now()-(PAPER_SHARE_WINDOW_MS-remain);paperShareWriteStamps(key,Array.from({length:PAPER_SHARE_LIMIT},()=>base))}function paperBoardShareCooldownRemainMs(){return paperShareRemainMs(PAPER_BOARD_SHARE_AT_KEY)}function paperBoardShareMarkSent(){paperShareMark(PAPER_BOARD_SHARE_AT_KEY)}function paperBoastCooldownRemainMs(){return paperShareRemainMs(PAPER_BOAST_AT_KEY)}function paperBoastMarkSent(){paperShareMark(PAPER_BOAST_AT_KEY),paperTabActive()&&renderPaperTab()}function paperBoastMarkRetryAfter(seconds){paperShareForce(PAPER_BOAST_AT_KEY,Math.max(0,Number(seconds)||0)*1e3),paperTabActive()&&renderPaperTab()}function paperBoastClearCooldown(){try{localStorage.removeItem(PAPER_BOAST_AT_KEY)}catch{}paperTabActive()&&renderPaperTab()}async function paperBoastToChat(){if(typeof sendChatMessage!="function"){showToast?.("채팅이 아직 준비되지 않았어요","warn");return}const eligibility=paperBoastEligibility();if(!eligibility.ok){eligibility.reason==="zero-profit"?showToast?.("손익이 0원일 때는 채팅에 자랑할 수 없어요","warn"):showToast?.("모의투자 주문 내역이 있어야 채팅에 자랑할 수 있어요","warn");return}const remain=paperBoastCooldownRemainMs();if(remain>0){const minutes=Math.ceil(remain/6e4);showToast?.(`채팅 자랑은 1시간에 2번까지예요. ${minutes>=60?`${Math.ceil(minutes/60)}시간`:`${minutes}분`} 뒤에 다시 가능해요.`,"warn");return}if(typeof chatSendCooldownMs=="function"){const cooldownMs=chatSendCooldownMs();if(cooldownMs>0){showToast?.(`채팅은 ${Math.ceil(cooldownMs/1e3)}초에 한 번만 보낼 수 있어요`,"warn");return}}typeof setChatOpen=="function"&&setChatOpen(!0,{connect:!0,persist:!1}),sendChatMessage(paperBoastMessage())}function paperBoardShareEligibility(){return paperData?.account?(Number(paperOrderBreakdown(paperData.account)?.total)||0)<=0?{ok:!1,reason:"no-order"}:{ok:!0}:{ok:!1,reason:"no-account"}}async function paperEnsureBoardShareEligibility(options={}){const current=paperBoardShareEligibility(),hasSellWithoutId=!!(options.withTrades&&paperData?.account&&(paperData?.orders||[]).some(order=>order?.side==="sell"&&Number.isFinite(Number(order.realized))&&!String(order.id||"").startsWith("o_")));if(current.ok&&!hasSellWithoutId)return current;let refreshFailed=!1;return(!paperData||hasSellWithoutId)&&await loadPaperTab({silent:!0}).catch(e=>{refreshFailed=!0;try{typeof debugWarn=="function"&&debugWarn("paper eligibility refresh failed",e)}catch{}}),refreshFailed&&!paperData?.account?{ok:!1,reason:"load-failed"}:paperBoardShareEligibility()}window.paperEnsureBoardShareEligibility=paperEnsureBoardShareEligibility;function paperReadBoardShareDraft(){if(!paperBoardShare)return;const titleEl=document.getElementById("ppBoardShareTitle"),bodyEl=document.getElementById("ppBoardShareComment"),tradeEl=document.getElementById("ppBoardShareTrade");titleEl&&(paperBoardShare.title=String(titleEl.value||"")),bodyEl&&(paperBoardShare.body=String(bodyEl.value||"")),tradeEl&&(paperBoardShare.tradeId=String(tradeEl.value||""))}async function paperShareToBoard(){if(paperBusy)return;paperReadBoardShareDraft();const elig=await paperEnsureBoardShareEligibility({withTrades:!0});if(!elig.ok){showToast?.(elig.reason==="no-order"?"모의투자 주문 내역이 있어야 게시판에 공유할 수 있어요":elig.reason==="load-failed"?"모의투자 기록을 확인하지 못했어요. 잠시 후 다시 눌러 주세요":"먼저 모의투자 계좌를 만들어 주세요","warn");return}const remain=paperBoardShareCooldownRemainMs();if(remain>0){const minutes=Math.ceil(remain/6e4);showToast?.(`게시판 자랑은 1시간에 2번까지예요. ${minutes>=60?`${Math.ceil(minutes/60)}시간`:`${minutes}분`} 뒤에 다시 가능해요.`,"warn");return}const channel=paperBoardShare?.channel||"free",title=String(paperBoardShare?.title||"").trim().slice(0,60),comment=String(paperBoardShare?.body||"").trim().slice(0,500),tradeId=String(paperBoardShare?.tradeId||"").trim(),nickname=typeof communityNicknameForSend=="function"?communityNicknameForSend(""):paperData?.account?.nickname||"";if(!nickname){showToast?.("닉네임을 확인할 수 없어요. 잠시 후 다시 시도해 주세요","warn");return}const guardText=[title,comment].filter(Boolean).join(`
`);if(!(guardText&&typeof guardChatMessage=="function"&&await guardChatMessage(guardText,"게시판 글").catch(()=>!0)===!1)){paperSetBusy("게시판에 자랑하는 중...");try{await paperIdentityToken(!1).catch(()=>"");const attempt=async(fresh,forceIdentity=!1)=>{const identityToken=await paperIdentityToken(forceIdentity),ttok=typeof chatTurnstileToken=="function"?await chatTurnstileToken(2e4,fresh?{fresh:!0}:{nonBlocking:!0}).catch(()=>""):"",res2=await paperFetch(paperApiUrl("/api/community"),{method:"POST",headers:{"content-type":"application/json",...identityToken?{"x-chat-identity-token":identityToken}:{},...paperAuthHeader()},body:JSON.stringify({action:"create",paper_share:!0,...ttok?{turnstile_token:ttok}:{},...identityToken?{identity_token:identityToken}:{},user_id:paperUserIdSafe(),nickname,channel,paper_badges:paperMyNickBadgeIds(),...title?{title}:{},...tradeId?{paper_trade_id:tradeId}:{},body:comment})}),data2=await res2.json().catch(()=>({}));return{res:res2,data:data2}};let{res,data}=await attempt(!1);if((res.status===403||data?.error==="turnstile_required")&&({res,data}=await attempt(!0)),(!res.ok||data?.ok===!1)&&paperIdentityError(data)&&(paperClearIdentityToken(),{res,data}=await attempt(!1,!0)),!res.ok||data?.ok===!1||!data?.post){const err=data?.error,msg=err==="no_paper_record"?"공유할 모의투자 기록이 없어요 (첫 주문 후 가능해요)":err==="turnstile_required"?"사람 확인을 마치지 못했어요. 다시 한 번 눌러 주세요":err==="rate_limited"||err==="first_write_cooldown"?"잠시 후 다시 시도해 주세요":data?.banned?"지금은 글을 쓸 수 없는 상태예요":"게시판 자랑에 실패했어요. 잠시 후 다시 시도해 주세요";showToast?.(msg,"warn");return}paperBoardShare=null,paperBoardShareMarkSent();try{const post=data.post,activeCh=typeof communityActiveChannel=="function"?communityActiveChannel():"",dedupe=list=>list.filter((p,i,l)=>p?.id&&l.findIndex(x=>String(x?.id||"")===String(p.id))===i);if(activeCh===channel&&typeof communityPosts<"u")communityPosts=dedupe([post,...communityPosts]),typeof writeCommunityPostsCache=="function"&&writeCommunityPostsCache(channel,communityPosts,{hasMore:typeof communityHasMore<"u"?communityHasMore:!1,nextCursor:typeof communityNextCursor<"u"?communityNextCursor:""}),typeof markCommunityPostInserted=="function"&&markCommunityPostInserted(post.id),typeof renderCommunityTable=="function"&&(typeof timelineIsCommunity!="function"||timelineIsCommunity())&&renderCommunityTable();else if(typeof readCommunityPostsCacheEntry=="function"&&typeof writeCommunityPostsCache=="function"){const entry=readCommunityPostsCacheEntry(channel,{allowStale:!0});entry&&Array.isArray(entry.posts)&&writeCommunityPostsCache(channel,dedupe([post,...entry.posts]),{hasMore:entry.hasMore,nextCursor:entry.nextCursor,mentionSummary:entry.mentionSummary})}}catch(e){try{debugWarn?.("paper share board cache refresh failed",e)}catch{}}const label=PAPER_SHARE_CHANNELS.find(c=>c.key===channel)?.label||"게시판";showToast?.(`${label} 게시판에 자랑했어요`,"info"),renderPaperTab()}catch(e){debugWarn("paper board share failed",e),showToast?.("게시판 자랑에 실패했어요. 잠시 후 다시 시도해 주세요","warn")}finally{paperClearBusy()}}}function paperTogglePinDraft(id){if(!PAPER_BADGE_BY_ID[id]||!paperEarnedBadgeIds().includes(id))return;const base=Array.isArray(paperPinDraft)?paperPinDraft.slice():paperPinnedBadgeIds().slice(),idx=base.indexOf(id);if(idx>=0)base.splice(idx,1);else if(base.length>=PAPER_MAX_PINNED_BADGES){showToast?.(`닉네임 뱃지는 최대 ${PAPER_MAX_PINNED_BADGES}개까지예요`,"warn"),paperPinDraft=base;return}else base.push(id);paperPinDraft=base}async function paperSavePinnedBadges(){if(paperBusy)return;paperSessionBadgeIds=(Array.isArray(paperPinDraft)?paperPinDraft.slice(0,PAPER_MAX_PINNED_BADGES):paperPinnedBadgeIds()).filter(id=>PAPER_BADGE_BY_ID[id]&&paperEarnedBadgeIds().includes(id)).slice(0,PAPER_MAX_PINNED_BADGES),paperPinDraft=null,paperBadgeModalOpen=!1,showToast?.("이번 세션 닉네임에 뱃지를 달았어요","info"),renderPaperTab()}async function paperHandleAction(act,btn){if(act==="open-tab"){typeof setTimelineTab=="function"&&setTimelineTab("paper");return}if(act==="member-start"){if(paperBusy)return;paperSetBusy("계좌를 확인하는 중...");try{const r=typeof authLinkPaperAccount=="function"?await authLinkPaperAccount():{ok:!1};r.ok&&r.absorbed?(paperMemberStage="",paperOnboardDraft={nickname:"",recovery:""},showToast?.("이 기기의 모의투자 계좌를 계정으로 가져왔어요","info"),await loadPaperTab({forceLb:!0,silent:!0}).catch(()=>null),await ensurePaperPerformanceNotificationDefault({silent:!0}).catch(()=>null)):r.status===409||r.error==="paper_conflict"?(paperMemberStage="",showToast?.("이미 이 계정에 모의투자 계좌가 있어요","info"),await loadPaperTab({forceLb:!0,silent:!0}).catch(()=>null)):(paperMemberStage="new",renderPaperTab(),setTimeout(()=>document.getElementById("ppNickInput")?.focus?.({preventScroll:!0}),0))}catch(e){showToast?.(e?.message||"확인에 실패했어요. 잠시 후 다시 시도해 주세요.","err")}finally{paperClearBusy()}return}if(act==="member-cancel"){paperMemberStage="",renderPaperTab();return}if(act==="create"||act==="member-create"){if(paperBusy)return;const nickname=String(document.getElementById("ppNickInput")?.value||"").trim();if(paperOnboardDraft.nickname=nickname,!nickname){showToast?.("닉네임을 입력해야 계좌를 만들 수 있어요","err"),document.getElementById("ppNickInput")?.focus();return}paperSetBusy("계좌를 만드는 중...");try{const data=await paperPost("create",{nickname});data.recoveryCode&&(paperRevealCode=data.recoveryCode),paperOnboardDraft={nickname:"",recovery:""},paperMemberStage="",paperNicknameEditing=!1,showToast?.("모의투자 계좌가 생겼어요! 가상 5,000만원 입금 완료","info"),await loadPaperTab({forceLb:!0,silent:!0}).catch(()=>null),await ensurePaperPerformanceNotificationDefault({silent:!0}).catch(()=>null)}catch(e){showToast?.(e?.message||"계좌 생성에 실패했어요","err")}finally{paperClearBusy()}return}if(act==="recover"){if(paperBusy)return;paperSetBusy("계좌를 복구하는 중...");try{const code=document.getElementById("ppRecoverInput")?.value||"";paperOnboardDraft.recovery=code,await paperPost("recover",{code}),paperOnboardDraft={nickname:"",recovery:""},paperNicknameEditing=!1,showToast?.("계좌를 되찾았어요!","info"),await loadPaperTab({forceLb:!0,silent:!0}).catch(()=>null),await ensurePaperPerformanceNotificationDefault({silent:!0}).catch(()=>null)}catch(e){showToast?.(e?.message||"복구에 실패했어요","err"),document.getElementById("ppRecoverInput")?.select?.()}finally{paperClearBusy()}return}if(act==="copy-code")return paperCopyText(btn.dataset.code||"");if(act==="nickname-edit"){if(!paperNicknameCanChange()){showToast?.(`${paperNicknameChangeButtonText()} 가능해요`,"warn");return}paperNicknameEditing=!0,renderPaperTab(),setTimeout(()=>document.getElementById("ppRenameInput")?.select?.(),0);return}if(act==="nickname-cancel"){paperNicknameEditing=!1,renderPaperTab();return}if(act==="nickname-save")return paperRenameNickname();if(act==="code-done"){paperRevealCode="",renderPaperTab();return}if(act==="ack-notice"){if(paperBusy)return;paperSetBusy("확인 중...");try{await paperPost("ack-notice"),await loadPaperTab({silent:!0}).catch(()=>null)}catch(e){showToast?.(e?.message||"처리하지 못했어요","err")}finally{paperClearBusy()}return}if(act==="link-close"){paperLinkInfo=null,renderPaperTab();return}if(act==="link-share")return paperShareLink();if(act==="side-buy"||act==="side-sell"){paperReadTicketInputs(),paperTicket.side=act==="side-buy"?"buy":"sell",renderPaperTab();return}if(act==="lookup")return paperLookupSymbol();if(act==="qty-add"){paperReadTicketInputs();const add=Number(btn.dataset.n)||1,current=Number(paperTicket.qty)||0;paperTicket.qty=String(current+add),renderPaperTab();return}if(act==="qty-clear"){paperTicket.qty="",renderPaperTab();return}if(act==="qty-percent"||act==="qty-max"){paperReadTicketInputs();const pct=act==="qty-max"?1:Math.max(0,Math.min(1,Number(btn.dataset.pct)||0)),nextQty=paperQtyForPct(pct);nextQty?paperTicket.qty=nextQty:showToast?.(paperTicket.side==="sell"?"매도할 보유 수량이 없어요":"살 수 있는 수량을 계산하지 못했어요","err"),renderPaperTab();return}if(act==="prefill"){paperTicket={...paperTicket,code:String(btn.dataset.code||"").toUpperCase(),market:String(btn.dataset.market||"KR").toUpperCase(),side:btn.dataset.side==="sell"?"sell":"buy",qty:btn.dataset.qty||"",session:""};const pos=(paperData?.positions||[]).find(p=>p.code===paperTicket.code&&p.market===paperTicket.market);paperTicket.name=pos?.name||paperTicket.code;const rawQuote=Number(paperQuoteMap[`${paperTicket.code}:${paperTicket.market}`]?.price);paperTicket.market==="US"?paperTicket.price=Number.isFinite(rawQuote)&&rawQuote>0?rawQuote:null:paperTicket.price=paperPositionMark(pos||{code:paperTicket.code,market:paperTicket.market,avg_price:null}),renderPaperTab();return}if(act==="submit-order")return paperSubmitOrder();if(act==="reissue"){if(paperBusy)return;paperSetBusy("복구코드를 재발급하는 중...");try{paperRevealCode=(await paperPost("reissue")).recoveryCode||"",renderPaperTab(),setTimeout(()=>document.querySelector(".pp-code")?.scrollIntoView?.({behavior:"smooth",block:"center"}),50),showToast?.("새 복구코드를 발급했어요. 이전 코드는 무효가 됐어요.","info")}catch(e){showToast?.(e?.message||"재발급에 실패했어요","err")}finally{paperClearBusy()}return}if(act==="qr"){if(paperBusy)return;paperSetBusy("기기 연결 링크를 만드는 중...");try{const data=await paperPost("link-issue");paperLinkInfo={token:data.token,url:`${paperShareUrl()}?paperlink=${data.token}`},renderPaperTab(),setTimeout(()=>document.querySelector(".pp-link")?.scrollIntoView?.({behavior:"smooth",block:"center"}),50)}catch(e){showToast?.(e?.message||"QR 생성에 실패했어요","err")}finally{paperClearBusy()}return}if(act==="refresh"){paperLeaderboardAt=0,loadPaperTab({forceLb:!0}).catch(()=>null);return}if(act==="rank-expand"){const key=String(btn.dataset.rankkey||"");paperExpandedRankKey=paperExpandedRankKey===key?"":key,renderPaperTabKeepParticipantScroll();return}if(act==="rank-scope"){const scope=btn.dataset.scope==="week"?"week":"all";paperRankScope=scope,scope==="week"&&!Array.isArray(paperLeaderboard?.participants)&&(renderPaperTab(),await paperFetchLeaderboard(!0,{participants:!0}).catch(()=>null)),renderPaperTab();return}if(act==="pill-quote"){const code=String(btn.dataset.code||"").toUpperCase(),market=String(btn.dataset.market||"").toUpperCase();if(!code)return;if(!paperData?.account){showToast?.("계좌를 만들면 바로 시세 확인·주문까지 할 수 있어요","info");return}paperTicket={...paperTicket,code,market:market||"KR",side:"buy",qty:"",session:""},await paperLookupSymbol(code,market||"AUTO"),document.querySelector(".pp-ticket")?.scrollIntoView?.({behavior:"smooth",block:"center"});return}if(act==="close-account"){if(paperBusy)return;const nickname=paperData?.account?.nickname||"내 계좌";if(!confirm(`'${nickname}' 계좌를 삭제하고 처음부터 다시 시작할까요?
보유 종목·체결 기록·랭킹이 모두 사라지고 복구할 수 없어요.`)||!confirm("정말 삭제할까요? 이 작업은 되돌릴 수 없어요."))return;paperSetBusy("계좌를 삭제하는 중...");try{await paperPost("close",{confirm:"DELETE"}),paperRevealCode="",paperLinkInfo=null,paperExpandedRankKey="",paperNicknameEditing=!1;try{localStorage.removeItem(PAPER_RECOVERY_ACK_KEY)}catch{}try{localStorage.removeItem(PAPER_SNAPSHOT_KEY)}catch{}showToast?.("계좌를 삭제했어요. 언제든 새로 시작할 수 있어요!","info"),await loadPaperTab({forceLb:!0,silent:!0}).catch(()=>null)}catch(e){showToast?.(e?.message||"계좌 삭제에 실패했어요","err")}finally{paperClearBusy()}return}if(act==="recovery-ack"){try{localStorage.setItem(PAPER_RECOVERY_ACK_KEY,"1")}catch{}renderPaperTab();return}if(act==="toggle-participants"){const willOpen=!paperShowParticipants;paperShowParticipants=willOpen,willOpen||(paperParticipantsError=""),paperShowParticipants&&!Array.isArray(paperLeaderboard?.participants)&&(paperParticipantsError="",renderPaperTab(),setTimeout(()=>document.querySelector(".pp-participants-expanded")?.scrollIntoView?.({behavior:"smooth",block:"nearest"}),30),await paperFetchLeaderboard(!0,{participants:!0}).catch(()=>(paperParticipantsError="전체 목록을 불러오지 못했어요",null))),renderPaperTab(),willOpen&&setTimeout(()=>document.querySelector(".pp-participants-expanded")?.scrollIntoView?.({behavior:"smooth",block:"nearest"}),30);return}if(act==="rank-window"){const key=String(btn.dataset.window||"all");PAPER_RANK_WINDOWS.some(item=>item.key===key)&&(paperRankWindow=key,renderPaperTabKeepParticipantScroll());return}if(act==="participant-sort"){paperParticipantSort=btn.dataset.sort==="orders"?"orders":"rank",renderPaperTabKeepParticipantScroll();return}if(act==="share-contest")return paperShareContest();if(act==="boast-chat")return paperBoastToChat();if(act==="share-board-open"){const elig=await paperEnsureBoardShareEligibility({withTrades:!0});if(!elig.ok){showToast?.(elig.reason==="no-order"?"모의투자 주문 내역이 있어야 게시판에 공유할 수 있어요":elig.reason==="load-failed"?"모의투자 기록을 확인하지 못했어요. 잠시 후 다시 눌러 주세요":"먼저 모의투자 계좌를 만들어 주세요","warn");return}paperBoardShare=paperBoardShare||{channel:"free",title:"",body:"",tradeId:""},renderPaperTab(),setTimeout(()=>document.getElementById("ppBoardShareTitle")?.focus({preventScroll:!0}),0);return}if(act==="share-board-channel"){paperReadBoardShareDraft(),paperBoardShare&&(paperBoardShare.channel=PAPER_SHARE_CHANNELS.some(c=>c.key===btn.dataset.ch)?btn.dataset.ch:"free"),renderPaperTab();return}if(act==="share-board-trade"){paperReadBoardShareDraft(),renderPaperTab();return}if(act==="share-board-cancel"){paperBoardShare=null,renderPaperTab();return}if(act==="share-board-submit")return paperShareToBoard();if(act==="badge-modal-open"){paperBadgeModalOpen=!0,paperPinDraft=null,renderPaperTab();return}if(act==="badge-modal-close"){paperBadgeModalOpen=!1,paperPinDraft=null,renderPaperTab();return}if(act==="badge-pin-toggle"){paperTogglePinDraft(btn.dataset.badge),renderPaperTab();return}if(act==="badge-pin-save")return paperSavePinnedBadges()}document.addEventListener("click",ev=>{const summary=ev.target?.closest?.(".pp-help > summary");if(summary){ev.preventDefault();const details=summary.closest(".pp-help");details&&(details.open=!details.open,summary.setAttribute("aria-expanded",details.open?"true":"false"));return}const btn=ev.target?.closest?.("[data-pp-act]");!btn||btn.disabled||btn.classList?.contains("pp-modal-backdrop")&&ev.target?.closest?.("[data-pp-stop]")||(ev.preventDefault(),paperHandleAction(btn.dataset.ppAct,btn).catch(e=>debugWarn("paper action failed",e)))}),document.addEventListener("change",async ev=>{if(ev.target?.id==="ppBoardShareTrade"){paperReadBoardShareDraft(),renderPaperTab();return}if(ev.target?.id!=="ppNotifySelect")return;const threshold=Number(ev.target.value)||0;try{if(threshold>0){if(typeof Notification<"u"&&Notification.permission!=="granted"&&await Notification.requestPermission()!=="granted"){showToast?.("브라우저 알림 권한을 허용해야 받을 수 있어요","warn"),renderPaperTab();return}writePaperPerformanceNotificationSetting(!0)}else writePaperPerformanceNotificationSetting(!1);try{const pending=window.syncChatPushSubscription?.();pending&&typeof pending.catch=="function"&&pending.catch(()=>{})}catch{}await paperPost("notify-settings",{threshold}),paperData?.account&&(paperData.account.notifyThreshold=threshold),showToast?.(threshold>0?`수익률이 ±${threshold}%p 움직이면 알려드릴게요`:"수익률 알림을 껐어요","info")}catch(e){showToast?.(e?.message||"알림 설정에 실패했어요","err")}}),document.addEventListener("input",ev=>{if(ev.target?.id==="ppQtyInput"){paperTicket.qty=ev.target.value,paperUpdateTicketFoot();return}if(ev.target?.id==="ppNickInput"){paperOnboardDraft.nickname=ev.target.value;return}if(ev.target?.id==="ppRecoverInput"){paperOnboardDraft.recovery=ev.target.value;return}(ev.target?.id==="ppBoardShareTitle"||ev.target?.id==="ppBoardShareComment")&&paperReadBoardShareDraft()}),document.addEventListener("keydown",ev=>{if(ev.target?.id==="ppSymbolInput"){if(paperSymbolAutocomplete?.handleKeydown?.(ev))return;ev.key==="Enter"&&(ev.preventDefault(),paperLookupSymbol());return}ev.key==="Enter"&&(ev.target?.id==="ppQtyInput"&&(ev.preventDefault(),paperSubmitOrder()),ev.target?.id==="ppRenameInput"&&(ev.preventDefault(),paperRenameNickname()),ev.target?.id==="ppRecoverInput"&&(ev.preventDefault(),paperHandleAction("recover",ev.target)),ev.target?.id==="ppNickInput"&&(ev.preventDefault(),paperHandleAction("create",ev.target)))}),window.ensurePaperPerformanceNotificationDefault=ensurePaperPerformanceNotificationDefault,document.addEventListener("DOMContentLoaded",()=>{let token="";try{token=new URLSearchParams(location.search).get("paperlink")||""}catch{}if(token){try{const url=new URL(location.href);url.searchParams.delete("paperlink"),history.replaceState(history.state,document.title,url.toString())}catch{}paperPost("link-claim",{token}).then(()=>{showToast?.("모의투자 계좌가 이 기기에 연결됐어요!","info"),typeof setTimelineTab=="function"?setTimelineTab("paper"):loadPaperTab().catch(()=>null),setTimeout(()=>{ensurePaperPerformanceNotificationDefault({silent:!0}).catch(()=>null)},0)}).catch(e=>{showToast?.(e?.message||"기기 연결에 실패했어요","err")})}});
