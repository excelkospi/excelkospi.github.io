const PAPER_REFRESH_MS=18e4,PAPER_LB_CLIENT_TTL_MS=18e4,PAPER_AUTO_LOAD_REUSE_MS=12e3,PAPER_FAILED_LOAD_RETRY_BASE_MS=5e3,PAPER_FAILED_LOAD_RETRY_MAX_MS=3e5,PAPER_FAILED_LOAD_RETRY_JITTER_MS=7e3,PAPER_PARTICIPANT_PAGE_SIZE=100,PAPER_QUOTE_RECOVERY_DELAYS_MS=Object.freeze([8e3,22e3,6e4]),PAPER_SHARE_PATH="/paper",PAPER_SHARE_BONUS_COPY="이 링크로 모의투자를 시작하면 +500만원을 더 받아요",PAPER_SHARE_REFERRAL_CLAIM_KEY="kg_paper_share_referral_claim_v1",PAPER_DEFAULT_NOTIFY_THRESHOLD=5,PAPER_RANK_WINDOWS=Object.freeze([{key:"d1",label:"1일",days:1},{key:"d3",label:"3일",days:3},{key:"d7",label:"7일",days:7},{key:"all",label:"전체",days:0}]);let paperData=null,paperQuoteMap={},paperServerMarks={},paperLeaderboard=null,paperLeaderboardAt=0,paperError="",paperLoadInFlight=null,paperLoadedAt=0,paperConsecutiveLoadFailures=0,paperPublicLeaderboardInFlight=null,paperLoadingText="",paperVisibleLoadPending=!1,paperTimer=null,paperQuoteRefreshTimer=null,paperQuoteRecoveryAttempt=0,paperRevealCode="",paperLinkInfo=null,paperContestSharePreview=null,paperShareInviteSnapshot=null,paperShareInviteRefTried="",paperShareInFlight=!1,paperDrawInFlight=!1,paperDrawResult=null,paperWelcomeBonusAmount=0,paperWelcomeTicket=!1,paperBusy=!1,paperBusyText="",paperRankWindow="all",paperRankScope="season",paperParticipantSort="rank",paperHotMode="net",paperHotModeUserSet=!1,paperTradesPage=0,paperShowParticipants=!1,paperRankModalOpen=!1,paperLeagueModalOpen=!1,paperLeagueDivView="all",paperParticipantsError="",paperParticipantsLoadingMore=!1,paperNicknameEditing=!1,paperTicket={code:"",market:"",name:"",price:null,changePct:null,side:"buy",qty:"",maxCashOrder:!1},paperSymbolAutocomplete=null,paperRankMotion=new Map,paperRankMotionUntil=0,paperLoadStartedAt=0,paperLoadSeq=0,paperLoadWatchdogTimer=null,paperOnboardDraft={nickname:"",recovery:""},paperGuestOnboardStarted=!1,paperIdentityStorageRestorePromise=null,paperMemberStage="",paperMemberScenario="";function paperResetMemberStage(){paperMemberStage="",paperMemberScenario=""}function paperIsLoggedIn(){try{return typeof authAvailable<"u"&&authAvailable===!0&&typeof currentAuthUser=="function"&&!!currentAuthUser()}catch{return!1}}function paperMemberNickname(){try{const u=typeof currentAuthUser=="function"?currentAuthUser():null;return String(u?.nickname||u?.loginId||"회원").replace(/[\u0000-\u001F\u007F-\u009F]/g,"").trim().replace(/\s+/g," ").slice(0,12)||"회원"}catch{return"회원"}}function paperOpenRecoveryImport(){paperMemberStage="recover",paperGuestOnboardStarted=!0;try{typeof setTimelineTab=="function"&&setTimelineTab("paper",{preferCached:!0})}catch{}renderPaperTab(),setTimeout(()=>document.getElementById("ppRecoverInput")?.focus?.(),0)}window.paperOpenRecoveryImport=paperOpenRecoveryImport;let paperBoardShare=null,paperBadgeModalOpen=!1,paperHelpOpen=!1,paperSettingsOpen=!1,paperBadgeFocusId="",paperSessionBadgeIds=[],paperNickCardMode="badge",paperNickCardModeDraft=null,paperPinDraft=null;const PAPER_BADGE_DEFS=[{id:"hall_of_fame",emoji:"👑",name:"명예의 전당",tier:"S",desc:"리더보드 1위를 한 번이라도 차지"},{id:"sharing_king_gold",emoji:"🥇",name:"공유 금메달",tier:"S",desc:"공유링크로 30명 모의투자 시작시킴"},{id:"sharing_king_silver",emoji:"🥈",name:"공유 은메달",tier:"A",desc:"공유링크로 10명 모의투자 시작시킴"},{id:"sharing_king_bronze",emoji:"🥉",name:"공유 동메달",tier:"B",desc:"공유링크로 3명 모의투자 시작시킴"},{id:"podium",emoji:"🏆",name:"시상대",tier:"S",desc:"리더보드 3위 안에 진입"},{id:"top10",emoji:"🎖️",name:"톱텐",tier:"A",desc:"리더보드 10위 안에 진입"},{id:"upper_rank",emoji:"📊",name:"상위권",tier:"B",desc:"리더보드 상위 25% 진입"},{id:"first_invite",emoji:"🤝",name:"첫 초대",tier:"C",desc:"친구 1명을 모의투자에 초대"},{id:"tenbagger",emoji:"💎",name:"텐배거",tier:"S",desc:"보유 종목 평가수익 +100% 달성"},{id:"whale",emoji:"🦈",name:"큰손",tier:"S",desc:"평가자산 1억원 돌파"},{id:"rocket",emoji:"🚀",name:"로켓",tier:"A",desc:"총수익률 +30% 달성"},{id:"hot_week",emoji:"🔥",name:"주간 불장",tier:"A",desc:"한 주 수익률 +20% 달성"},{id:"veteran",emoji:"🏃",name:"백전노장",tier:"A",desc:"누적 체결 200회"},{id:"climber",emoji:"📈",name:"상승가도",tier:"B",desc:"총수익률 +10% 달성"},{id:"trader",emoji:"⚡",name:"트레이더",tier:"B",desc:"누적 체결 50회"},{id:"diversified",emoji:"🎯",name:"분산투자",tier:"B",desc:"동시 5종목 이상 보유"},{id:"old_timer",emoji:"🐢",name:"고인물",tier:"B",desc:"계좌 개설 30일 경과"},{id:"in_profit",emoji:"💵",name:"수익권",tier:"C",desc:"5회 이상 체결 + 수익률 0% 이상"},{id:"first_buy",emoji:"🛒",name:"첫 매수",tier:"C",desc:"매수 주문 1회"},{id:"first_sell",emoji:"💸",name:"첫 매도",tier:"C",desc:"매도 주문 1회"},{id:"rookie",emoji:"🐣",name:"입문",tier:"C",desc:"첫 체결"},{id:"sprout",emoji:"🌱",name:"새싹",tier:"C",desc:"모의투자 계좌 개설"},{id:"downhill",emoji:"📉",name:"우하향 장인",tier:"F",desc:"총수익률 -10% 이하 — 차트가 우하향 예술"},{id:"freefall",emoji:"☄️",name:"노낙하산 강하",tier:"F",desc:"총수익률 -30% 이하 — 낙하산 없이 자유낙하"},{id:"tin_can",emoji:"💀",name:"깡통 컬렉터",tier:"F",desc:"총수익률 -50% 이하 — 반토막 그 이상"},{id:"cold_week",emoji:"❄️",name:"주간 한파주의보",tier:"F",desc:"한 주 수익률 -20% 이하 — 계좌에 한파"},{id:"underwater",emoji:"🤿",name:"물 들어왔다",tier:"F",desc:"보유 종목 평가손익 -30% 이하 — 깊이 물렸어요"},{id:"deep_sea",emoji:"⚓",name:"심해 다이버",tier:"F",desc:"보유 종목 평가손익 -50% 이하 — 심해까지 입수"},{id:"cellar",emoji:"🕳️",name:"지하실 단골",tier:"F",desc:"리더보드 하위 10% — 지하실 터줏대감"},{id:"last_place",emoji:"🤡",name:"꼴찌의 품격",tier:"F",desc:"리더보드 최하위 — 그래도 참가에 의의를"}],PAPER_BADGE_BY_ID=Object.fromEntries(PAPER_BADGE_DEFS.map(b=>[b.id,b])),PAPER_BADGE_BY_EMOJI=Object.fromEntries(PAPER_BADGE_DEFS.map(b=>[b.emoji,b]));function paperBadgeTipByEmoji(emoji){const def=PAPER_BADGE_BY_EMOJI[String(emoji||"")];return def?`모의투자 뱃지 : "${def.name}"
${def.desc} — 이 뱃지를 받았어요`:""}window.paperBadgeTipByEmoji=paperBadgeTipByEmoji;const PAPER_MAX_PINNED_BADGES=3,PAPER_SHARE_CHANNELS=[{key:"free",label:"자유"},{key:"kr",label:"국장"},{key:"us",label:"미장"},{key:"coin",label:"코인"}];function paperEarnedBadgeIds(){const earned=paperData?.badges?.earned;return Array.isArray(earned)?earned.filter(id=>PAPER_BADGE_BY_ID[id]):[]}function paperPreservePinnedBadges(){return paperIsLoggedIn()}function paperStoredPinnedBadgeIds(){return paperSessionBadgeIds.filter(id=>PAPER_BADGE_BY_ID[id]).slice(0,PAPER_MAX_PINNED_BADGES)}function paperPinnedBadgeIds(){if(Array.isArray(paperPinDraft))return paperPinDraft.filter(id=>PAPER_BADGE_BY_ID[id]);if(paperPreservePinnedBadges())return paperStoredPinnedBadgeIds();const earned=paperEarnedBadgeIds();return paperStoredPinnedBadgeIds().filter(id=>earned.includes(id))}function paperHydratePinnedBadges(){try{(paperData?.nickCardMode==="div"||paperData?.nickCardMode==="badge")&&(paperNickCardMode=paperData.nickCardMode);const pinned=paperData?.badges?.pinned;if(!Array.isArray(pinned))return;const earned=paperEarnedBadgeIds(),clean=pinned.filter(id=>PAPER_BADGE_BY_ID[id]).slice(0,PAPER_MAX_PINNED_BADGES);paperSessionBadgeIds=paperPreservePinnedBadges()?clean:clean.filter(id=>earned.includes(id))}catch{}}function paperBadgeEmoji(id){return PAPER_BADGE_BY_ID[id]?.emoji||""}function paperMyNickBadgeIds(){if(paperNickCardMode==="div")return["__nickcard:div"];const own=paperPinnedBadgeIds().slice(0,3);if(own.length)return own;try{if(typeof window.authMemberPaperPinnedBadges=="function"){const bundle=window.authMemberPaperPinnedBadges();if(Array.isArray(bundle)&&bundle.length)return bundle.filter(id=>PAPER_BADGE_BY_ID[id]).slice(0,3)}}catch{}return own}window.paperMyNickBadgeIds=paperMyNickBadgeIds;function paperMyNickBadgeEmojis(){return paperMyNickBadgeIds().map(paperBadgeEmoji).filter(Boolean).slice(0,3)}window.paperMyNickBadgeEmojis=paperMyNickBadgeEmojis;function paperMyRankPercentile(){const me=paperLeaderboard?.me||{},rank=Number(me.rank)||0,total=Number(paperLeaderboard?.total)||0;if(rank<1||total<10)return null;const topPct=Math.max(1,Math.round(rank/total*100)),bottomPct=Math.max(1,Math.round((total-rank+1)/total*100));return topPct<=20?{side:"top",pct:topPct}:bottomPct<=20?{side:"bottom",pct:bottomPct}:null}window.paperMyRankPercentile=paperMyRankPercentile;function paperApiUrl(path){try{if(typeof window.apiUrl=="function")return window.apiUrl(path);if(typeof apiUrl=="function")return apiUrl(path)}catch{}return path}function paperIdentityStorageKeys(){const keys=[];try{keys.push(typeof VISITOR_ID_KEY=="string"?VISITOR_ID_KEY:"kg_visitor_id_v1")}catch{keys.push("kg_visitor_id_v1")}try{keys.push(typeof CHAT_IDENTITY_SECRET_KEY=="string"?CHAT_IDENTITY_SECRET_KEY:"kg_chat_identity_secret_v1")}catch{keys.push("kg_chat_identity_secret_v1")}try{keys.push(typeof CHAT_IDENTITY_TOKEN_KEY=="string"?CHAT_IDENTITY_TOKEN_KEY:"kg_chat_identity_token_v1")}catch{keys.push("kg_chat_identity_token_v1")}return[...new Set(keys.filter(Boolean))]}function paperOpenSettingsDb(){return new Promise((resolve,reject)=>{if(!window.indexedDB){reject(new Error("indexeddb_unavailable"));return}let settled=!1;const timer=setTimeout(()=>{settled||(settled=!0,reject(new Error("indexeddb_open_timeout")))},1400),settle=(fn,value)=>{settled||(settled=!0,clearTimeout(timer),fn(value))},req=indexedDB.open("excelkospi_settings_v1",1);req.onupgradeneeded=()=>req.result.createObjectStore("kv"),req.onsuccess=()=>settle(resolve,req.result),req.onerror=()=>settle(reject,req.error||new Error("indexeddb_open_error")),req.onblocked=()=>settle(reject,new Error("indexeddb_open_blocked"))})}function paperShouldRestoreIdentityKey(key,current,stored){if(stored==null||stored==="")return!1;if(!current)return!0;let visitorKey="kg_visitor_id_v1";try{visitorKey=typeof VISITOR_ID_KEY=="string"?VISITOR_ID_KEY:visitorKey}catch{}if(key===visitorKey&&stored!==current)try{return typeof window.__xkVisitorIdGeneratedThisPage=="function"&&window.__xkVisitorIdGeneratedThisPage(current)}catch{}return!1}async function paperRestoreIdentityStorage(){return paperIdentityStorageRestorePromise||(paperIdentityStorageRestorePromise=(async()=>{let db=null;try{db=await paperOpenSettingsDb();const store=db.transaction("kv","readonly").objectStore("kv"),rows=await Promise.all(paperIdentityStorageKeys().map(key=>new Promise(resolve=>{let done=!1;const finish=value=>{done||(done=!0,resolve({key,value}))},timer=setTimeout(()=>finish(null),900);try{const req=store.get(key);req.onsuccess=()=>{clearTimeout(timer),finish(req.result??null)},req.onerror=()=>{clearTimeout(timer),finish(null)}}catch{clearTimeout(timer),finish(null)}})));let restored=!1;return rows.forEach(({key,value})=>{let current="";try{current=String(localStorage.getItem(key)||"")}catch{}if(paperShouldRestoreIdentityKey(key,current,value))try{localStorage.setItem(key,String(value)),restored=!0}catch{}}),restored&&typeof window.__xkAddBreadcrumb=="function"&&window.__xkAddBreadcrumb("paper","identity_restore",{restored:!0}),restored}catch{return!1}finally{try{db?.close?.()}catch{}}})(),paperIdentityStorageRestorePromise)}function paperUserIdSafe(){try{if(typeof authAvailable<"u"&&authAvailable===!0&&typeof currentAuthUser=="function"){const u=currentAuthUser();if(u&&u.userId)return String(u.userId)}}catch{}try{return typeof chatUserId=="function"?chatUserId():""}catch{return""}}window.paperMemberAccountInfo=function(){try{const acc=typeof paperData<"u"&&paperData?paperData.account:null;return acc?{hasAccount:!0,nickname:String(acc.nickname||""),cash:Number(acc.cash)||0}:{hasAccount:!1}}catch{return{hasAccount:!1}}};function paperAuthHeader(){try{if(typeof authAvailable<"u"&&authAvailable===!0&&typeof authSessionToken=="function"){const t=authSessionToken();if(t)return{"x-session-token":t}}}catch{}return{}}function paperNotificationSettings(){try{const key=typeof CHAT_NOTIFICATION_SETTINGS_KEY=="string"?CHAT_NOTIFICATION_SETTINGS_KEY:"kg_chat_notification_settings_v1";return JSON.parse(localStorage.getItem(key)||"{}")||{}}catch{return{}}}function writePaperPerformanceNotificationSetting(enabled){try{const key=typeof CHAT_NOTIFICATION_SETTINGS_KEY=="string"?CHAT_NOTIFICATION_SETTINGS_KEY:"kg_chat_notification_settings_v1",settings=paperNotificationSettings();return settings.notifyPaperPerformance=!!enabled,localStorage.setItem(key,JSON.stringify(settings)),typeof persistSet=="function"&&persistSet(key,JSON.stringify(settings)),settings}catch{return paperNotificationSettings()}}function paperPerformanceNotificationDefaultEnabled(){return paperNotificationSettings().notifyPaperPerformance!==!1}async function ensurePaperPerformanceNotificationDefault(options={}){if(!paperData?.account)return{ok:!1,skipped:"no-account"};if(!paperPerformanceNotificationDefaultEnabled())return{ok:!1,skipped:"disabled"};if(typeof Notification>"u"||Notification.permission!=="granted")return{ok:!1,skipped:"permission"};if(Number(paperData.account.notifyThreshold)>0)return{ok:!0,skipped:"already-on"};const data=await paperPost("notify-settings",{threshold:PAPER_DEFAULT_NOTIFY_THRESHOLD}),threshold=Number(data?.notifyThreshold)||PAPER_DEFAULT_NOTIFY_THRESHOLD;return paperData?.account&&(paperData.account.notifyThreshold=threshold),options.render!==!1&&paperTabActive()&&renderPaperTab(),options.silent||showToast?.(`모의투자 알림을 ±${threshold}%p 기준으로 켰어요`,"info"),{ok:!0,notifyThreshold:threshold}}function paperShareUsesQueryUrl(){try{return location.hostname==="excelkospi.github.io"}catch{return!1}}function paperShareUrl(ref=""){const suffix=ref?paperShareUsesQueryUrl()?`${PAPER_SHARE_PATH}/?p=${encodeURIComponent(ref)}`:`${PAPER_SHARE_PATH}/${encodeURIComponent(ref)}`:paperShareUsesQueryUrl()?`${PAPER_SHARE_PATH}/`:PAPER_SHARE_PATH;try{return new URL(suffix,location.origin).toString()}catch{return`${location.origin||"https://excelkospi.pages.dev"}${suffix}`}}function paperShareToken(value){const token=String(value||"").trim();return/^[A-Za-z0-9_-]{10,32}$/.test(token)?token:""}function paperSharePathToken(pathname){const path=String(pathname||"").replace(/\/+$/g,""),prefix=`${PAPER_SHARE_PATH}/`;if(!path.startsWith(prefix))return"";const encoded=path.slice(prefix.length);if(!encoded||encoded.includes("/"))return"";try{return paperShareToken(decodeURIComponent(encoded))}catch{return""}}function paperShareUrlToken(value){try{const url=new URL(String(value||""),location.origin),path=url.pathname.replace(/\/+$/g,""),pathToken=paperSharePathToken(path);return pathToken||(path!==PAPER_SHARE_PATH?"":paperShareToken(url.searchParams.get("p")))}catch{return""}}function paperContestShareUrl(token,candidate=""){const ref=paperShareToken(token)||paperShareUrlToken(candidate);if(!ref)return"";try{const url=new URL(String(candidate||""),location.origin),path=url.pathname.replace(/\/+$/g,"");if(paperSharePathToken(path)===ref||path===PAPER_SHARE_PATH&&url.searchParams.get("p")===ref)return url.toString()}catch{}return paperShareUrl(ref)}const PAPER_SHARE_REF_BOOT_KEY="kg_paper_share_ref_boot_v1";let paperBootShareRef="";(function(){try{const url=new URL(location.href),path=url.pathname.replace(/\/+$/g,"");let ref=paperSharePathToken(path);if(!ref&&path===PAPER_SHARE_PATH&&(ref=paperShareToken(url.searchParams.get("p"))),ref){paperBootShareRef=ref;try{sessionStorage.setItem(PAPER_SHARE_REF_BOOT_KEY,ref)}catch{}}else try{paperBootShareRef=sessionStorage.getItem(PAPER_SHARE_REF_BOOT_KEY)||""}catch{}}catch{}})();function paperCurrentShareRef(){try{const url=new URL(location.href),path=url.pathname.replace(/\/+$/g,""),pathToken=paperSharePathToken(path);if(pathToken)return pathToken;if(path===PAPER_SHARE_PATH){const q=paperShareToken(url.searchParams.get("p"));if(q)return q}}catch{}return paperBootShareRef||""}function paperKstDayKey(){return new Date(Date.now()+9*60*60*1e3).toISOString().slice(0,10)}function paperUseNativeShare(){try{if(!navigator.share)return!1;const ua=String(navigator.userAgent||"");return!!(window.matchMedia?.("(max-width: 760px)")?.matches||/Mobi|Android|iPhone|iPad|iPod/i.test(ua)||Number(navigator.maxTouchPoints)>1)}catch{return!1}}function paperSetBusy(text="처리 중..."){paperBusy=!0,paperBusyText=text,paperTabActive()&&renderPaperTab()}function paperClearBusy(){paperBusy=!1,paperBusyText="",paperTabActive()&&renderPaperTab()}function paperWon(value,withSign=!1){const n=Number(value);return Number.isFinite(n)?`${withSign&&n>0?"+":""}${Math.round(n).toLocaleString("ko-KR")}원`:"-"}function paperSignedPct(value,digits=2){const n=Number(value);return Number.isFinite(n)?`${n>0?"+":""}${n.toFixed(digits)}%`:"-"}function paperWonShort(value){const n=Math.round(Number(value)||0);if(!Number.isFinite(n)||n<=0)return"0원";if(n>=1e8){const v=n/1e8;return`${v>=100?Math.round(v).toLocaleString("ko-KR"):v.toFixed(1).replace(/\.0$/,"")}억원`}return n>=1e4?`${Math.round(n/1e4).toLocaleString("ko-KR")}만원`:`${n.toLocaleString("ko-KR")}원`}function paperDirClass(value){const n=Number(value);return!Number.isFinite(n)||Math.abs(n)<1e-9?"flat":n>0?"up":"down"}function paperQtyText(market,qty){const n=Number(qty);return Number.isFinite(n)?market==="COIN"?String(Math.round(n*1e6)/1e6):n.toLocaleString("ko-KR"):"-"}function paperRecentShareTrades(limit=8){const max=Math.max(1,Math.min(20,Number(limit)||8));return(paperData?.orders||[]).filter(order=>String(order?.id||"").startsWith("o_")&&(order.side==="buy"||order.side==="sell"&&Number.isFinite(Number(order.realized)))).slice(0,max).map(order=>{const code=String(order.code||"").toUpperCase(),market=String(order.market||"").toUpperCase(),qty=Number(order.qty)||0,price=Number(order.price)||0,amount=Number(order.amount)||0,base={id:String(order.id),code,market,name:String(order.name||order.code||"").slice(0,40),side:order.side==="buy"?"buy":"sell",qty,price,amount,fee:Number(order.fee)||0,createdAt:order.created_at||order.createdAt||"",timeText:typeof relativeTimeKR=="function"?relativeTimeKR(order.created_at||order.createdAt):""};if(base.side==="sell"){const realizedRaw=Number(order.realized)||0,costBasis=amount-realizedRaw,returnPct=costBasis>0?Math.round(realizedRaw/costBasis*1e4)/100:null;return{...base,realized:Math.round(realizedRaw),returnPct}}const markKRW=paperLiveMarkKRW(code,market);return Number.isFinite(markKRW)&&markKRW>0&&price>0?{...base,markPrice:Math.round(markKRW),unrealized:Math.round((markKRW-price)*qty),returnPct:Math.round((markKRW/price-1)*1e4)/100}:base})}window.paperRecentShareTrades=paperRecentShareTrades;function paperTradePctText(order){const pct=Number(order?.returnPct);return Number.isFinite(pct)?`${pct>=0?"+":""}${pct.toFixed(2)}%`:""}window.paperTradePctText=paperTradePctText;function paperShareTradeOptionText(order){const name=order?.name||order?.code||"체결",time=order?.timeText?` · ${order.timeText}`:"",pct=paperTradePctText(order),pctSuffix=pct?` (${pct})`:"";if(order?.side==="buy"){const unrealized=Number(order?.unrealized),valText=Number.isFinite(unrealized)?`평가 ${paperWon(unrealized,!0)}`:"평가 집계 중";return`${name} 매수 · ${valText}${pctSuffix}${time}`}const profit=Number(order?.realized),profitText=Number.isFinite(profit)?paperWon(profit,!0):"손익 집계 중";return`${name} 매도 · ${profitText}${pctSuffix}${time}`}window.paperShareTradeOptionText=paperShareTradeOptionText;function paperJosaEul(word){const s=String(word||"");if(!s)return"를";const c=s.charCodeAt(s.length-1);return c>=44032&&c<=55203?(c-44032)%28===0?"를":"을":"를"}function paperBoastTradeSentence(order){if(!order)return"";const name=String(order.name||order.code||"").trim().slice(0,40);if(!name)return"";const isBuy=order.side==="buy",value=Number(isBuy?order.unrealized:order.realized),amountRaw=Math.round(Number(order.amount)||(Number(order.price)||0)*(Number(order.qty)||0)),amount=amountRaw>0?`${paperWon(Math.abs(amountRaw))}어치 `:"",sideText=isBuy?"사서":"팔아서";if(!Number.isFinite(value))return`${name}${paperJosaEul(name)} ${amount}${sideText}, 손익은 집계 중이에요!`;let sentence;if(Math.round(value)===0)sentence=`${name}${paperJosaEul(name)} ${amount}${sideText}, 손익 0원이에요!`;else{const profit=paperWon(Math.abs(value)),verb=value>0?"벌었어요":"잃었어요";sentence=`${name}${paperJosaEul(name)} ${amount}${sideText}, ${profit} ${verb}!`}const pct=Number(order.returnPct);return Number.isFinite(pct)&&(sentence+=` 수익률은 ${pct>=0?"+":""}${pct.toFixed(2)}%에요!`),sentence}window.paperBoastTradeSentence=paperBoastTradeSentence;function paperBoastTradeValue(order){return order?order.side==="buy"?Number(order.unrealized):Number(order.realized):NaN}function paperBestBoastTrade(){let best=null,fallback=null,bestVal=-1/0;for(const t of paperRecentShareTrades(12)){fallback||(fallback=t);const v=paperBoastTradeValue(t);!Number.isFinite(v)||Math.round(v)===0||v>bestVal&&(bestVal=v,best=t)}return best||fallback}window.paperBestBoastTrade=paperBestBoastTrade;function paperBoastTradeForShare(preferredId=""){const trades=paperRecentShareTrades(20),wanted=String(preferredId||"").trim(),picked=wanted?trades.find(order=>order.id===wanted):null;return picked||paperBestBoastTrade()}window.paperBoastTradeForShare=paperBoastTradeForShare;function paperShareTradeProofHtml(order,className="pp-trade-proof"){if(!order)return"";const isBuy=order.side==="buy",value=Number(isBuy?order.unrealized:order.realized),dir=paperDirClass(Number.isFinite(value)?value:0),market=paperTradeMarketLabel(order.market),qty=paperQtyText(order.market,order.qty),time=order.timeText||"",sideLabel=isBuy?"매수":"매도",cap=isBuy?"선택 매수 · 공유 시점 평가손익":"선택 매매 실현손익",aria=isBuy?"선택한 매수 평가손익":"선택한 매매 실현손익",pct=paperTradePctText(order),valueText=Number.isFinite(value)?`${paperWon(value,!0)}${pct?` (${pct})`:""}`:isBuy?"평가 집계 중":"손익 집계 중";return`<div class="${className} ${dir}" role="group" aria-label="${aria}">
    <span class="${className}-cap">${esc(cap)}</span>
    <b class="${className}-profit">${esc(valueText)}</b>
    <span class="${className}-meta">${esc([market,order.name||order.code,`${qty} ${sideLabel}`,time].filter(Boolean).join(" · "))}</span>
  </div>`}const PAPER_FETCH_TIMEOUT_MS=12*1e3,PAPER_FETCH_PORTFOLIO_TIMEOUT_MS=8*1e3,PAPER_FETCH_MAX_ATTEMPTS=2,PAPER_LOAD_WATCHDOG_MS=18*1e3;let paperPageHiding=!1;try{addEventListener("pagehide",()=>{paperPageHiding=!0},{capture:!0})}catch{}function paperFetchMethod(init={}){return String(init?.method||"GET").trim().toUpperCase()||"GET"}function paperFetchUrl(url){try{return new URL(String(url||""),location.href)}catch{return null}}function paperFetchPath(url){return paperFetchUrl(url)?.pathname||String(url||"").split(/[?#]/)[0]||"/"}function paperFetchAction(url,init={}){const method=paperFetchMethod(init),parsed=paperFetchUrl(url),path=parsed?.pathname||paperFetchPath(url);if(path==="/api/paper"){if(method==="GET")return(parsed?.searchParams?.get("action")||"")==="leaderboard"?parsed?.searchParams?.get("participants")?"load_participants":"load_leaderboard":"load_portfolio";if(method==="POST"){try{const body=JSON.parse(String(init?.body||"{}")),action=String(body?.action||"").trim().replace(/[^a-z0-9_-]+/gi,"_").slice(0,40);if(action==="order")return"submit_order";if(action==="create"||action==="member-create"||action==="member-new")return"create_account";if(action==="recover")return"recover_account";if(action==="reset")return"reset_account";if(action)return`post_${action}`}catch{}return"post_paper"}}if(path==="/api/quote"){const code=String(parsed?.searchParams?.get("code")||parsed?.searchParams?.get("codes")||"");return/KRW%3DX|KRW=X/i.test(code)?"load_fx_quote":"load_quotes"}return path==="/api/community"?"share_board":path==="/api/paper-share"?"share_snapshot":"api"}function paperFetchDiagnostic(url,init={},extra={}){return{method:paperFetchMethod(init),path:paperFetchPath(url),status:Math.max(0,Math.min(999,Math.floor(Number(extra.status||0)||0))),durationMs:Math.max(0,Math.round(Number(extra.durationMs||0)||0)),timeoutMs:PAPER_FETCH_TIMEOUT_MS,aborted:!!extra.aborted,cache:String(init?.cache||extra.cache||"no-store").slice(0,80),source:String(extra.source||"paper").slice(0,80),error:String(extra.error||"").slice(0,140),responseError:String(extra.responseError||"").slice(0,140)}}function paperResponseApi(res,fallback=null){return res?.__paperApi||fallback||{}}function paperResponseErrorText(data){return String(data?.code||data?.error||data?.reason||"").slice(0,120)}function paperAddBreadcrumb(action,api={},extra={}){try{typeof window.__xkAddBreadcrumb=="function"&&window.__xkAddBreadcrumb("paper",action,{...api,...extra})}catch{}}function paperReportError(action,error,meta={}){try{if(error&&typeof error=="object"){if(error.__paperReported)return;error.__paperReported=!0}if(typeof window.reportExcelkospiError!="function")return;window.reportExcelkospiError("paper",action||"error",error,{code:String(error?.payload?.code||error?.payload?.error||error?.code||error?.name||action||"paper_error"),status:Number(error?.status||error?.api?.status||0)||0,aborted:!!(error?.aborted||error?.api?.aborted),api:error?.api||null,appState:{timeline:"paper",hasAccount:!!paperData?.account,hasSnapshot:!!paperData,hasLeaderboard:!!paperLeaderboard,loadInFlight:!!paperLoadInFlight},...meta})}catch{}}function paperFetchShouldRetry(error,method){if(method!=="GET"&&method!=="HEAD"||error?.aborted)return!1;try{if(typeof isRetryableFetchError=="function")return isRetryableFetchError(error)}catch{}const msg=String(error?.message||error||"");return/failed to fetch|networkerror|network error|load failed|api_fetch_failed/i.test(msg)}function paperRetryDelayMs(attempt){try{if(typeof fetchRetryDelayMs=="function")return fetchRetryDelayMs(attempt)}catch{}return Math.min(900,320+Math.max(0,attempt)*260)}function paperSleep(ms){try{if(typeof sleep=="function")return sleep(ms)}catch{}return new Promise(resolve=>setTimeout(resolve,Math.max(0,Number(ms)||0)))}async function paperFetch(url,init={}){const{timeoutMs:timeoutOverride,retry:retryOverride,...fetchInit}=init||{},method=paperFetchMethod(fetchInit),action=paperFetchAction(url,fetchInit),perAttemptTimeout=Math.max(1e3,Number(timeoutOverride)||PAPER_FETCH_TIMEOUT_MS),maxAttempts=Math.max(1,Number.isFinite(Number(retryOverride))?Number(retryOverride):(method==="GET"||method==="HEAD")&&fetchInit.body==null?PAPER_FETCH_MAX_ATTEMPTS:1);let lastError=null;for(let attempt=0;attempt<maxAttempts;attempt+=1){const controller=new AbortController;let timedOut=!1;const timeoutError=new Error("요청이 너무 오래 걸려요. 네트워크 상태를 확인해 주세요.");timeoutError.aborted=!0;const startedAt=Date.now(),timer=setTimeout(()=>{timedOut=!0;try{controller.abort(timeoutError)}catch{}},perAttemptTimeout);try{const res=await fetch(url,{...fetchInit,signal:controller.signal});clearTimeout(timer);const api=paperFetchDiagnostic(url,fetchInit,{status:res?.status,durationMs:Date.now()-startedAt,cache:res?.headers?.get?.("x-excelkospi-cache")||res?.headers?.get?.("x-quote-cache")||"",source:res?.headers?.get?.("x-excelkospi-source")||""});try{res.__paperApi=api}catch{}return paperAddBreadcrumb(res?.ok===!1?"api_http_error":"api_success",api,{action}),res}catch(error){clearTimeout(timer);const aborted=timedOut||error?.name==="AbortError",out=aborted?timeoutError:error,api=paperFetchDiagnostic(url,fetchInit,{durationMs:Date.now()-startedAt,aborted,error:aborted?"timeout":error?.name||error?.message||"fetch_failed"});if(out&&typeof out=="object"&&(out.aborted=aborted,out.api=out.api||api),lastError=out,paperPageHiding)throw out&&typeof out=="object"&&(out.__paperReported=!0),out;if(attempt<maxAttempts-1&&paperFetchShouldRetry(out,method)){paperAddBreadcrumb("api_retry",{...api,attempt:attempt+1,attempts:maxAttempts,retry:!0},{action}),await paperSleep(paperRetryDelayMs(attempt));continue}throw paperAddBreadcrumb("api_failure",api,{action}),paperReportError(action,out,{phase:"fetch"}),out}}throw lastError||new Error("요청이 너무 오래 걸려요. 네트워크 상태를 확인해 주세요.")}function paperIdentityError(data){const code=String(data?.code||data?.error||"");return code==="chat_identity_required"||code==="identity_mismatch"||code==="identity_owner_mismatch"}async function paperIdentityToken(force=!1){try{if(typeof ensureChatIdentityToken=="function")return await ensureChatIdentityToken(force?{force:!0}:{})}catch{}return""}function paperClearIdentityToken(){try{typeof clearChatIdentityToken=="function"&&clearChatIdentityToken()}catch{}}function paperCachedIdentityToken(){try{if(typeof readChatIdentityToken=="function")return readChatIdentityToken()}catch{}return""}async function paperReadJsonSafe(res){let text;try{text=await res.text()}catch{return{ok:!1,error:"응답을 받다가 끊겼어요. 네트워크 상태를 확인하고 다시 시도해 주세요.",_readFail:"aborted"}}if(text==null||text==="")return{ok:!1,error:"응답이 비어 있어요. 잠시 후 다시 시도해 주세요.",_readFail:"empty"};try{return JSON.parse(text)}catch{return{ok:!1,error:"응답을 읽지 못했어요",_readFail:String(text).trimStart().charAt(0)==="<"?"shell":"parse"}}}async function paperFetchJsonWithIdentity(url,init={},retry=!0,token=paperCachedIdentityToken()){const headers={...init.headers||{},...token?{"x-chat-identity-token":token}:{},...paperAuthHeader()},res=await paperFetch(url,{...init,headers}),data=await paperReadJsonSafe(res);if((!res.ok||data?.ok===!1)&&paperIdentityError(data)&&retry){paperClearIdentityToken();const freshToken=await paperIdentityToken(!0);return paperFetchJsonWithIdentity(url,init,!1,freshToken)}if(!res.ok||data?.ok===!1){const error=new Error(data?.error||`요청 실패 (${res.status})`);throw error.status=res.status,error.payload=data,error.api={...paperResponseApi(res,paperFetchDiagnostic(url,init,{status:res.status})),responseError:paperResponseErrorText(data)},paperReportError(paperFetchAction(url,init),error,{phase:"response"}),error}return data}async function paperPostJsonWithIdentity(url,payload={},retry=!0,token=paperCachedIdentityToken()){const body={...payload,...token?{identity_token:token}:{}},res=await paperFetch(url,{method:"POST",headers:{"content-type":"application/json",...token?{"x-chat-identity-token":token}:{},...paperAuthHeader()},body:JSON.stringify(body)}),data=await paperReadJsonSafe(res);if((!res.ok||data?.ok===!1)&&paperIdentityError(data)&&retry){paperClearIdentityToken();const freshToken=await paperIdentityToken(!0);return paperPostJsonWithIdentity(url,payload,!1,freshToken)}if(!res.ok||data?.ok===!1){const error=new Error(data?.error||`요청 실패 (${res.status})`);throw error.status=res.status,error.payload=data,error.api={...paperResponseApi(res,paperFetchDiagnostic(url,{method:"POST"},{status:res.status})),responseError:paperResponseErrorText(data)},paperReportError(paperFetchAction(url,{method:"POST",body:JSON.stringify(body)}),error,{phase:"response"}),error}return data}async function paperPost(action,payload={}){const userId=paperUserIdSafe();if(action!=="create")return paperPostJsonWithIdentity(paperApiUrl("/api/paper"),{action,user_id:userId,...payload});const attempt=async(fresh,forceIdentity=!1)=>{const identityToken=forceIdentity?await paperIdentityToken(!0):paperCachedIdentityToken(),ttok=fresh&&typeof chatTurnstileToken=="function"?await chatTurnstileToken(1e4,{fresh:!0,visibleTimeoutMs:1e4}).catch(()=>""):"",res2=await paperFetch(paperApiUrl("/api/paper"),{method:"POST",headers:{"content-type":"application/json",...identityToken?{"x-chat-identity-token":identityToken}:{},...paperAuthHeader()},body:JSON.stringify({action,user_id:userId,...ttok?{turnstile_token:ttok}:{},...identityToken?{identity_token:identityToken}:{},...payload})}),data2=await paperReadJsonSafe(res2);return{res:res2,data:data2}};let{res,data}=await attempt(!1);if((!res.ok||data?.ok===!1)&&paperIdentityError(data)&&(paperClearIdentityToken(),{res,data}=await attempt(!1,!0)),res.status===403&&(data?.code==="turnstile_required"||data?.error==="turnstile_required")&&({res,data}=await attempt(!0)),(!res.ok||data?.ok===!1)&&paperIdentityError(data)&&(paperClearIdentityToken(),{res,data}=await attempt(!0,!0)),!res.ok||data?.ok===!1){const error=new Error(data?.error||`요청 실패 (${res.status})`);throw error.status=res.status,error.payload=data,error.api={...paperResponseApi(res,paperFetchDiagnostic(paperApiUrl("/api/paper"),{method:"POST"},{status:res.status})),responseError:paperResponseErrorText(data)},paperReportError(paperFetchAction(paperApiUrl("/api/paper"),{method:"POST",body:JSON.stringify({action})}),error,{phase:"response"}),error}return data}async function paperFetchPortfolio(){const userId=paperUserIdSafe();return paperFetchJsonWithIdentity(paperApiUrl(`/api/paper?user_id=${encodeURIComponent(userId)}`),{cache:"no-store",timeoutMs:PAPER_FETCH_PORTFOLIO_TIMEOUT_MS})}async function paperMaybeClaimShareReferral(){const ref=paperCurrentShareRef();if(!ref||!paperData?.account)return;const key=`${PAPER_SHARE_REFERRAL_CLAIM_KEY}:${ref}:${paperKstDayKey()}`;try{if(localStorage.getItem(key))return}catch{}try{const data=await paperPost("share-referral-claim",{ref});if(data?.rewarded||data?.welcomeBonus||data?.reason==="visitor_dup"||data?.reason==="self")try{localStorage.setItem(key,String(Date.now()))}catch{}data?.welcomeBonus?(paperWelcomeBonusAmount=Number(data.welcomeBonus)||0,paperWelcomeTicket=!!data.welcomeTicket,paperTabActive()&&renderPaperTab()):data?.rewarded&&showToast?.("공유해주신 분께 초대 보너스가 지급됐어요","info")}catch(e){debugWarn("paper referral claim failed",e)}}async function paperMaybeLoadShareInvite(){const ref=paperCurrentShareRef();if(!(!ref||paperShareInviteRefTried===ref)){paperShareInviteRefTried=ref;try{const data=await(await fetch(paperApiUrl(`/api/paper?action=share-info&ref=${encodeURIComponent(ref)}`),{cache:"no-store",headers:{accept:"application/json"}})).json().catch(()=>null);data?.ok&&data.snapshot&&(paperShareInviteSnapshot={...data.snapshot,__ref:ref},renderPaperTab())}catch(e){debugWarn("share-info load failed",e)}}}function paperShareInviteBannerHtml(){const s=paperShareInviteSnapshot;if(!s||s.__ref&&s.__ref!==paperCurrentShareRef())return"";const nick=esc(s.nickname||"월급루팡"),pct=Number(s.returnPct)||0,rank=Number(s.rank)||0,proof=[];pct&&proof.push(`<b>${pct>0?"+":""}${Math.round(pct*100)/100}%</b> 수익`),rank&&proof.push(`전체 ${rank}위`);const proofText=proof.length?` · 지금 ${proof.join(" · ")} 🔥`:"",bonus=Math.round((Number(s.welcomeBonus)||0)/1e4),ticketLine=s.drawTicketsEvent?" + <b>랜덤주식 1주 뽑기권 1장</b>":"",sub=bonus?`지금 시작하면 시작자금 5,000만 + <b>초대 보너스 ${bonus}만</b>${ticketLine} 🎁`:s.drawTicketsEvent?"지금 함께 시작하면 <b>두 분 모두 주식 뽑기권 +1장</b> 🎁":"지금 함께 시작하면 시작자금 5,000만원으로 바로 출발해요";return`<div class="pp-card pp-invite-banner">
    <span class="pp-invite-ico" aria-hidden="true">🎉</span>
    <div class="pp-invite-tx">
      <span class="pp-invite-head"><b>${nick}</b>님의 초대로 오셨어요${proofText}</span>
      <span class="pp-invite-sub">${sub}</span>
    </div>
  </div>`}function paperQuoteTtlSeconds(){try{const kind=typeof pollProfileKind=="function"?pollProfileKind():"regular";if(kind==="weekend")return 300;if(kind==="extended")return 120}catch{}return 60}async function paperFetchQuotes(positions){const codes=(positions||[]).map(p=>paperPositionKey(p.code,p.market)).filter(Boolean);paperTicket.code&&codes.push(paperPositionKey(paperTicket.code,paperTicket.market)),codes.some(c=>c.endsWith(":US"))&&codes.push("KRW=X:US");const unique=[...new Set(codes)].slice(0,30);if(!unique.length)return{};const map={},misses=[];for(const c of unique){const[code,market]=c.split(":"),shared=typeof window.sharedQuoteForPaper=="function"?window.sharedQuoteForPaper(code,market):null;shared&&Number.isFinite(Number(shared.price))?map[`${String(code).toUpperCase()}:${String(market).toUpperCase()}`]={price:Number(shared.price),changePct:Number(shared.changePct)}:misses.push(c)}if(!misses.length)return map;try{const res=await paperFetch(paperApiUrl(`/api/quote?codes=${encodeURIComponent(misses.join(","))}&ttl=${paperQuoteTtlSeconds()}&coinSource=upbit`)),data=await res.json();if(!res.ok||data?.ok===!1){const error=new Error(data?.error||`요청 실패 (${res.status})`);throw error.status=res.status,error.payload=data,error.api={...paperResponseApi(res),responseError:paperResponseErrorText(data)},paperReportError("load_quotes",error,{phase:"response"}),error}for(const row of data?.results||[])row?.ok&&Number.isFinite(Number(row.price))&&(map[`${String(row.code||"").toUpperCase()}:${String(row.market||"").toUpperCase()}`]={price:Number(row.price),changePct:Number(row.changePct??row.changePercent)});return map}catch{return map}}async function paperFetchLeaderboard(force=!1,options={}){const needsParticipants=!!options.participants,publicOnly=!!options.publicOnly,hasParticipants=Array.isArray(paperLeaderboard?.participants);if(!force&&paperLeaderboard&&Date.now()-paperLeaderboardAt<18e4&&(!needsParticipants||hasParticipants))return paperLeaderboard;const params=new URLSearchParams({action:"leaderboard"});if(publicOnly?params.set("public","1"):params.set("user_id",paperUserIdSafe()),needsParticipants){params.set("participants","1"),params.set("limit",String(100));const nextOffset=Number(paperLeaderboard?.participantsNextOffset),offset=options.append&&Number.isFinite(nextOffset)?nextOffset:0;offset>0&&params.set("offset",String(offset))}const data=await paperFetchJsonWithIdentity(paperApiUrl(`/api/paper?${params.toString()}`),{cache:"no-store"}).catch(()=>null);if(data?.ok){if(needsParticipants&&options.append&&Array.isArray(paperLeaderboard?.participants)&&Array.isArray(data.participants)){const seen=new Set;data.participants=[...paperLeaderboard.participants,...data.participants].filter(row=>{const key=paperRankRowKey(row);return seen.has(key)?!1:(seen.add(key),!0)})}const nextLeaderboard=needsParticipants?data:{...paperLeaderboard||{},...data,participants:paperLeaderboard?.participants};paperPrepareRankMotion(paperLeaderboard,nextLeaderboard),paperLeaderboard=nextLeaderboard,paperLeaderboardAt=Date.now()}return paperLeaderboard}function paperPrimePublicLeaderboard(seq){return paperLeaderboard?Promise.resolve(paperLeaderboard):paperPublicLeaderboardInFlight||(paperPublicLeaderboardInFlight=paperFetchLeaderboard(!1,{publicOnly:!0}).then(lb=>(seq===paperLoadSeq&&lb&&paperTabActive()&&renderPaperTab(),lb)).catch(()=>null).finally(()=>{paperPublicLeaderboardInFlight=null}),paperPublicLeaderboardInFlight)}const PAPER_SNAPSHOT_KEY="kg_paper_snapshot_v1",PAPER_SNAPSHOT_MAX_AGE_MS=24*60*60*1e3,PAPER_ANON_SNAPSHOT_KEY="kg_paper_anon_snapshot_v1",PAPER_ANON_SNAPSHOT_MAX_AGE_MS=6*60*60*1e3;function clearPaperSnapshot(){try{localStorage.removeItem(PAPER_SNAPSHOT_KEY)}catch{}}window.paperForgetLocalAccount=function(){try{clearPaperSnapshot()}catch{}};function writePaperSnapshot(){try{const leaderboard=paperLeaderboard?{...paperLeaderboard,participants:void 0}:null;if(!paperData?.account){clearPaperSnapshot(),leaderboard&&localStorage.setItem(PAPER_ANON_SNAPSHOT_KEY,JSON.stringify({at:Date.now(),leaderboard,quotes:paperQuoteMap}));return}localStorage.setItem(PAPER_SNAPSHOT_KEY,JSON.stringify({at:Date.now(),userId:paperUserIdSafe(),data:paperData,quotes:paperQuoteMap,leaderboard}))}catch{}}function hydratePaperSnapshot(){if(paperData)return!1;try{const snap=JSON.parse(localStorage.getItem(PAPER_SNAPSHOT_KEY)||"null");if(snap?.data?.account&&String(snap.userId||"")===String(paperUserIdSafe()||"")&&Date.now()-Number(snap.at||0)<=PAPER_SNAPSHOT_MAX_AGE_MS)return paperData=snap.data,paperHydratePinnedBadges(),paperQuoteMap=snap.quotes&&typeof snap.quotes=="object"?snap.quotes:{},snap.leaderboard&&(paperLeaderboard=snap.leaderboard,paperLeaderboardAt=0),!0;const anon=JSON.parse(localStorage.getItem(PAPER_ANON_SNAPSHOT_KEY)||"null");return anon?.leaderboard&&Date.now()-Number(anon.at||0)<=PAPER_ANON_SNAPSHOT_MAX_AGE_MS?(paperLeaderboard=anon.leaderboard,paperLeaderboardAt=0,anon.quotes&&typeof anon.quotes=="object"&&(paperQuoteMap=anon.quotes),!0):!1}catch{return!1}}async function loadPaperTab(options={}){const bg=!!options.silent;if(clearPaperQuoteRefresh(),paperLoadInFlight){if(Date.now()-paperLoadStartedAt<PAPER_LOAD_WATCHDOG_MS)return paperLoadInFlight;paperLoadSeq+=1,paperLoadInFlight=null,paperLoadingText="",paperVisibleLoadPending=!1,paperError="모의투자 로딩이 오래 걸려 다시 시도할게요.",paperRecordColdLoadFailure(),paperTabActive()&&renderPaperTab({background:bg})}if(await paperRestoreIdentityStorage(),paperLoadInFlight&&Date.now()-paperLoadStartedAt<PAPER_LOAD_WATCHDOG_MS)return paperLoadInFlight;if(!options.silent&&!options.force&&!options.forceLb&&!options.allowRecentReload&&paperData&&Date.now()-paperLoadedAt<12e3)return paperTabActive()&&renderPaperTab(),paperData;const seq=paperLoadSeq+1;paperLoadSeq=seq,paperLoadStartedAt=Date.now(),paperLoadWatchdogTimer&&clearTimeout(paperLoadWatchdogTimer),paperLoadWatchdogTimer=setTimeout(()=>{seq!==paperLoadSeq||!paperLoadInFlight||(paperLoadSeq+=1,paperLoadInFlight=null,paperLoadingText="",paperVisibleLoadPending=!1,paperError="모의투자 로딩이 오래 걸려 중단했어요. 다시 불러오기를 눌러 주세요.",paperRecordColdLoadFailure(),paperTabActive()&&renderPaperTab({background:bg}),paperTabActive()&&schedulePaperRefresh())},PAPER_LOAD_WATCHDOG_MS),paperError="",paperVisibleLoadPending=!options.silent;const hydrated=!paperData&&!options.silent&&hydratePaperSnapshot(),shouldRenderLoading=!options.silent&&!paperData;return paperLoadingText=options.silent||hydrated?"":options.forceLb?"랭킹을 다시 불러오는 중...":paperData?"모의투자 현황을 새로고침 중...":"모의투자 데이터를 불러오는 중...",(paperLoadingText||hydrated||shouldRenderLoading)&&paperTabActive()&&renderPaperTab(),paperLoadInFlight=(async()=>{paperMaybeLoadShareInvite();let data=await paperFetchPortfolio();if(seq!==paperLoadSeq)return data;if(data&&!data.account&&!data.deleted&&paperData?.account&&(data={...paperData,leaderboard:data.leaderboard||paperData.leaderboard,policy:data.policy||paperData.policy,accountStale:!0}),paperData=data,paperHydratePinnedBadges(),data?.marks&&typeof data.marks=="object"&&(paperServerMarks={...paperServerMarks,...data.marks}),data?.deleted==="moderation")try{showToast?.("운영정책 위반으로 계정이 삭제되었습니다.","warn")}catch{}if(data?.account&&paperMaybeClaimShareReferral(),data?.leaderboard){const next={...paperLeaderboard||{},...data.leaderboard,participants:paperLeaderboard?.participants};paperPrepareRankMotion(paperLeaderboard,next),paperLeaderboard=next,paperLeaderboardAt=Date.now()}if(paperShouldDeferLiveQuotes(data?.positions))paperTabActive()&&renderPaperTab({background:bg}),paperScheduleDeferredQuotes(data?.positions,seq);else{const quotesPromise=paperFetchQuotes(data?.positions).then(map=>(map&&Object.keys(map).length&&(paperQuoteMap={...paperQuoteMap,...map}),paperMaybeResetQuoteRecovery(data?.positions),map));await Promise.race([quotesPromise,new Promise(resolve=>setTimeout(()=>resolve("__slow__"),2500))])==="__slow__"&&(paperTabActive()&&renderPaperTab({background:bg}),await quotesPromise.catch(()=>null)),seq===paperLoadSeq&&paperScheduleQuoteRecovery(data?.positions,seq)}return paperShowParticipants&&await paperFetchLeaderboard(!!options.forceLb,{participants:!0}).catch(()=>null),await ensurePaperPerformanceNotificationDefault({silent:!0,render:!1}).catch(()=>null),seq!==paperLoadSeq||(paperTabActive()&&renderPaperTab({background:bg}),writePaperSnapshot(),paperLoadedAt=Date.now(),paperConsecutiveLoadFailures=0),data})().catch(error=>{if(paperPageHiding)throw error;if(seq===paperLoadSeq){const hadRealData=!!paperData;hadRealData||paperRecordColdLoadFailure(),paperError=error?.message||"paper load failed",paperReportError("load_tab",error,{phase:"load",hadRealData}),hadRealData||clearPaperSnapshot(),paperTabActive()&&renderPaperTab({background:bg})}throw error}).finally(()=>{if(seq===paperLoadSeq){paperLoadInFlight=null,paperLoadWatchdogTimer&&(clearTimeout(paperLoadWatchdogTimer),paperLoadWatchdogTimer=null);const hadLoadingText=!!paperLoadingText,hadVisibleLoad=!!paperVisibleLoadPending;paperLoadingText="",paperVisibleLoadPending=!1,(hadLoadingText||hadVisibleLoad)&&paperTabActive()&&renderPaperTab(),paperTabActive()&&schedulePaperRefresh()}}),paperLoadInFlight}function paperTabActive(){try{return typeof timelineIsPaper=="function"&&timelineIsPaper()}catch{return!1}}function clearPaperRefresh(){paperTimer&&(clearTimeout(paperTimer),paperTimer=null)}function clearPaperQuoteRefresh(){paperQuoteRefreshTimer&&(clearTimeout(paperQuoteRefreshTimer),paperQuoteRefreshTimer=null)}function paperEquityIsStatic(){const positions=paperData?.positions||[];if(!positions.length)return!0;const policy=paperData?.policy||{};return positions.every(p=>{const m=String(p.market||"").toUpperCase();return m==="COIN"?!1:m==="KR"?policy.krOpen===!1&&policy.krEtfOpen===!1:m==="US"?policy.usOpen===!1:!0})}const PAPER_STATIC_REFRESH_MS=15*60*1e3,PAPER_ACTIVE_REFRESH_MS=75*1e3,PAPER_ACTIVE_WINDOW_MS=2*60*1e3;let paperInteractionAt=0;function paperNoteInteraction(){paperInteractionAt=Date.now()}function paperRecentlyActive(){return Date.now()-paperInteractionAt<PAPER_ACTIVE_WINDOW_MS}function paperRefreshDelayMs(){try{if(paperEquityIsStatic())return PAPER_STATIC_REFRESH_MS;const kind=typeof pollProfileKind=="function"?pollProfileKind():"regular";if(kind==="weekend")return 5*60*1e3;if(kind==="extended")return 3*60*1e3;if(paperRecentlyActive())return Math.min(18e4,PAPER_ACTIVE_REFRESH_MS)}catch{}return 18e4}function paperRecordColdLoadFailure(){paperData||(paperConsecutiveLoadFailures=Math.min(paperConsecutiveLoadFailures+1,6))}function paperRetryJitterMs(maxMs=7e3){const cap=Math.max(1,Number(maxMs)||1);let key="";try{key=paperUserIdSafe()||""}catch{}if(!key)try{key=`${navigator.userAgent||""}|${location.pathname||""}`}catch{}let hash=0;for(let i=0;i<key.length;i+=1)hash=hash*31+key.charCodeAt(i)>>>0;return hash%cap}function paperFailedLoadRetryDelayMs(){const base=5e3*2**(Math.max(1,Math.min(6,Number(paperConsecutiveLoadFailures)||1))-1);return Math.min(3e5,base)+paperRetryJitterMs()}function schedulePaperRefresh(){if(clearPaperRefresh(),window.CHAT_POPUP_MODE||!paperTabActive()||typeof shouldPauseDataRefreshForHidden=="function"&&shouldPauseDataRefreshForHidden())return;const delay=!paperData&&paperError?paperFailedLoadRetryDelayMs():paperRefreshDelayMs();paperTimer=setTimeout(()=>{paperTimer=null,paperTabActive()&&(typeof shouldPauseDataRefreshForHidden=="function"&&shouldPauseDataRefreshForHidden()||loadPaperTab({silent:!0}).catch(e=>debugWarn("paper refresh failed",e)))},delay)}function paperShouldDeferLiveQuotes(positions){return!(!(Array.isArray(positions)?positions:[]).length||paperTicket.code||paperRecentlyActive()||paperMissingPositionQuotesCount()>0)}function paperScheduleDeferredQuotes(positions,seq){clearPaperQuoteRefresh();const list=Array.isArray(positions)?positions.slice():[];if(!list.length||paperEquityIsStatic())return;paperQuoteRefreshTimer=setTimeout(async()=>{if(paperQuoteRefreshTimer=null,seq!==paperLoadSeq||!paperTabActive()||typeof shouldPauseDataRefreshForHidden=="function"&&shouldPauseDataRefreshForHidden())return;const map=await paperFetchQuotes(list).catch(()=>null);map&&Object.keys(map).length&&(paperQuoteMap={...paperQuoteMap,...map},seq===paperLoadSeq&&paperTabActive()&&(renderPaperTab({background:!0}),writePaperSnapshot())),seq===paperLoadSeq&&paperScheduleQuoteRecovery(list,seq)},4500)}function paperApplyApproxRank(rank){if(!rank||typeof rank!="object")return;const r=Number(rank.rank);r>=1&&(paperLeaderboard||(paperLeaderboard={}),paperLeaderboard.me={...paperLeaderboard.me||{},rank:r},Number(rank.total)>0&&(paperLeaderboard.total=Number(rank.total)),Number.isFinite(Number(rank.returnPct))&&(paperLeaderboard.me.returnPct=Number(rank.returnPct)))}const PAPER_RANK_CATCHUP_MS=2500;let paperRankCatchUpTimer=null;function paperScheduleRankCatchUp(){paperRankCatchUpTimer&&clearTimeout(paperRankCatchUpTimer),paperRankCatchUpTimer=setTimeout(()=>{paperRankCatchUpTimer=null,paperTabActive()&&(typeof shouldPauseDataRefreshForHidden=="function"&&shouldPauseDataRefreshForHidden()||(paperLeaderboardAt=0,loadPaperTab({forceLb:!0,silent:!0}).catch(()=>null)))},PAPER_RANK_CATCHUP_MS)}function paperFx(){const validFx=value=>Number.isFinite(value)&&value>=800&&value<=3e3,fx=Number(paperQuoteMap["KRW=X:US"]?.price);if(validFx(fx))return fx;const shared=typeof window.sharedQuoteForPaper=="function"?window.sharedQuoteForPaper("KRW=X","US",10*60*1e3):null,sharedFx=Number(shared?.price);return validFx(sharedFx)?(paperQuoteMap["KRW=X:US"]={price:sharedFx,changePct:Number(shared.changePct)},sharedFx):null}async function paperEnsureFxQuote(){if(paperFx())return paperFx();try{const fxRes=await paperFetch(paperApiUrl("/api/quote?code=KRW%3DX&market=US&ttl=300")),fx=await fxRes.json();if(!fxRes.ok||fx?.ok===!1){const error=new Error(fx?.error||`요청 실패 (${fxRes.status})`);throw error.status=fxRes.status,error.payload=fx,error.api={...paperResponseApi(fxRes),responseError:paperResponseErrorText(fx)},paperReportError("load_fx_quote",error,{phase:"response"}),error}const price=Number(fx?.price);if(fx?.ok&&Number.isFinite(price))return paperQuoteMap["KRW=X:US"]={price,changePct:Number(fx.changePct)},paperFx()}catch(e){paperReportError("load_fx_quote",e,{phase:"catch"})}return null}function paperPositionKey(code,market){const c=String(code||"").trim().toUpperCase(),m=String(market||"").trim().toUpperCase();return c&&m?`${c}:${m}`:""}function paperPositionLiveMark(pos){const quote=paperQuoteMap[paperPositionKey(pos?.code,pos?.market)];let price=Number(quote?.price);if(String(pos?.market||"").toUpperCase()==="US"){const fx=paperFx();price=Number.isFinite(price)&&fx?price*fx:NaN}return Number.isFinite(price)&&price>0?price:NaN}function paperPositionServerMark(pos){const m=Number(paperServerMarks[paperPositionKey(pos?.code,pos?.market)]);return Number.isFinite(m)&&m>0?m:NaN}function paperPositionMark(pos){const live=paperPositionLiveMark(pos);if(Number.isFinite(live)&&live>0)return live;const srv=paperPositionServerMark(pos);return Number.isFinite(srv)&&srv>0?srv:Number(pos?.avg_price)||0}function paperPositionQuoteReady(pos){return Number.isFinite(paperPositionLiveMark(pos))||Number.isFinite(paperPositionServerMark(pos))}function paperMissingPositionQuotesCount(){return(paperData?.positions||[]).filter(pos=>!paperPositionQuoteReady(pos)).length}function paperMissingQuotePositions(positions){return(Array.isArray(positions)?positions:paperData?.positions||[]).filter(pos=>!paperPositionQuoteReady(pos))}function paperMaybeResetQuoteRecovery(positions){paperMissingQuotePositions(positions).length||(paperQuoteRecoveryAttempt=0)}function paperScheduleQuoteRecovery(positions,seq){const missing=paperMissingQuotePositions(positions);if(!missing.length){paperQuoteRecoveryAttempt=0;return}if(paperQuoteRecoveryAttempt>=PAPER_QUOTE_RECOVERY_DELAYS_MS.length||paperEquityIsStatic()||typeof shouldPauseDataRefreshForHidden=="function"&&shouldPauseDataRefreshForHidden())return;const delay=PAPER_QUOTE_RECOVERY_DELAYS_MS[paperQuoteRecoveryAttempt++];clearPaperQuoteRefresh(),paperQuoteRefreshTimer=setTimeout(async()=>{if(paperQuoteRefreshTimer=null,seq!==paperLoadSeq||!paperTabActive()||typeof shouldPauseDataRefreshForHidden=="function"&&shouldPauseDataRefreshForHidden())return;const map=await paperFetchQuotes(missing).catch(()=>null);map&&Object.keys(map).length&&(paperQuoteMap={...paperQuoteMap,...map},seq===paperLoadSeq&&paperTabActive()&&(renderPaperTab({background:!0}),writePaperSnapshot())),seq===paperLoadSeq&&paperScheduleQuoteRecovery(positions,seq)},delay)}function paperLiveMarkKRW(code,market){const quote=paperQuoteMap[paperPositionKey(code,market)];let price=Number(quote?.price);if(!Number.isFinite(price)||price<=0)return NaN;if(String(market).toUpperCase()==="US"){const fx=paperFx();if(!fx)return NaN;price=price*fx}return price}function paperEquity(){const account=paperData?.account;if(!account)return null;let equity=Number(account.cash)||0;for(const pos of paperData?.positions||[])equity+=(Number(pos.qty)||0)*paperPositionMark(pos);return Math.round(equity)}function paperDashWrap(inner){return`<tr class="paper-dash-row"><td class="forecast-dash-cell">${inner}</td></tr>`}function paperLoadStatusCardHtml(kind="loading"){const isError=kind==="error",title=isError?"모의투자 정보를 불러오지 못했어요.":"계좌·랭킹·시세를 나눠서 불러오는 중이에요.",detail=isError?"잠시 후 자동으로 다시 시도합니다. 캐시가 꼬인 브라우저도 새 빌드에서 한 번 더 정리돼요.":"먼저 화면을 열고, 포트폴리오와 랭킹은 도착하는 순서대로 채워요.",action=isError?'<button class="pp-btn" type="button" data-pp-act="refresh">다시 불러오기</button>':"";return`<div class="pp-card pp-load-card ${isError?"is-error":"is-loading"}" role="${isError?"alert":"status"}">
    <div class="pp-load-head">${isError?"":'<span class="news-loading-spin"></span>'}<b>${esc(title)}</b>${action}</div>
    <p>${esc(detail)}</p>
  </div>`}function paperPortfolioLoadingCardsHtml(){return`<div class="pp-card pp-load-card is-loading" role="status">
    <div class="pp-card-head"><span class="pp-card-title">내 계좌</span><em>확인 중</em></div>
    <div class="pp-load-lines" aria-hidden="true"><span></span><span></span><span></span></div>
    <div class="pp-empty">계좌 확인이 끝나면 주문·보유·체결 내역이 바로 들어와요.</div>
  </div>
  <div class="pp-card pp-load-card is-loading" role="status">
    <div class="pp-card-head"><span class="pp-card-title">보유 종목 · 최근 체결</span><em>대기 중</em></div>
    <div class="pp-load-lines" aria-hidden="true"><span></span><span></span></div>
  </div>`}function paperLeaderboardLoadingCardHtml(){return`<div class="pp-card pp-load-card is-loading"><div class="pp-card-head"><span class="pp-card-title">수익률 랭킹</span><em>불러오는 중</em></div>
    <div class="pp-load-lines" aria-hidden="true"><span></span><span></span><span></span></div></div>`}function paperLoadingShellHtml(kind="loading"){const compactLeague=paperLeaderboard?paperLeagueDeckHtml({tradesCompact:!0,includeTrades:!1,rankingCompact:!0}):`<div class="pp-league-wrap">${paperLeaderboardLoadingCardHtml()}</div>`;return paperDashWrap(`<div class="pp pp-loading-shell">
    ${paperPageHeaderHtml()}
    ${paperLoadStatusCardHtml(kind)}
    <div class="pp-grid">
      <div class="pp-col">${paperPortfolioLoadingCardsHtml()}</div>
      <div class="pp-col pp-league-col">${compactLeague}</div>
    </div>
  </div>`)}const PAPER_TOOL_ICON_INFO='<svg class="pp-tool-ico" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><circle cx="12" cy="12" r="9"/><path d="M12 16v-4M12 8h.01"/></svg>',PAPER_TOOL_ICON_GEAR='<svg class="pp-tool-ico" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><circle cx="12" cy="12" r="3"/><path d="M19 12a7 7 0 0 0-.1-1l2-1.6-2-3.4-2.3 1a7 7 0 0 0-1.7-1l-.3-2.5H9.4l-.3 2.5a7 7 0 0 0-1.7 1l-2.3-1-2 3.4 2 1.6a7 7 0 0 0 0 2l-2 1.6 2 3.4 2.3-1a7 7 0 0 0 1.7 1l.3 2.5h5.2l.3-2.5a7 7 0 0 0 1.7-1l2.3 1 2-3.4-2-1.6a7 7 0 0 0 .1-1Z"/></svg>';function paperPageHeaderHtml(){const total=Number(paperLeaderboard?.total)||0,sub=total?`가상 5,000만원으로 하는 연습 투자 · 참가 ${total.toLocaleString("ko-KR")}명`:"가상 5,000만원으로 하는 연습 투자 · 베타",tools=paperData?.account?`<div class="pp-page-head-tools">
      <button class="pp-tool" type="button" data-pp-act="help-open" ${paperBusy?"disabled":""}>${PAPER_TOOL_ICON_INFO}이용안내</button>
      <button class="pp-tool" type="button" data-pp-act="settings-open" ${paperBusy?"disabled":""}>${PAPER_TOOL_ICON_GEAR}설정</button>
    </div>`:"";return`<div class="pp-page-head"><div class="pp-page-head-main"><strong>excelkospi 모의투자</strong><em>${esc(sub)}</em></div>${tools}</div>`}function paperParticipantBadgeHtml(){const total=Number(paperLeaderboard?.total)||0;return total?`<span class="pp-onboard-count" aria-label="참가자 ${total.toLocaleString("ko-KR")}명">참가 ${total.toLocaleString("ko-KR")}명</span>`:""}function paperProgressHtml(){const text=paperBusyText||paperLoadingText;return text?`<div class="pp-progress" role="status"><span class="news-loading-spin"></span><b>${esc(text)}</b><em>백그라운드에서 자동으로 갱신돼요. 다른 화면을 봐도 돼요.</em></div>`:""}function paperSummaryLoading(){return!!paperVisibleLoadPending&&!!paperData?.account}const PAPER_RECOVERY_ACK_KEY="kg_paper_recovery_ack_v1";function paperRecoveryBannerHtml(){return!paperData?.account||paperIsLoggedIn()?"":!!paperData?.policy?.drawTicketsEvent?`<div class="pp-login-nudge">
      <span>로그인하면 투자한 내용이 모든 기기에서 자동으로 연동돼요. 공유 랜덤주식 1주 뽑기권·보상도 안전하게 보관돼요.</span>
      <button class="pp-btn pp-btn-primary" type="button" data-pp-act="signup-start" ${paperBusy?"disabled":""}>간단 가입하고 이어가기</button>
      <em class="pp-login-nudge-warn">로그인하지 않으면 브라우저 저장소 상태에 따라 랜덤주식 1주 뽑기권이 사라질 수 있어요.</em>
    </div>`:`<div class="pp-login-nudge">
      <span>로그인하면 투자한 내용이 모든 기기에서 자동으로 연동돼요. 복구코드를 따로 보관하지 않아도 계좌를 안전하게 지킬 수 있어요.</span>
      <button class="pp-btn pp-btn-primary" type="button" data-pp-act="signup-start" ${paperBusy?"disabled":""}>간단 가입하고 이어가기</button>
    </div>`}function paperHelpContentHtml(){const rankDays=Number(paperData?.policy?.inactiveRankDays)||7,deleteDays=Number(paperData?.policy?.inactiveDeleteDays??paperData?.policy?.inactiveDays)||10;return`<p class="pp-disc pp-beta-disc"><b>베타 기능</b><br>거래내역과 계좌가 예고 없이 초기화될 수 있어요.</p>
    <p class="pp-disc">가상 자금으로 하는 연습 투자예요. 체결가·잔고·순위는 모두 서버가 계산해요.
    ${paperIsLoggedIn()?"회원 계정은 1년 동안 접속하지 않으면 계정이 삭제될 수 있어요.":`${rankDays}일 동안 접속하지 않으면 랭킹에서 빠지고, ${deleteDays}일이 지나면 계좌가 삭제돼요.`}</p>
    <p class="pp-disc"><b>운영정책</b><br>부적절한 닉네임·활동(욕설·사칭·도배·어뷰징 등)이 확인되면 사전 통지 없이
    닉네임이 임의로 변경되거나, 계정이 제한·삭제될 수 있어요. 가상 자산이라 보상 대상이 아니에요.</p>
    <p class="pp-disc"><b>거래 시간</b><br>
    국내주식: 거래일 08:00~20:00 (정규장 앞뒤 시간외 포함, ETF는 09:00~15:30만)<br>
    해외주식: 미국 프리장~애프터장 (달러 시세에 환율을 곱해 원화로 체결)<br>
    코인: 24시간</p>
    <p class="pp-disc"><b>수수료</b> (실제 한국 표준 요율과 같아요)<br>
    국내 0.015% (매도 시 증권거래세 0.2% 추가) · 해외 0.25% · 코인 0.05%</p>`}function paperHelpModalHtml(){return paperHelpOpen?`<div class="pp-modal-backdrop" data-pp-act="help-close">
    <div class="pp-modal pp-info-modal" role="dialog" aria-modal="true" aria-label="이용안내" data-pp-stop>
      <div class="pp-modal-head"><strong>이용안내</strong><button class="pp-modal-x" type="button" data-pp-act="help-close" aria-label="닫기">×</button></div>
      <div class="pp-modal-body pp-info-body">${paperHelpContentHtml()}</div>
    </div>
  </div>`:""}function paperSettingsModalHtml(){if(!paperSettingsOpen||!paperData?.account)return"";const resetDays=Number(paperData?.policy?.resetCooldownDays)||7,notifyOn=Number(paperData?.account?.notifyThreshold)>0,nickReady=paperNicknameCanChange(),notifyRow=`<div class="pp-settings-row">
      <span class="pp-settings-emoji" aria-hidden="true">🔔</span>
      <div class="pp-settings-tx"><b>모의투자 알림</b><small>순위가 10% 이상 움직이거나 수익률이 ±5%p 변하면 알려드려요(하루 최대 3회).</small></div>
      <select id="ppNotifySelect" class="pp-settings-select" ${paperBusy?"disabled":""} aria-label="모의투자 알림">
        ${[0,5].map(v=>`<option value="${v}" ${(notifyOn?5:0)===v?"selected":""}>${v===0?"끄기":"켜기"}</option>`).join("")}
      </select>
    </div>`,nickRow=`<div class="pp-settings-row">
      <span class="pp-settings-emoji" aria-hidden="true">✏️</span>
      <div class="pp-settings-tx"><b>닉네임 변경</b><small>7일에 한 번 · 채팅·게시판·랭킹 공통${nickReady?"":` · ${esc(paperNicknameChangeButtonText())} 가능`}</small></div>
      <button class="pp-btn pp-btn-sm" type="button" data-pp-act="nickname-edit" ${paperBusy||!nickReady?"disabled":""}>변경</button>
    </div>`,recoverRow=paperIsLoggedIn()?"":`<div class="pp-settings-row">
      <span class="pp-settings-emoji" aria-hidden="true">🔑</span>
      <div class="pp-settings-tx"><b>복구코드 · 다른 기기 연결</b><small>QR로 다른 기기에서 이어가기 · 복구코드 재발급</small></div>
      <div class="pp-settings-act">
        <button class="pp-btn pp-btn-sm" type="button" data-pp-act="qr" ${paperBusy?"disabled":""}>QR 연결</button>
        <button class="pp-btn pp-btn-sm" type="button" data-pp-act="reissue" ${paperBusy?"disabled":""}>재발급</button>
      </div>
    </div>`,badgeRow=`<div class="pp-settings-row">
      <span class="pp-settings-emoji" aria-hidden="true">🏅</span>
      <div class="pp-settings-tx"><b>뱃지 도감</b><small>전체 뱃지 보기 · 닉네임에 달 뱃지 고르기</small></div>
      <button class="pp-btn pp-btn-sm" type="button" data-pp-act="badge-modal-open" ${paperBusy?"disabled":""}>열기</button>
    </div>`,resetRow=`<div class="pp-settings-row pp-settings-row-danger">
      <span class="pp-settings-emoji" aria-hidden="true">♻️</span>
      <div class="pp-settings-tx"><b>계좌 초기화</b><small>${resetDays}일에 한 번 · 보유·거래내역을 지우고 5,000만원으로 새 출발</small></div>
      <button class="pp-btn pp-btn-sm pp-btn-danger" type="button" data-pp-act="reset-account" ${paperBusy?"disabled":""}>초기화</button>
    </div>`;return`<div class="pp-modal-backdrop" data-pp-act="settings-close">
    <div class="pp-modal pp-settings-modal" role="dialog" aria-modal="true" aria-label="설정" data-pp-stop>
      <div class="pp-modal-head"><strong>설정</strong><button class="pp-modal-x" type="button" data-pp-act="settings-close" aria-label="닫기">×</button></div>
      <div class="pp-modal-body pp-settings-list">${notifyRow}${nickRow}${recoverRow}${badgeRow}${resetRow}</div>
    </div>
  </div>`}function paperAccountSeedCash(){const accountSeed=Number(paperData?.account?.seedCash??paperData?.account?.seed_cash);if(Number.isFinite(accountSeed)&&accountSeed>0)return accountSeed;const policySeed=Number(paperData?.policy?.seedCash);return Number.isFinite(policySeed)&&policySeed>0?policySeed:5e7}function paperMyRankSnapshot(){const account=paperData?.account;if(!account)return null;const equity=paperEquity(),seed=paperAccountSeedCash(),returnPct=Number.isFinite(equity)?Math.round((equity/seed-1)*1e4)/100:null,me=paperLeaderboard?.me||{};return{nickname:account.nickname||"월급루팡",rank:Number(me.rank)||null,total:Number(paperLeaderboard?.total)||null,equity,profit:Number.isFinite(equity)?equity-seed:null,returnPct,ordersCount:Number(account.orders_count??account.ordersCount)||0,buyOrdersCount:Number(account.buy_orders_count??account.buyOrdersCount)||0,sellOrdersCount:Number(account.sell_orders_count??account.sellOrdersCount)||0,badges:paperMyNickBadgeEmojis()}}function paperNumberAttr(value){const number=Number(value);return Number.isFinite(number)?String(Math.round(number*100)/100):""}function paperShareDomNumber(name){try{const root=document.querySelector(".pp-summary");if(!root)return null;const raw=root.getAttribute(`data-pp-${name}`);if(raw===null||raw==="")return null;const number=Number(raw);return Number.isFinite(number)?number:null}catch{return null}}function paperShareVisibleSnapshot(base=null){const root=document.querySelector(".pp-summary");if(!root||root.getAttribute("data-pp-loading")==="1")return null;const number=(name,fallback=null)=>{const value=paperShareDomNumber(name);return value===null?fallback:value},snap=base||{},equity=number("equity",snap.equity),profit=number("profit",snap.profit),returnPct=number("return-pct",snap.returnPct),rank=number("rank",snap.rank),total=number("total",snap.total),ordersCount=number("orders",snap.ordersCount);return{...snap,equity,profit,returnPct,rank:Number.isFinite(rank)&&rank>0?Math.round(rank):null,total:Number.isFinite(total)&&total>0?Math.round(total):null,ordersCount:Number.isFinite(ordersCount)&&ordersCount>=0?Math.round(ordersCount):snap.ordersCount||0}}function paperProfitText(profit){if(!Number.isFinite(Number(profit)))return"손익 집계 중";const value=paperWon(profit,!0);return Number(profit)>0?`수익 ${value}`:Number(profit)<0?`손실 ${value}`:"손익 0원"}function paperProfitSharePhrase(profit){if(!Number.isFinite(Number(profit)))return"손익 집계 중이에요!";const value=paperWon(Math.abs(Number(profit)));return Number(profit)>0?`${value} 벌었어요!`:Number(profit)<0?`${value} 잃었어요!`:"손익 0원이에요!"}function paperShareText(input=null){const snap=input||paperShareVisibleSnapshot(paperMyRankSnapshot())||paperMyRankSnapshot();if(!snap)return"엑셀코스피 모의투자 대회 같이 해보세요!";const rankText=snap.rank&&snap.total?`${snap.total}명 중 ${snap.rank}등`:snap.rank?`${snap.rank}등`:"순위 집계 중",profitText=paperProfitSharePhrase(snap.profit),retText=Number.isFinite(snap.returnPct)?`수익률 ${paperSignedPct(snap.returnPct)}`:"수익률 집계 중";return`${rankText} · ${profitText} · ${retText}`}function paperContestShareText(input=null){return paperShareText(input)}window.paperContestShareText=paperContestShareText;function paperContestShareEligibility(snapshot=paperShareSnapshot()){return paperShareSnapshotReady(snapshot)?Math.max(0,Math.floor(Number(snapshot?.ordersCount??paperData?.account?.orders_count??paperData?.account?.ordersCount)||0))<=0?{ok:!1,reason:"no-order",message:"한 번 이상 주문해야 자랑할 수 있어요"}:{ok:!0}:{ok:!1,reason:"snapshot-not-ready",message:"수익 집계가 화면에 뜬 뒤 다시 공유해 주세요"}}function paperShareSnapshot(){const snap=paperShareVisibleSnapshot(paperMyRankSnapshot());return snap?{...snap,seedCash:paperAccountSeedCash(),topSymbols:paperTopShareSymbols(3)}:null}function paperShareSnapshotReady(snapshot){return!!snapshot&&Number.isFinite(Number(snapshot.equity))&&Number.isFinite(Number(snapshot.profit))&&Number.isFinite(Number(snapshot.returnPct))}function paperContestShareImageUrl(token,candidate=""){const image=String(candidate||"").trim();if(image)return image;const ref=paperShareToken(token);if(!ref)return"";try{return new URL(paperApiUrl(`/api/paper-og?p=${encodeURIComponent(ref)}`),location.href).toString()}catch{return""}}function paperRememberContestSharePreview(share){paperContestSharePreview={url:String(share?.url||""),image:String(share?.image||""),title:String(share.title||"엑셀코스피 모의투자 대회"),text:String(share.text||"모의투자 수익률을 확인하세요."),copyText:String(share?.copyText||""),status:String(share.status||"ready")},paperTabActive()&&renderPaperTab()}function paperContestSharePreviewHtml(){const preview=paperContestSharePreview;if(!preview)return"";const ready=!!preview.url&&!!preview.image&&preview.status!=="loading",statusText=preview.status==="loading"?"공유 이미지 생성 중...":preview.status==="error"?preview.text||"이미지 생성에 실패했어요. 잠시 후 다시 눌러 주세요.":preview.text,body=`${preview.image?`<img src="${esc(preview.image)}" alt="공유 썸네일 미리보기" loading="lazy" decoding="async">`:'<span class="pp-share-preview-thumb"><i>생성중</i></span>'}
    <span><b>${ready?"공유될 썸네일":"수익 자랑 미리보기"}</b><em>${esc(preview.title)}</em><small>${esc(statusText)}</small></span>`;return ready?`<div class="pp-share-ready">
    <a class="pp-share-preview" href="${esc(preview.url)}" target="_blank" rel="noopener" aria-label="공유 썸네일 미리보기">${body}</a>
    <button type="button" class="pp-btn pp-btn-sm pp-share-copy-btn" data-pp-act="copy-share-text" title="공유 문구와 링크를 클립보드에 복사합니다">📋 공유 문구·링크 복사</button>
  </div>`:`<div class="pp-share-preview is-${esc(preview.status||"loading")}" role="status" aria-live="polite">${body}</div>`}function paperRemainingShort(ms){const n=Math.max(0,Number(ms)||0);if(n<=0)return"";const days=Math.ceil(n/(24*60*60*1e3));if(days>=2)return`${days}일`;const hours=Math.ceil(n/(60*60*1e3));return hours>=2?`${hours}시간`:`${Math.ceil(n/(60*1e3))}분`}function paperNicknameNextChangeMs(account=paperData?.account){const ms=Date.parse(account?.nicknameNextChangeAt||"");return Number.isFinite(ms)?ms:0}function paperNicknameCanChange(account=paperData?.account){const nextMs=paperNicknameNextChangeMs(account);return!nextMs||nextMs<=Date.now()}function paperNicknameChangeButtonText(account=paperData?.account){return paperNicknameCanChange(account)?"닉네임 변경":`${paperRemainingShort(paperNicknameNextChangeMs(account)-Date.now())} 후 변경`}function paperNicknameEditorHtml(nickname){return paperNicknameEditing?`<div class="pp-nick-editor">
    <input id="ppRenameInput" type="text" maxlength="12" value="${esc(nickname||"")}" autocomplete="off" ${paperBusy?"disabled":""} aria-label="모의투자 닉네임">
    <button class="pp-btn pp-btn-primary" type="button" data-pp-act="nickname-save" ${paperBusy?"disabled":""}>저장</button>
    <button class="pp-btn" type="button" data-pp-act="nickname-cancel" ${paperBusy?"disabled":""}>취소</button>
    <small>닉네임은 7일에 한 번 변경할 수 있어요.</small>
  </div>`:""}function paperTopHoldingNames(limit=3){return(paperData?.positions||[]).map(pos=>({name:String(pos.name||pos.code||"").trim(),value:(Number(pos.qty)||0)*paperPositionMark(pos)})).filter(item=>item.name&&item.value>0).sort((a,b)=>b.value-a.value).slice(0,limit).map(item=>item.name.slice(0,12))}function paperTopShareSymbols(limit=3){const map=new Map,add=(item,weight)=>{const code=String(item?.code||"").trim().toUpperCase(),market=String(item?.market||"").trim().toUpperCase(),key=`${code}:${market}`;if(!code||!market)return;const prev=map.get(key)||{code,market,name:String(item?.name||code).trim(),score:0};prev.name=prev.name||String(item?.name||code).trim(),prev.score+=Math.max(0,Number(weight)||0),map.set(key,prev)};for(const pos of paperData?.positions||[])add(pos,(Number(pos.qty)||0)*paperPositionMark(pos)*2);for(const order of paperData?.orders||[])add(order,Number(order.amount)||(Number(order.qty)||0)*(Number(order.price)||0));return[...map.values()].filter(item=>item.score>0).sort((a,b)=>b.score-a.score).slice(0,limit).map(item=>({code:item.code.slice(0,16),market:item.market,name:item.name.slice(0,20)}))}function paperBoastMessage(){const snap=paperMyRankSnapshot(),text=paperShareText();if(!snap)return text;const ordersCount=Math.max(0,Math.floor(Number(snap.ordersCount)||0)),bestTrade=paperBoastTradeForShare(),meta={r:snap.rank||0,t:snap.total||0,ret:Number.isFinite(snap.returnPct)?snap.returnPct:null,p:Number.isFinite(snap.profit)?Math.round(snap.profit):null,o:ordersCount,h:paperTopHoldingNames(),b:snap.badges||[]};bestTrade?.id&&(meta.tr=bestTrade.id);const token=`⁣p${JSON.stringify(meta)}⁣`,sentence=paperBoastTradeSentence(bestTrade);if(sentence){const withSummary=`${sentence} ${text} ${token}`;if(withSummary.length<=278)return withSummary;const sentenceOnly=`${sentence} ${token}`;if(sentenceOnly.length<=278)return sentenceOnly;const maxSentence=Math.max(0,277-token.length),clipped=sentence.slice(0,maxSentence).trim();if(clipped)return`${clipped} ${token}`}const cardOnly=`${text} ${token}`;return cardOnly.length<=278?cardOnly:text}function paperBoastEligibility(){const snap=paperMyRankSnapshot();return!paperData?.account||!snap?{ok:!1,reason:"no-account"}:(Number(snap.ordersCount)||0)<=0?{ok:!1,reason:"no-order"}:!Number.isFinite(snap.profit)||Math.round(snap.profit)===0?{ok:!1,reason:"zero-profit"}:{ok:!0,snap}}function paperSyncOnboardDraftFromDom(){const nick=document.getElementById("ppNickInput"),recovery=document.getElementById("ppRecoverInput");nick&&(paperOnboardDraft.nickname=String(nick.value||"")),recovery&&(paperOnboardDraft.recovery=String(recovery.value||""))}function paperOnboardFocusState(){const el=document.activeElement;return!el||el.id!=="ppNickInput"&&el.id!=="ppRecoverInput"&&el.id!=="ppSymbolInput"?null:{id:el.id,value:typeof el.value=="string"?el.value:null,start:Number.isFinite(el.selectionStart)?el.selectionStart:null,end:Number.isFinite(el.selectionEnd)?el.selectionEnd:null}}function paperRestoreOnboardFocus(state){state?.id&&setTimeout(()=>{const next=document.getElementById(state.id);if(!(!next||next.disabled)&&(state.id==="ppSymbolInput"&&state.value!==null&&next.value!==state.value&&(next.value=state.value),next.focus({preventScroll:!0}),state.start!==null&&typeof next.setSelectionRange=="function"))try{next.setSelectionRange(state.start,state.end??state.start)}catch{}},0)}function paperMarketPulseHtml(){const stats=paperLeaderboard?.stats,total=Number(paperLeaderboard?.total)||0;if(!stats&&!total)return"";const popular=(stats?.popular||[]).slice(0,4).map(row=>`<em>${esc(row.name)}<i>${Number(row.holders)||0}명</i></em>`).join("");return`<div class="pp-pulse">
    <span><b>${total.toLocaleString("ko-KR")}</b>명 참가 중</span>
    ${Number(stats?.ordersToday)>0?`<span>오늘 체결 <b>${Number(stats.ordersToday).toLocaleString("ko-KR")}</b>건</span>`:""}
    ${popular?`<span class="pp-pulse-popular">인기 보유 ${popular}</span>`:""}
  </div>`}function paperTradedTodayStripHtml(){const t=paperLeaderboard?.stats?.tradedToday;if(!t)return"";const total=Math.max(0,Number(t.total)||0),buy=Math.max(0,Number(t.buy)||0),sell=Math.max(0,Number(t.sell)||0),count=Math.max(0,Number(t.count)||0);return count<=0||total<=0?'<div class="pp-traded-today pp-traded-quiet"><span class="pp-traded-kicker">오늘 excelkospi 모의투자 거래액</span><b class="pp-traded-total">오늘 첫 거래를 기다리는 중</b></div>':`<div class="pp-traded-today pp-traded-${buy>sell?"hot":sell>buy?"cool":"flat"}">
    <span class="pp-traded-kicker">🔥 오늘 excelkospi 모의투자 거래액</span>
    <b class="pp-traded-total">${paperWonShort(total)}</b>
    <span class="pp-traded-split">매수 <i>${paperWonShort(buy)}</i> · 매도 <i>${paperWonShort(sell)}</i> · ${count.toLocaleString("ko-KR")}건</span>
  </div>`}function paperMemberRecoverBlock(){return`<div class="pp-onboard-recover">
    <span>복구코드로 계좌 복원하기</span>
    <input id="ppRecoverInput" type="text" placeholder="복구코드 (KOSPI-XXXX-XXXX)" autocomplete="one-time-code" autocapitalize="characters" spellcheck="false" value="${esc(paperOnboardDraft.recovery)}" ${paperBusy?"disabled":""}>
    <button class="pp-btn" type="button" data-pp-act="recover" ${paperBusy?"disabled":""}>계좌 불러오기</button>
  </div>`}function paperMemberScenarioHtml(){const status=String(paperMemberScenario||"");if(!status)return"";const copy=status==="no-source"?["브라우저 계좌가 감지되지 않았어요","새 계좌를 만들거나 복구코드로 기존 계좌를 불러오세요."]:status==="conflict"?["계정에 이미 계좌가 있어요","브라우저 계좌는 합치지 않고, 로그인 계정의 계좌를 유지합니다."]:["브라우저 계좌를 계정에 연결했어요","이제 로그인한 기기에서 같은 모의투자 계좌를 사용합니다."];return`<div class="pp-onboard-detect-result" data-state="${esc(status)}">
    <b>${esc(copy[0])}</b>
    <span>${esc(copy[1])}</span>
  </div>`}function paperMemberOnboardingHtml(){const u=typeof currentAuthUser=="function"?currentAuthUser():null,name=esc(String(u?.nickname||u?.loginId||"회원").slice(0,24));return`<div class="pp-card pp-onboard">
    <div class="pp-onboard-head">
      <strong>모의투자에 처음 오신 것을 환영합니다. <em class="tab-new">N</em></strong>
      ${paperMemberScenarioHtml()}
      <span class="pp-onboard-hero"><b>${name}</b>님 어서오세요.</span>
    </div>
    ${paperMarketPulseHtml()}
    <div class="pp-onboard-choices">
      <button class="pp-onboard-choice pp-onboard-choice-primary" type="button" data-pp-act="member-new" ${paperBusy?"disabled":""}>
        <strong>새로 계좌 만들기</strong>
        <span>이 계정에 새로 계좌를 만듭니다.</span>
      </button>
      <button class="pp-onboard-choice" type="button" data-pp-act="member-import" ${paperBusy?"disabled":""}>
        <strong>로그인 없이 쓰던 계좌 불러오기</strong>
        <span>이 계정으로 이 브라우저의 계좌를 가져옵니다.</span>
      </button>
    </div>
    ${paperMemberStage==="recover"?paperMemberRecoverBlock():""}
    <button class="pp-onboard-policy" type="button" data-pp-act="help-open" ${paperBusy?"disabled":""}>▸ 이용 안내 · 거래 시간 · 휴면/초기화 정책</button>
  </div>`}function paperMemberLockedHtml(){return`<div class="pp-card pp-member-locked" role="status">
    <div class="pp-card-head"><span class="pp-card-title">로그인이 필요해요</span></div>
    <p>이 모의투자 계좌는 로그인 계정에 연결되어 있어요. 계정 보호를 위해 로그인한 뒤에 이어서 이용할 수 있어요.</p>
    <div class="pp-onboard-start-actions">
      <a class="pp-btn pp-btn-primary pp-onboard-primary" href="/login">로그인하기</a>
    </div>
  </div>`}function paperHasInviteContext(){const s=paperShareInviteSnapshot;return!!(s&&(!s.__ref||s.__ref===paperCurrentShareRef()))}function paperOnboardPopularHtml(){const popular=(paperLeaderboard?.stats?.popular||[]).slice(0,4);return popular.length?`<div class="pp-poprow"><span class="pp-poprow-lab">🔥 인기 보유</span>${popular.map(row=>`<span class="pp-poprow-chip"><span class="cn">${esc(row.name)}</span><b>${Number(row.holders)||0}</b></span>`).join("")}</div>`:""}function paperOnboardingHtml(){const banner=paperShareInviteBannerHtml();if(paperIsLoggedIn())return banner+paperMemberOnboardingHtml();const mainCtaText=paperHasInviteContext()?"초대 수락하고 시작하기":"간단 가입하고 시작하기",guestForm=paperGuestOnboardStarted?`<div class="pp-onboard-form pp-onboard-guest-form">
      <label class="pp-field"><span>닉네임 (최대 12자)</span>
        <input id="ppNickInput" type="text" maxlength="12" placeholder="예: 단타의신" autocomplete="nickname" autocapitalize="off" spellcheck="false" value="${esc(paperOnboardDraft.nickname)}" ${paperBusy?"disabled":""}>
      </label>
      <button class="pp-btn pp-btn-primary" type="button" data-pp-act="create" ${paperBusy?"disabled":""}>${paperBusyText||"계좌 만들기"}</button>
    </div>`:`<div class="pp-onboard-ctas">
      <button class="pp-btn pp-onboard-cta-main" type="button" data-pp-act="signup-start" ${paperBusy?"disabled":""}>${mainCtaText}</button>
      <button class="pp-btn pp-onboard-cta-sub" type="button" data-pp-act="guest-start" ${paperBusy?"disabled":""}>로그인 없이 시작하기</button>
    </div>`;return banner+`<div class="pp-card pp-onboard pp-onboard-start">
    ${paperParticipantBadgeHtml()}
    <div class="pp-onboard-top">
      <div class="pp-onboard-lead">
        <div class="pp-onboard-ttl"><h3>모의투자 시작하기</h3><em class="tab-new">N</em></div>
        <p class="pp-onboard-seed">가상 자금 <b>5,000만원</b>으로 국내주식·해외주식·코인을 연습해요.</p>
        <p class="pp-onboard-sub">실제 체결가·수수료·환율을 그대로 반영해, 손해는 가상이지만 실력은 진짜로 늘어요. 전체 참가자와 수익률로 겨뤄보세요.</p>
      </div>
      ${paperMarketHoursNoteHtml("clock")}
    </div>
    ${paperOnboardPopularHtml()}
    <ol class="pp-onboard-steps3">
      <li><span class="sn">1</span><span>가입은 <b>5초</b>, 개인정보 없이 가능해요</span></li>
      <li><span class="sn">2</span><span>실제 시세로 주문하면 자동으로 <b>체결</b>돼요</span></li>
      <li><span class="sn">3</span><span>로그인 없이 시작하면 이 <b>브라우저에만</b> 저장돼요</span></li>
    </ol>
    ${guestForm}
    <div class="pp-onboard-recover">
      <span>복구코드로 계좌 복원하기</span>
      <input id="ppRecoverInput" type="text" placeholder="복구코드 (KOSPI-XXXX-XXXX)" autocomplete="one-time-code" autocapitalize="characters" spellcheck="false" value="${esc(paperOnboardDraft.recovery)}" ${paperBusy?"disabled":""}>
      <button class="pp-btn" type="button" data-pp-act="recover" ${paperBusy?"disabled":""}>계좌 복구</button>
    </div>
    <button class="pp-onboard-policy" type="button" data-pp-act="help-open" ${paperBusy?"disabled":""}>▸ 이용 안내 · 거래 시간 · 휴면/초기화 정책</button>
  </div>`}function paperModNoticeHtml(){const acct=paperData?.account;if(!acct)return"";const parts=[];if(acct.deleteAfter){const days=Math.max(0,Math.ceil((Date.parse(acct.deleteAfter)-Date.now())/864e5));parts.push(`<div class="pp-card pp-mod-warn pp-mod-danger" role="alert">
      <strong>⚠️ 계정 삭제 예정</strong>
      <span>계정에서 부적절한 활동이 감지되어 <b>${days}일 후 계정이 삭제</b>됩니다. 문의: <a href="mailto:excelkospi@outlook.com">excelkospi@outlook.com</a></span>
    </div>`)}return acct.modNotice==="nickname_changed"&&parts.push(`<div class="pp-card pp-mod-warn" role="alert">
      <span>부적절한 닉네임이 감지되어 <b>닉네임이 임의로 변경</b>되었습니다.</span>
      <button class="pp-btn pp-btn-sm" type="button" data-pp-act="ack-notice" ${paperBusy?"disabled":""}>확인</button>
    </div>`),parts.join("")}function paperCodeRevealHtml(){return!paperRevealCode||paperIsLoggedIn()?"":`<div class="pp-card pp-code" role="alert">
    <div class="pp-card-head"><span class="pp-card-title">복구코드 저장</span></div>
    <div class="pp-code-row"><code>${esc(paperRevealCode)}</code>
      <button class="pp-btn" type="button" data-pp-act="copy-code" data-code="${esc(paperRevealCode)}" ${paperBusy?"disabled":""}>복사</button>
    </div>
    <span>이 코드가 계좌를 되찾는 유일한 방법이에요. 화면을 캡처하거나 메모에 적어 두세요.</span>
    <button class="pp-btn pp-btn-primary" type="button" data-pp-act="code-done" ${paperBusy?"disabled":""}>저장했어요</button>
  </div>`}function paperLinkHtml(){if(!paperLinkInfo)return"";const shareButton=paperUseNativeShare()?`<button class="pp-btn pp-btn-primary" type="button" data-pp-act="link-share" ${paperBusy?"disabled":""}>공유창 열기</button>`:"";return`<div class="pp-card pp-link">
    <div class="pp-card-head"><span class="pp-card-title">다른 기기 연결</span></div>
    <div class="pp-link-qr" id="ppLinkQr" aria-label="기기 연결 QR"></div>
    <span>휴대폰 카메라로 QR을 찍으면 같은 계좌가 열려요. 링크는 1회용이고 5분이 지나면 만료돼요.</span>
    <div class="pp-code-row"><code class="pp-link-url">${esc(paperLinkInfo.url)}</code>
      ${shareButton}
      <button class="pp-btn" type="button" data-pp-act="copy-code" data-code="${esc(paperLinkInfo.url)}" ${paperBusy?"disabled":""}>주소 복사</button>
      <button class="pp-btn" type="button" data-pp-act="link-close" ${paperBusy?"disabled":""}>닫기</button>
    </div>
  </div>`}function paperSummaryHtml(){const account=paperData?.account||{},summaryLoading=paperSummaryLoading(),equity=paperEquity(),seed=paperAccountSeedCash(),returnPct=Number.isFinite(equity)?(equity/seed-1)*100:null,dir=summaryLoading?"flat":paperDirClass(returnPct),profit=Number.isFinite(equity)?equity-seed:null,profitDir=summaryLoading?"flat":paperDirClass(profit),invested=Math.max(0,equity-(Number(account.cash)||0)),me=paperLeaderboard?.me,meDaily=paperLeaderboard?.meDaily,dailyReturnPct=meDaily&&Number.isFinite(Number(meDaily.returnPct))?Number(meDaily.returnPct):null,dailyProfit=dailyReturnPct!=null&&Number.isFinite(equity)?Math.round(equity-equity/(1+dailyReturnPct/100)):null,dailyDir=paperDirClass(dailyReturnPct),ordersCount=paperOrderBreakdown(account).total,rankText=summaryLoading?"집계 중":me?.rank?`${me.rank}위 / ${paperLeaderboard.total}명`:ordersCount>0?"순위 집계 중":"첫 주문 후 랭킹확인",nickname=account.nickname||"내 계좌",missingQuotes=paperMissingPositionQuotesCount(),quoteNote=missingQuotes?` · ${missingQuotes}종목 시세 확인 중`:"",summaryNote=summaryLoading?"계좌와 시세를 다시 확인하고 있어요":`시드 ${paperWonShort(seed)}${quoteNote}`,nativeTools=`<span class="pp-summary-native-actions" role="group" aria-label="모의투자 도구">
        <button class="pp-summary-tool pp-summary-tool-info" type="button" data-pp-act="help-open" aria-label="이용안내" title="이용안내" ${paperBusy?"disabled":""}>${PAPER_TOOL_ICON_INFO}</button>
        <button class="pp-summary-tool pp-summary-tool-settings" type="button" data-pp-act="settings-open" aria-label="설정" title="설정" ${paperBusy?"disabled":""}>${PAPER_TOOL_ICON_GEAR}</button>
      </span>`;return`<div class="pp-summary pp-${dir}${summaryLoading?" is-loading":""}" data-pp-loading="${summaryLoading?"1":"0"}" data-pp-equity="${summaryLoading?"":esc(paperNumberAttr(equity))}" data-pp-profit="${summaryLoading?"":esc(paperNumberAttr(profit))}" data-pp-return-pct="${summaryLoading?"":esc(paperNumberAttr(returnPct))}" data-pp-rank="${summaryLoading?"":esc(paperNumberAttr(me?.rank))}" data-pp-total="${summaryLoading?"":esc(paperNumberAttr(paperLeaderboard?.total))}" data-pp-orders="${summaryLoading?"":esc(paperNumberAttr(ordersCount))}" aria-busy="${summaryLoading?"true":"false"}">
    <div class="pp-summary-head">
      <span class="pp-summary-nickline"><em>${esc(nickname)}</em></span>
      <span class="pp-summary-rankline">${nativeTools}<button class="pp-summary-rank-badge" type="button" data-pp-act="rank-jump" aria-label="수익률 랭킹 보기"><em>랭킹</em><b>${esc(rankText)}</b><span class="pp-rank-go" aria-hidden="true">›</span></button></span>
    </div>
    ${paperNicknameEditorHtml(nickname)}
    <div class="pp-summary-main">
      <span class="pp-cap">총 평가자산</span>
      <span class="pp-summary-eqline">
        <strong class="pp-summary-num">${summaryLoading?'<span class="news-loading-spin"></span>불러오는 중':paperWon(equity)}</strong>
        ${!summaryLoading&&Number.isFinite(returnPct)?`<span class="pp-delta-pill ${dir}"><i class="pp-delta-arw" aria-hidden="true">${dir==="down"?"▼":dir==="up"?"▲":"·"}</i>${Math.abs(returnPct).toFixed(2)}%</span>`:""}
      </span>
      <span class="pp-summary-retline">
        <em class="pp-summary-profit ${profitDir}">평가손익 ${summaryLoading?"집계 중":paperWon(profit,!0)}</em>
        ${!summaryLoading&&dailyReturnPct!=null?`<em class="pp-summary-ret-daily ${dailyDir}">오늘 ${paperSignedPct(dailyReturnPct)}${dailyProfit!=null?` · ${paperWon(dailyProfit,!0)}`:""}</em>`:""}
        <small>${esc(summaryNote)}</small>
      </span>
    </div>
    <div class="pp-summary-side" aria-label="계좌 구성">
      <span><em>현금</em><b>${summaryLoading?"확인 중":paperWon(account.cash)}</b></span>
      <span><em>투자중</em><b>${summaryLoading?"확인 중":paperWon(invested)}</b></span>
      <span><em>체결</em><b class="pp-trade-breakdown">${summaryLoading?'<span class="pp-summary-loading-mini">확인 중</span>':paperOrderBreakdownHtml(account)}</b></span>
    </div>
    ${paperBoardSharePanelHtml()}
    ${paperActionBarHtml()}
  </div>`}function paperActionBarHtml(){if(!paperData?.account)return"";const summaryLoading=paperSummaryLoading(),earned=paperEarnedBadgeIds(),pinnedSet=new Set(paperPinnedBadgeIds()),ordered=PAPER_BADGE_DEFS.filter(b=>earned.includes(b.id)||pinnedSet.has(b.id)),shown=[...ordered].sort((a,b)=>(pinnedSet.has(b.id)?1:0)-(pinnedSet.has(a.id)?1:0)).slice(0,4),moreCount=Math.max(0,ordered.length-shown.length),chips=ordered.length?shown.map(b=>`<span class="pp-badge-chip${pinnedSet.has(b.id)?" pp-badge-pinned":""}" title="${esc(b.name)} · ${esc(b.desc)}${pinnedSet.has(b.id)?" · 닉네임 표시 중":""}">${esc(b.emoji)}</span>`).join("")+(moreCount?`<span class="pp-badge-more">+${moreCount}</span>`:""):'<span class="pp-badge-empty">아직 뱃지가 없어요 — 첫 주문부터!</span>',ticketsMode=!!paperData?.policy?.drawTicketsEvent,linkSub=ticketsMode?"자랑 카드 링크 복사 · 친구가 시작하면 주식 뽑기권 +1장":"자랑 카드 링크 복사 · 친구가 시작하면 내 시드 +100만원",shareLure=ticketsMode?"수익을 자랑하고 뽑기권을 받으세요":"수익을 자랑하고 친구를 초대해보세요",earnedCount=new Set(earned).size,totalBadges=PAPER_BADGE_DEFS.length,shareBusy=paperBusy||summaryLoading;return`<div class="pp-actionbar" aria-label="내 뱃지와 자랑">
    <div class="pp-badges-line">
      <span class="pp-actionbar-label">내 뱃지</span>
      <span class="pp-badge-chips" aria-label="획득한 뱃지">${chips}</span>
      <button class="pp-badge-codex" type="button" data-pp-act="badge-modal-open" ${paperBusy?"disabled":""}>도감 ${earnedCount}/${totalBadges} ›</button>
    </div>
    <div class="pp-share-line">
      <span class="pp-share-lure">${shareLure}</span>
      <details class="pp-share-menu">
        <summary class="pp-share-trigger"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M4 12v8h16v-8M16 6l-4-4-4 4M12 2v13"/></svg>자랑하기<i class="pp-share-caret" aria-hidden="true">▾</i></summary>
        <div class="pp-share-menu-list" role="menu">
          <button class="pp-share-menu-item" type="button" role="menuitem" data-pp-act="boast-chat" ${shareBusy?"disabled":""}><b>💬 채팅에 자랑</b><small>지금 보는 종목방에 수익 카드를 바로 전송</small></button>
          <button class="pp-share-menu-item" type="button" role="menuitem" data-pp-act="share-board-open" ${shareBusy?"disabled":""}><b>📋 게시판에 자랑</b><small>채널 골라 글 등록 · 매매 1건 첨부</small></button>
          <button class="pp-share-menu-item" type="button" role="menuitem" data-pp-act="share-contest" ${paperBusy||paperShareInFlight||summaryLoading?"disabled":""}><b>🔗 ${paperShareInFlight?"이미지 생성 중…":"링크로 자랑"}</b><small>${linkSub}</small></button>
        </div>
      </details>
    </div>
    ${paperContestSharePreviewHtml()}
  </div>`}function paperBoardShareTradePickerHtml(){const trades=paperRecentShareTrades(12),selectedId=trades.some(order=>order.id===paperBoardShare?.tradeId)?paperBoardShare.tradeId:"",selected=selectedId?trades.find(order=>order.id===selectedId):null,options=['<option value="">전체 수익률 카드만 첨부</option>',...trades.map(order=>`<option value="${esc(order.id)}" ${order.id===selectedId?"selected":""}>${esc(paperShareTradeOptionText(order))}</option>`)].join(""),detail=selected?paperShareTradeProofHtml(selected,"pp-trade-proof"):trades.length?"":'<span class="pp-board-trade-empty">매수·매도 체결이 생기면 그 매매 손익 카드를 붙일 수 있어요.</span>';return`<div class="pp-board-trade-picker">
    <div class="pp-board-trade-control">
      <label for="ppBoardShareTrade">최근 매매 손익</label>
      <select id="ppBoardShareTrade" ${paperBusy||!trades.length?"disabled":""}>${options}</select>
    </div>
    ${detail?`<div class="pp-board-trade-detail">${detail}</div>`:""}
  </div>`}function paperBoardSharePanelHtml(){if(!paperBoardShare)return"";const summaryLoading=paperSummaryLoading(),channel=paperBoardShare.channel||"free";return`<div class="pp-board-share" role="group" aria-label="게시판에 자랑">
    <div class="pp-board-share-head"><span class="pp-cap">게시판에 자랑</span><span class="pp-board-share-hint">내 순위·수익률 카드가 함께 올라가요</span></div>
    <div class="pp-share-channels">${PAPER_SHARE_CHANNELS.map(c=>`<button class="pp-share-ch${c.key===channel?" is-on":""}" type="button" data-pp-act="share-board-channel" data-ch="${esc(c.key)}" ${paperBusy?"disabled":""}>${esc(c.label)}</button>`).join("")}</div>
    <input id="ppBoardShareTitle" class="pp-board-share-title" maxlength="60" placeholder="제목 (선택)" value="${esc(paperBoardShare.title||"")}" ${paperBusy?"disabled":""} />
    <textarea id="ppBoardShareComment" class="pp-board-share-comment" maxlength="500" rows="2" placeholder="내용 (선택)" ${paperBusy?"disabled":""}>${esc(paperBoardShare.body||paperBoardShare.comment||"")}</textarea>
    ${paperBoardShareTradePickerHtml()}
    <div class="pp-board-share-actions">
      <button class="pp-btn pp-btn-primary" type="button" data-pp-act="share-board-submit" ${paperBusy||summaryLoading?"disabled":""}>자랑하기</button>
      <button class="pp-btn" type="button" data-pp-act="share-board-cancel" ${paperBusy?"disabled":""}>취소</button>
    </div>
  </div>`}function paperWelcomeBonusModalHtml(){if(!(paperWelcomeBonusAmount>0))return"";const man=Math.round(paperWelcomeBonusAmount/1e4),nick=esc(paperShareInviteSnapshot?.nickname||"");return`<div class="pp-modal-backdrop" data-pp-act="welcome-bonus-close">
    <div class="pp-modal pp-welcome-modal" role="dialog" aria-modal="true" aria-label="초대 환영 보너스" data-pp-stop>
      <div class="pp-welcome-emoji" aria-hidden="true">🎁</div>
      <strong class="pp-welcome-title">초대 환영 보너스 <span>${man}만원</span> 지급!</strong>
      <p class="pp-welcome-sub">${nick?`<b>${nick}</b>님의 초대로 오신 기념이에요. `:""}내 모의투자 계좌에 시드머니 <b>${man}만원</b>이 바로 들어왔어요.</p>
      ${paperWelcomeTicket?'<div class="pp-welcome-ticket">🎟️ <b>랜덤주식 1주 뽑기권 1장</b>도 받았어요! 공유 링크 이벤트 카드에서 바로 뽑아보세요.</div>':""}
      <button class="pp-btn pp-btn-primary pp-welcome-cta" type="button" data-pp-act="welcome-bonus-close">받고 시작하기</button>
    </div>
  </div>`}function paperBadgeModalHtml(){if(!paperBadgeModalOpen)return"";const earned=new Set(paperEarnedBadgeIds()),pinned=paperPinnedBadgeIds(),pinnedSet=new Set(pinned),earnedOrPinned=b=>earned.has(b.id)||pinnedSet.has(b.id),rows=[...PAPER_BADGE_DEFS].sort((a,b)=>(earnedOrPinned(b)?1:0)-(earnedOrPinned(a)?1:0)).map(b=>{const got=earned.has(b.id),isPinned=pinnedSet.has(b.id),isFocus=paperBadgeFocusId===b.id,pinBtn=got||paperPreservePinnedBadges()&&isPinned?`<button class="pp-badge-pin-btn${isPinned?" is-on":""}" type="button" data-pp-act="badge-pin-toggle" data-badge="${esc(b.id)}" aria-pressed="${isPinned?"true":"false"}">${isPinned?"선택됨":"선택"}</button>`:'<span class="pp-badge-lock">미획득</span>';return`<li class="pp-badge-item${got?" is-earned":isPinned?" is-earned is-pinned-unmet":" is-locked"}${isFocus?" is-focus":""}" data-badge="${esc(b.id)}">
      <span class="pp-badge-item-emoji">${esc(b.emoji)}</span>
      <span class="pp-badge-item-main"><b>${esc(b.name)}<i class="pp-badge-tier pp-badge-tier-${esc(b.tier)}" title="난이도 ${esc(b.tier)}">${esc(b.tier)}</i></b><small>${esc(b.desc)}</small></span>
      ${pinBtn}
    </li>`}).join(""),meDiv=paperLeaderboard?.meDivision||paperData?.leaderboard?.meDivision,canFrame=meDiv==="whale"||meDiv==="shark",curMode=canFrame?paperNickCardModeDraft==="div"||paperNickCardModeDraft==="badge"?paperNickCardModeDraft:paperNickCardMode:"badge",divLabel=meDiv==="whale"?"챌린저":meDiv==="shark"?"다이아":"",pct=paperMyRankPercentile(),pctNote=pct?`모투 ${pct.side==="top"?"상위":"하위"} ${pct.pct}% 뱃지도 같이 붙어요.`:"상·하위 20% 안에 들면 % 뱃지도 같이 붙어요.",modeToggle=canFrame?`<div class="pp-nickcard-toggle">
      <span class="pp-nickcard-toggle-l">닉네임에</span>
      <button class="pp-nickcard-opt${curMode==="badge"?" on":""}" type="button" data-pp-act="nickcard-mode" data-mode="badge">뱃지</button>
      <button class="pp-nickcard-opt pp-nickcard-opt-${esc(meDiv)}${curMode==="div"?" on":""}" type="button" data-pp-act="nickcard-mode" data-mode="div">${esc(divLabel)} 칩</button>
      ${curMode==="div"?`<small class="pp-nickcard-note">뱃지 대신 리그 칩을 달아요. ${esc(pctNote)}</small>`:""}
    </div>`:"",seasonRule=`<div class="pp-season-rule">
    <b>시즌 승강</b>
    <span>2주 종료 때 체결 6회·활동 2일 이상만 승급 대상. 챌린저 상위 50% 잔류·하위 다이아, 다이아 상위 30% 승급·하위 20% 강등, 개미 상위 20% 다이아·상위 3% 챌린저 직행.</span>
  </div>`,dirty=Array.isArray(paperPinDraft)||!canFrame&&paperNickCardMode==="div"||canFrame&&paperNickCardModeDraft&&paperNickCardModeDraft!==paperNickCardMode;return`<div class="pp-modal-backdrop" data-pp-act="badge-modal-close">
    <div class="pp-modal pp-badge-modal" role="dialog" aria-modal="true" aria-label="뱃지 도감" data-pp-stop>
      <div class="pp-modal-head"><strong>뱃지 도감</strong><span class="pp-badge-count">${earned.size}/${PAPER_BADGE_DEFS.length} 획득 · 표시 ${pinned.length}/${PAPER_MAX_PINNED_BADGES}</span><button class="pp-modal-x" type="button" data-pp-act="badge-modal-close" aria-label="닫기">×</button></div>
      ${modeToggle}
      ${seasonRule}
      <ul class="pp-badge-list${curMode==="div"?" is-div-mode":""}">${rows}</ul>
      <div class="pp-modal-foot">
        <button class="pp-btn pp-btn-primary" type="button" data-pp-act="badge-pin-save" ${paperBusy||!dirty?"disabled":""}>닉네임에 달기</button>
        <button class="pp-btn" type="button" data-pp-act="badge-modal-close" ${paperBusy?"disabled":""}>닫기</button>
        <small class="pp-badge-foot-note">최대 ${PAPER_MAX_PINNED_BADGES}개까지 채팅·게시판 닉네임 옆에 표시돼요. 계정에 저장돼 <b>직접 빼기 전까지 다시 접속해도 계속</b> 표시됩니다.</small>
      </div>
    </div>
  </div>`}function paperScrollFocusedBadge(){const id=String(paperBadgeFocusId||"");id&&setTimeout(()=>{[...document.querySelectorAll(".pp-badge-item[data-badge]")].find(el=>el.getAttribute("data-badge")===id)?.scrollIntoView?.({behavior:"smooth",block:"center"})},30)}function paperTicketKrwPrice(){const t=paperTicket,quote=t.code?paperQuoteMap[`${t.code}:${t.market}`]:null,live=Number(quote?.price??t.price);if(!Number.isFinite(live)||live<=0)return null;if(t.market!=="US")return live;const fx=paperFx();return fx?live*fx:null}function paperQtyValueText(market,qty){const n=Number(qty);if(!Number.isFinite(n)||n<=0)return"";if(market==="COIN"){const rounded=Math.floor(n*1e6)/1e6;return rounded>0?String(rounded):""}const whole=Math.floor(n);return whole>0?String(whole):""}function paperHeldQty(){const pos=(paperData?.positions||[]).find(p=>p.code===paperTicket.code&&p.market===paperTicket.market),qty=Number(pos?.qty);return Number.isFinite(qty)&&qty>0?qty:0}function paperBuyQtyForCashPct(pct){const price=Number(paperTicketKrwPrice());if(!Number.isFinite(price)||price<=0||!paperData?.account)return"";const cash=Math.max(0,Number(paperData.account.cash)||0),feeRate=paperFeeRate(paperTicket.market).fee,maxBuffer=pct>=.999?paperTicket.market==="US"||paperTicket.market==="COIN"?.01:.005:0,max=cash*pct*(1-maxBuffer)/(price*(1+feeRate));return paperQtyValueText(paperTicket.market,max)}function paperSellQtyForHoldingPct(pct){const held=paperHeldQty();if(!held)return"";if(paperTicket.market==="COIN")return paperQtyValueText("COIN",held*pct);const qty=Math.floor(held*pct);return String(Math.min(held,Math.max(1,qty)))}function paperQtyForPct(pct){return paperTicket.side==="sell"?paperSellQtyForHoldingPct(pct):paperBuyQtyForCashPct(pct)}function paperTicketMarketClosed(){const t=paperTicket,policy=paperData?.policy||{};return t.market==="KR"?policy.krOpen===!1:t.market==="US"?policy.usOpen===!1:!1}function paperTicketClosedCopy(market){return market==="US"?"미국주식은 프리장부터 애프터장까지만 주문할 수 있어요. 지금은 주문 시간이 아니에요.":market==="KR"?"국내주식은 거래일 08:00~20:00에만 주문할 수 있어요. ETF는 정규장만 가능해요.":"선택한 시장은 지금 주문할 수 없어요."}function paperMarketHoursNoteHtml(variant){const policy=paperData?.policy||{},krClosed=policy.krOpen===!1,usClosed=policy.usOpen===!1,chip=(label,closed)=>`<span class="pp-mkt-item ${closed?"is-closed":"is-open"}"><b>${label}</b>${closed?"닫힘":"열림"}</span>`;return variant==="clock"?`<div class="pp-onboard-clock" role="note" aria-label="시장별 거래 가능 시간">
      <div class="pp-clock-h">⏰ 지금 거래</div>
      <div class="pp-clock-st">${chip("코인",!1)}${chip("국내",krClosed)}${chip("미국",usClosed)}</div>
      <div class="pp-clock-hours">국내 08:00~20:00 · 미국 프리~애프터 · 코인 24시간</div>
    </div>`:`<div class="pp-mkt-hours" role="note" aria-label="시장별 거래 가능 시간">
    <span class="pp-mkt-kicker">⏰ 지금 거래</span>
    ${chip("코인",!1)}${chip("국내",krClosed)}${chip("미국",usClosed)}
    <small class="pp-mkt-hint">국내 08:00~20:00 · 미국 프리~애프터 · 코인 24시간</small>
  </div>`}function paperTicketDisabledReason(){const t=paperTicket,hasQuote=t.code&&Number.isFinite(Number(t.price)),krwPrice=paperTicketKrwPrice(),qty=Number(t.qty),validQty=Number.isFinite(qty)&&qty>0;return paperBusy?{type:"busy",title:paperBusyText||"처리 중",detail:"잠시만 기다려 주세요."}:t.code?hasQuote?paperTicketMarketClosed()?{type:"closed",title:"거래시간 아님",detail:paperTicketClosedCopy(t.market)}:validQty?null:{type:"qty",title:"수량 입력 필요",detail:"주문할 수량을 입력하거나 빠른 수량 버튼을 눌러 주세요."}:{type:"quote",title:"시세 확인 필요",detail:"현재가를 먼저 확인해야 주문 금액을 계산할 수 있어요."}:{type:"symbol",title:"종목 선택 필요",detail:"종목을 검색하거나 자동완성 목록에서 선택해 주세요."}}const PAPER_FEE_FALLBACK=Object.freeze({KR:{fee:15e-5,sellTax:.002},US:{fee:.0025,sellTax:0},COIN:{fee:5e-4,sellTax:0}});function paperFeeRate(market){return(paperData?.policy?.fees||{})[market]||PAPER_FEE_FALLBACK[market]||PAPER_FEE_FALLBACK.KR}function paperPctText(rate){const pct=(Number(rate)||0)*100;return`${(Math.round(pct*1e3)/1e3).toLocaleString("ko-KR",{maximumFractionDigits:3})}%`}function paperFeeTooltip(market){const rate=paperFeeRate(market);return market==="COIN"?`수수료 ${paperPctText(rate.fee)} — 업비트 KRW 마켓 기준`:market==="US"?`수수료 ${paperPctText(rate.fee)} — 국내 증권사 해외주식 온라인 표준`:`매수·매도 수수료 ${paperPctText(rate.fee)} + 매도 시 증권거래세 ${paperPctText(rate.sellTax)} — 2026년 국내 표준 요율`}function paperTicketFootHtml(){const t=paperTicket,hasQuote=t.code&&Number.isFinite(Number(t.price)),krwPrice=paperTicketKrwPrice(),qty=Number(t.qty),validQty=Number.isFinite(qty)&&qty>0,feeRate=paperFeeRate(t.market),gross=krwPrice!==null&&validQty?krwPrice*qty:null,commission=gross!==null?gross*feeRate.fee:null,tax=gross!==null&&t.side==="sell"?gross*feeRate.sellTax:0,fee=gross!==null?commission+tax:null,estimate=gross!==null?t.side==="buy"?Math.ceil(gross+fee):Math.floor(gross-fee):null,disabledReason=paperTicketDisabledReason(),marketClosed=disabledReason?.type==="closed",disabled=!!disabledReason,buttonText=paperBusy?paperBusyText||"처리 중...":marketClosed?"거래시간 아님 · 주문 불가":estimate!==null?`${paperWon(estimate)} ${t.side==="buy"?"매수":"매도"}`:disabledReason?.title||`${t.side==="buy"?"매수":"매도"} 주문`;let idleText="종목과 수량을 입력하면 예상 체결 금액이 표시돼요";return disabledReason?idleText=disabledReason.detail:t.market==="US"&&krwPrice===null&&validQty&&(idleText="환율은 주문 순간 서버가 확인해 체결합니다."),`<div class="pp-ticket-foot">
      ${estimate!==null?`<div class="pp-ticket-summary" aria-live="polite">
        <div class="pp-ts-line"><span>주문금액</span><b>${paperWon(Math.round(gross))}</b></div>
        <div class="pp-ts-line pp-ts-fee"><span>수수료 <i>${paperPctText(feeRate.fee)}</i></span><b>${paperWon(Math.round(commission))}</b></div>
        ${t.side==="sell"&&feeRate.sellTax>0?`<div class="pp-ts-line pp-ts-fee"><span>증권거래세 <i>${paperPctText(feeRate.sellTax)}</i></span><b>${paperWon(Math.round(tax))}</b></div>`:""}
        ${t.market==="US"?`<div class="pp-ts-line pp-ts-fee"><span>환율 적용</span><b>${paperTicketFxText()}</b></div>`:""}
        <div class="pp-ts-line pp-ts-total"><span>${t.side==="buy"?"총 매수금액":"예상 수령액"}</span><b>${paperWon(estimate)}</b></div>
      </div>`:`<div class="pp-ticket-summary pp-ts-idle ${marketClosed?"pp-ts-blocked":""}" aria-live="polite"><b>${esc(disabledReason?.title||(t.market==="US"&&validQty?"서버 환율 확인":"주문 준비"))}</b><span>${esc(idleText)}</span></div>`}
      <button class="pp-btn pp-btn-primary pp-btn-${t.side} pp-order-submit" type="button" data-pp-act="submit-order" title="${esc(disabledReason?.detail||buttonText)}" ${disabled?"disabled":""}>${esc(buttonText)}</button>
    </div>`}function paperTicketFxText(){const fx=paperFx();return fx?`1$ = ${Math.round(fx).toLocaleString("ko-KR")}원`:"불러오는 중..."}function paperUpdateTicketFoot(){const foot=document.querySelector(".pp-ticket .pp-ticket-foot");foot&&(foot.outerHTML=paperTicketFootHtml())}function paperSessionLabel(state){const st=String(state||"").toUpperCase();return st==="PRE"?"프리장":st==="POST"?"애프터장":st==="REGULAR"?"정규장":st==="CLOSED"?"장 닫힘":""}function paperTicketHtml(){const t=paperTicket,hasQuote=t.code&&Number.isFinite(Number(t.price)),quote=t.code?paperQuoteMap[`${t.code}:${t.market}`]:null,livePrice=Number(quote?.price??t.price),krwPrice=paperTicketKrwPrice(),marketName=t.market==="COIN"?"코인 · 24시간":t.market==="US"?"해외주식":"국내주식",session=t.market==="COIN"?"":paperSessionLabel(t.session)||(paperTicketMarketClosed()?"장 닫힘":"거래 가능"),marketTag=[marketName,session].filter(Boolean).join(" · "),priceText=t.market==="US"?`$${livePrice.toLocaleString("en-US",{maximumFractionDigits:2})}${krwPrice?` ≈ ${Math.round(krwPrice).toLocaleString("ko-KR")}원`:""}`:`${livePrice.toLocaleString("ko-KR")}원`,cash=Number(paperData?.account?.cash)||0,held=paperHeldQty(),cashLine=t.side==="sell"&&t.code?`보유 <b>${paperQtyText(t.market,held)}${t.market==="COIN"?"":"주"}</b>`:`주문 가능 <b>${paperWon(cash)}</b>`;return`<div class="pp-card pp-ticket">
    <div class="pp-card-head"><span class="pp-card-title">주문하기</span>
      <button type="button" class="pp-fee-info" data-tip="${esc(paperFeeTooltip(t.market))}" aria-label="수수료 안내">수수료 안내</button>
      <span class="pp-ticket-cash">${cashLine}</span>
    </div>
    <div class="pp-side-seg" role="tablist" aria-label="매수/매도 선택">
      <button type="button" role="tab" aria-selected="${t.side==="buy"}" class="pp-side-btn buy ${t.side==="buy"?"on":""}" data-pp-act="side-buy" ${paperBusy?"disabled":""}>매수</button>
      <button type="button" role="tab" aria-selected="${t.side==="sell"}" class="pp-side-btn sell ${t.side==="sell"?"on":""}" data-pp-act="side-sell" ${paperBusy?"disabled":""}>매도</button>
    </div>
    ${paperMarketHoursNoteHtml()}
    <div class="pp-ticket-row">
      <input id="ppSymbolInput" type="text" placeholder="예: 삼성전자, 005930, TSLA, BTC" value="${esc(t.code||"")}" autocomplete="off" ${paperBusy?"disabled":""}>
      <button class="pp-btn" type="button" data-pp-act="lookup" ${paperBusy?"disabled":""}>${paperBusyText==="시세 확인 중..."?"확인 중...":"시세 확인"}</button>
    </div>
    <small class="pp-ticket-hint">목록에 없어도 종목명·코드로 검색할 수 있어요.</small>
    ${hasQuote?`<div class="pp-ticket-quote ${paperDirClass(quote?.changePct??t.changePct)}">
      <span class="pp-ticket-quote-id"><b>${esc(t.name||t.code)}</b><em>${esc(marketTag)}</em></span>
      <span class="pp-ticket-price"><strong>${esc(priceText)}</strong><em>${paperSignedPct(Number(quote?.changePct??t.changePct))}</em></span>
    </div>`:'<div class="pp-ticket-quote pp-muted">종목을 검색하면 현재가가 표시돼요. 국내주식·해외주식·코인을 지원해요.</div>'}
    <div class="pp-ticket-row pp-ticket-qty">
      <input id="ppQtyInput" type="number" min="0" step="${t.market==="COIN"?"0.000001":"1"}" placeholder="수량" value="${esc(String(t.qty||""))}" ${paperBusy?"disabled":""}>
      <button class="pp-btn" type="button" data-pp-act="qty-add" data-n="1" ${paperBusy?"disabled":""}>+1</button>
      <button class="pp-btn" type="button" data-pp-act="qty-add" data-n="10" ${paperBusy?"disabled":""}>+10</button>
      <button class="pp-btn" type="button" data-pp-act="qty-clear" ${paperBusy?"disabled":""}>초기화</button>
    </div>
    <div class="pp-ticket-quick" role="group" aria-label="주문 수량 빠른 선택">
      <span>${t.side==="sell"?"보유수량":"현금"} 기준</span>
      <button class="pp-btn" type="button" data-pp-act="qty-percent" data-pct="0.1" ${paperBusy?"disabled":""}>10%</button>
      <button class="pp-btn" type="button" data-pp-act="qty-percent" data-pct="0.25" ${paperBusy?"disabled":""}>25%</button>
      <button class="pp-btn" type="button" data-pp-act="qty-percent" data-pct="0.5" ${paperBusy?"disabled":""}>50%</button>
      <button class="pp-btn" type="button" data-pp-act="qty-percent" data-pct="1" ${paperBusy?"disabled":""}>최대</button>
    </div>
    ${paperTicketFootHtml()}
  </div>`}function paperOrderAdBannerHtml(){return'<div class="pp-notice" data-xk-area="papercard" hidden></div>'}function paperMarketName(market){const m=String(market||"").toUpperCase();return m==="COIN"?"코인":m==="US"?"해외":"국내"}function paperMarketBadgeHtml(market){const m=String(market||"").toUpperCase();return`<span class="pp-mk ${m==="COIN"?"pp-mk-coin":m==="US"?"pp-mk-us":"pp-mk-kr"}">${paperMarketName(m)}</span>`}function paperPositionsHtml(){const positions=paperData?.positions||[];if(!positions.length)return`<div class="pp-card"><div class="pp-card-head"><span class="pp-card-title">보유 종목</span></div>
      <div class="pp-empty">아직 보유 종목이 없어요. 위 주문 카드에서 첫 종목을 매수해 보세요.</div></div>`;const rows=positions.map(pos=>{const hasLiveQuote=paperPositionQuoteReady(pos),mark=paperPositionMark(pos),qty=Number(pos.qty)||0,cost=qty*(Number(pos.avg_price)||0),value=qty*mark,pnl=value-cost,pnlPct=cost>0?pnl/cost*100:0,dir=hasLiveQuote?paperDirClass(pnl):"flat",valueText=hasLiveQuote?paperWon(value):"시세 확인 중",pnlText=hasLiveQuote?`${paperWon(pnl,!0)} (${paperSignedPct(pnlPct)})`:"현재가 확인 중";return`<div class="pp-pos-row">
      <div class="pp-pos-id"><span class="pp-pos-name"><b>${esc(pos.name||pos.code)}</b>${paperMarketBadgeHtml(pos.market)}</span><em>${paperQtyText(pos.market,qty)}${pos.market==="COIN"?"":"주"} · 평단 ${Math.round(pos.avg_price).toLocaleString("ko-KR")}</em></div>
      <div class="pp-pos-val"><strong>${esc(valueText)}</strong><em class="${dir}">${esc(pnlText)}</em></div>
      <div class="pp-pos-act">
        <button class="pp-btn" type="button" data-pp-act="prefill" data-code="${esc(pos.code)}" data-market="${esc(pos.market)}" data-side="buy" ${paperBusy?"disabled":""}>매수</button>
        <button class="pp-btn" type="button" data-pp-act="prefill" data-code="${esc(pos.code)}" data-market="${esc(pos.market)}" data-side="sell" data-qty="${esc(String(qty))}" ${paperBusy?"disabled":""}>매도</button>
      </div>
    </div>`}).join("");return`<div class="pp-card"><div class="pp-card-head"><span class="pp-card-title">보유 종목</span><em>${positions.length}종목</em></div>${rows}</div>`}const PAPER_RANK_HOLDINGS_INLINE_LIMIT=5;function paperRankHoldingItem(item){const name=String(item?.name||item?.code||"").trim();if(!name)return null;const code=String(item?.code||"").trim(),market=String(item?.market||"").trim(),weightRaw=item?.weightPct,hasWeight=weightRaw!=null&&String(weightRaw).trim()!==""&&Number.isFinite(Number(weightRaw));return!!(code||market||hasWeight)?{...item&&typeof item=="object"?item:{},name,code,market}:null}function paperRankHoldingItems(row){return(Array.isArray(row?.holdings)?row.holdings:[]).map(paperRankHoldingItem).filter(Boolean)}function paperRankRecentNames(row,fallback=[]){const out=[],add=value=>{const name=String(value||"").trim();name&&!out.includes(name)&&out.push(name)};return(Array.isArray(row?.recent)?row.recent:[]).forEach(add),fallback.forEach(add),out.slice(0,3)}function paperRankMetaHtml(row){const badges=(row?.badges||[]).join("");return badges?`<span class="pp-lb-meta"><i class="pp-badges" title="🐣 첫 체결 · ⚡ 거래 50회 · 🚀 수익률 10% · 👑 1위 경험">${esc(badges)}</i></span>`:""}function paperRankOrdersHtml(row){return`<i class="pp-lb-orders">${paperOrderBreakdownText(row)}</i>`}function paperRankOrdersCount(row){return paperOrderBreakdown(row).total}function paperPortfolioPctText(value){const n=Number(value);return Number.isFinite(n)?`${(Math.round(n*10)/10).toLocaleString("ko-KR",{maximumFractionDigits:1})}%`:""}function paperOrderBreakdown(row){const total=Math.max(0,Math.floor(Number(row?.ordersCount??row?.orders_count)||0)),buy=Math.max(0,Math.floor(Number(row?.buyOrdersCount??row?.buy_orders_count)||0)),sell=Math.max(0,Math.floor(Number(row?.sellOrdersCount??row?.sell_orders_count)||0));return{total:Math.max(total,buy+sell),buy,sell,hasSplit:buy+sell>0||total===0}}function paperOrderBreakdownText(row){const stats=paperOrderBreakdown(row);return stats.hasSplit?`매수 ${stats.buy.toLocaleString("ko-KR")}회 · 매도 ${stats.sell.toLocaleString("ko-KR")}회`:`거래 ${stats.total.toLocaleString("ko-KR")}회`}function paperOrderBreakdownHtml(row){const stats=paperOrderBreakdown(row);return stats.hasSplit?`<span class="pp-trade-cell buy"><i>매수</i><b>${stats.buy.toLocaleString("ko-KR")}회</b></span><span class="pp-trade-cell sell"><i>매도</i><b>${stats.sell.toLocaleString("ko-KR")}회</b></span>`:`<span class="pp-trade-cell"><i>거래</i><b>${stats.total.toLocaleString("ko-KR")}회</b></span>`}function paperRankHoldingsHtml(row){const rawHoldings=Array.isArray(row?.holdings)?row.holdings:[],holdings=paperRankHoldingItems(row);if(!holdings.length){const legacyRecent=rawHoldings.filter(item=>!paperRankHoldingItem(item)).map(item=>String(item?.name||item?.code||item||"").trim()).filter(Boolean),recent=paperRankRecentNames(row,legacyRecent);return`<span class="pp-lb-sub"><span class="pp-lb-holdings is-cash"><i>현금 보유</i>${recent.length?`<em class="pp-lb-recent">최근 거래 ${esc(recent.join(" · "))}</em>`:""}</span>${paperRankOrdersHtml(row)}</span>`}return`<span class="pp-lb-sub"><span class="pp-lb-holdings"><i>보유</i>${holdings.slice(0,PAPER_RANK_HOLDINGS_INLINE_LIMIT).map(item=>{const name=String(item?.name||item?.code||"").trim();return name?`<button type="button" class="pp-lb-pill" data-pp-act="pill-quote" data-code="${esc(item?.code||"")}" data-market="${esc(item?.market||"")}" data-name="${esc(name)}" title="주문창에 ${esc(name)} 채우기"><span>${esc(name)}</span><em>주문</em></button>`:""}).join("")}</span>${paperRankOrdersHtml(row)}</span>`}function paperRankPortfolioPreviewHtml(row){const weights=paperRankHoldingItems(row).filter(item=>Number.isFinite(Number(item?.weightPct))&&Number(item.weightPct)>0).sort((a,b)=>Number(b.weightPct)-Number(a.weightPct));if(!weights.length){const recent=paperRankRecentNames(row),suffix=recent.length?` · 최근 ${recent.slice(0,2).join(" · ")}`:"";return`<span class="pp-lb-portfolio is-cash"><i>포트폴리오</i><em>현금 100%${esc(suffix)}</em></span>`}const sum=weights.reduce((total,item)=>total+(Number(item.weightPct)||0),0),cashPct=Math.max(0,Math.round((100-sum)*10)/10),fullParts=weights.map(item=>`${String(item.name||item.code)} ${paperPortfolioPctText(item.weightPct)}`);cashPct>.1&&fullParts.push(`현금 ${paperPortfolioPctText(cashPct)}`);const visibleParts=weights.slice(0,3).map(item=>`${String(item.name||item.code)} ${paperPortfolioPctText(item.weightPct)}`);return cashPct>.1&&visibleParts.push(`현금 ${paperPortfolioPctText(cashPct)}`),`<span class="pp-lb-portfolio" title="${esc(fullParts.join(" · "))}"><i>포트폴리오</i>${visibleParts.map(part=>`<em>${esc(part)}</em>`).join("")}</span>`}function paperRankMoneyHtml(row,pct,dir,options={}){const weekly=options.weekly?`<small class="pp-lb-week">주간 ${paperSignedPct(row.weeklyReturnPct,1)}</small>`:"";return`<span class="pp-lb-money"><em class="pp-lb-ret ${dir}">${paperSignedPct(pct)}${weekly}</em><span class="pp-lb-equity">${paperWon(row.equity)}</span></span>`}function paperRankMainHtml(row,isMe=!1){return`<span class="pp-lb-main"><span class="pp-lb-head"><b class="pp-lb-nick">${esc(row.nickname)}${isMe?" (나)":""}</b>${paperRankMetaHtml(row)}</span>${paperRankHoldingsHtml(row)}${paperRankPortfolioPreviewHtml(row)}</span>`}let paperExpandedRankKey="";function paperRankDetailHtml(row){if(!paperExpandedRankKey||paperRankRowKey(row)!==paperExpandedRankKey)return"";const weights=(row?.holdings||[]).filter(h=>Number.isFinite(Number(h?.weightPct)));if(!weights.length){const recent=(row?.recent||[]).slice(0,3).join(" · ");return`<div class="pp-lb-detail">현금 100%${recent?` · 최근 거래 ${esc(recent)}`:""}</div>`}const sum=weights.reduce((s,h)=>s+Number(h.weightPct),0),cashPct=Math.max(0,Math.round((100-sum)*10)/10),parts=weights.map(h=>`${String(h.name||h.code)} ${Number(h.weightPct)}%`);return cashPct>.1&&parts.push(`현금 ${cashPct}%`),`<div class="pp-lb-detail">포트폴리오 — ${esc(parts.join(" · "))}</div>`}function paperRankRowKey(row){return`${String(row?.nickname||"")}|${String(row?.createdAt||"")}`}function paperRankScopeLabel(scope=paperRankScope){return scope==="week"?"주간":scope==="all"?"누적":scope==="season"?"시즌":"일간"}function paperRankScopeRowsKey(scope=paperRankScope){return scope==="week"?"weeklyTop":scope==="all"?"allTop":scope==="season"?"seasonTop":"dailyTop"}function paperRankScopeReturnField(scope=paperRankScope){return scope==="week"?"weeklyReturnPct":scope==="all"?"allReturnPct":scope==="season"?"seasonReturnPct":"dailyReturnPct"}function paperSeasonScopeActive(){return Number(paperLeaderboard?.season?.no)>0}function paperRankReturnPct(row,scope=paperRankScope){const field=paperRankScopeReturnField(scope),scoped=Number(row?.[field]);return Number.isFinite(scoped)?scoped:Number(row?.returnPct)||0}function paperScopeRows(rows=[],scope=paperRankScope){const list=Array.isArray(rows)?rows:[],field=paperRankScopeReturnField(scope);return list.map(row=>{const pct=Number(row?.[field]),scopedPct=Number.isFinite(pct)?pct:Number(row?.returnPct)||0;return{...row,allRank:Number(row?.allRank??row?.rank)||null,allReturnPct:Number(row?.allReturnPct??row?.returnPct)||0,rankScope:scope,scopeReturnPct:scopedPct,returnPct:scopedPct}}).sort((a,b)=>(Number(b.scopeReturnPct)||0)-(Number(a.scopeReturnPct)||0)||(Number(b.equity)||0)-(Number(a.equity)||0)||String(a.createdAt||"").localeCompare(String(b.createdAt||""))).map((row,index)=>({...row,rank:index+1}))}function paperLeaderboardRowsForScope(lb=paperLeaderboard,scope=paperRankScope){const key=paperRankScopeRowsKey(scope),rows=Array.isArray(lb?.[key])&&lb[key].length?lb[key]:null;return rows||(scope==="all"&&Array.isArray(lb?.top)?lb.top:Array.isArray(lb?.participants)&&lb.participants.length?paperScopeRows(lb.participants,scope):Array.isArray(lb?.top)?lb.top:[])}function paperLeaderboardMeForScope(lb=paperLeaderboard,scope=paperRankScope){const direct=lb?.[scope==="week"?"meWeekly":scope==="all"?"meAll":scope==="season"?"meSeason":"meDaily"]||(scope==="all"?lb?.me:null);return direct?.rank?direct:Array.isArray(lb?.participants)&&lb.participants.length&&paperScopeRows(lb.participants,scope).find(row=>paperRankRowIsMe(row,lb))||null}function paperRankRowIsMe(row,lb=paperLeaderboard){const me=lb?.me||{};if(!me?.nickname||!row?.nickname||row.nickname!==me.nickname)return!1;const meRank=Number(me.rank),rowAllRank=Number(row.allRank??row.rank);return Number.isFinite(meRank)&&Number.isFinite(rowAllRank)&&meRank===rowAllRank?!0:!!me.createdAt&&!!row.createdAt&&String(me.createdAt)===String(row.createdAt)}function paperRankComparableRows(lb){const rows=Array.isArray(lb?.participants)&&lb.participants.length?lb.participants:[...lb?.dailyTop||[],...lb?.weeklyTop||[],...lb?.allTop||[],...lb?.top||[]],seen=new Set,out=[];for(const row of rows){const key=paperRankRowKey(row);!key.trim()||seen.has(key)||(seen.add(key),out.push(row))}return lb?.me?.rank&&out.push(lb.me),out.filter(row=>paperRankRowKey(row).trim()!=="|")}function paperPrepareRankMotion(prev,next){if(paperRankMotion=new Map,paperRankMotionUntil=0,!prev?.updatedAt||!next?.updatedAt||prev.updatedAt===next.updatedAt)return;const before=new Map;for(const row of paperRankComparableRows(prev))before.set(paperRankRowKey(row),{rank:Number(row.rank),equity:Math.round(Number(row.equity)||0),returnPct:Math.round((Number(row.returnPct)||0)*100)/100});for(const row of paperRankComparableRows(next)){const key=paperRankRowKey(row),old=before.get(key);if(!old)continue;const rank=Number(row.rank),equity=Math.round(Number(row.equity)||0),returnPct=Math.round((Number(row.returnPct)||0)*100)/100;Number.isFinite(rank)&&Number.isFinite(old.rank)&&rank<old.rank?paperRankMotion.set(key,"pp-rank-up"):Number.isFinite(rank)&&Number.isFinite(old.rank)&&rank>old.rank?paperRankMotion.set(key,"pp-rank-down"):(equity!==old.equity||returnPct!==old.returnPct)&&paperRankMotion.set(key,"pp-rank-change")}paperRankMotion.size&&(paperRankMotionUntil=Date.now()+2400)}function paperRankMotionClass(row){return!paperRankMotion.size||Date.now()>paperRankMotionUntil?(paperRankMotion=new Map,paperRankMotionUntil=0,""):paperRankMotion.get(paperRankRowKey(row))||""}function paperParticipantRows(lb=paperLeaderboard){const rows=Array.isArray(lb?.participants)&&lb.participants.length?paperScopeRows(lb.participants,paperRankScope):paperLeaderboardRowsForScope(lb,paperRankScope),cfg=PAPER_RANK_WINDOWS.find(item=>item.key===paperRankWindow)||PAPER_RANK_WINDOWS[PAPER_RANK_WINDOWS.length-1],cutoff=cfg.days?Date.now()-cfg.days*24*60*60*1e3:0,byRank=(a,b)=>(Number(a.rank)||999999)-(Number(b.rank)||999999);return rows.filter(row=>{if(!cutoff)return!0;const activeAt=Date.parse(row.lastActiveAt||row.createdAt||"");return Number.isFinite(activeAt)&&activeAt>=cutoff}).sort((a,b)=>{if(paperParticipantSort==="orders"){const diff=paperRankOrdersCount(b)-paperRankOrdersCount(a);if(diff)return diff}return byRank(a,b)})}function paperParticipantFiltersHtml(){const sortOptions=[{key:"rank",label:"수익률순"},{key:"orders",label:"거래횟수순"}];return`<div class="pp-lb-filters" role="group" aria-label="참가자 최근 활동 기준">
    <span>최근 활동</span>
    ${PAPER_RANK_WINDOWS.map(item=>`<button class="pp-btn ${paperRankWindow===item.key?"on":""}" type="button" data-pp-act="rank-window" data-window="${esc(item.key)}">${esc(item.label)}</button>`).join("")}
    <span>정렬</span>
    ${sortOptions.map(item=>`<button class="pp-btn ${paperParticipantSort===item.key?"on":""}" type="button" data-pp-act="participant-sort" data-sort="${esc(item.key)}">${esc(item.label)}</button>`).join("")}
  </div>`}function paperParticipantListHtml(){return paperShowParticipants?paperParticipantsInlineHtml():`<div class="pp-participants pp-participants-collapsed">
      <button class="pp-btn pp-btn-ghost" type="button" data-pp-act="toggle-participants">전체 참가자 목록 보기 ›</button>
    </div>`}function paperParticipantSortLabel(){return paperParticipantSort==="orders"?"거래횟수순":"수익률순"}function paperParticipantRankLabel(row,index){if(paperParticipantSort==="orders")return`${index+1}위`;const rank=Number(row?.rank);return Number.isFinite(rank)&&rank>0?`${rank}위`:"-"}function paperParticipantsInlineHtml(lb=paperLeaderboard,opts={}){const inModal=!!opts.modal,rows=paperParticipantRows(lb),hasFullList=Array.isArray(lb?.participants),hasMore=!!lb?.participantsHasMore,headText=hasFullList?`${rows.length}명 표시${Number(lb?.participantsTotal)>rows.length?` / ${Number(lb.participantsTotal).toLocaleString("ko-KR")}명`:""} · ${paperParticipantSortLabel()}`:`상위 ${rows.length}명 먼저 표시 · ${paperParticipantsError||"불러오는 중"}`,body=rows.length?rows.map((row,index)=>{const dir=paperDirClass(row.returnPct),isMe=paperRankRowIsMe(row,lb),rankTitle=paperParticipantSort==="orders"?`거래횟수순 ${index+1}위 · ${paperRankScopeLabel()} 수익률 ${Number(row.rank)||"-"}위`:`${paperRankScopeLabel()} 수익률 ${Number(row.rank)||"-"}위`;return`<div class="pp-lb-row pp-participant-row ${isMe?"is-me":""} ${paperRankMotionClass(row)}" data-pp-act="rank-expand" data-rankkey="${esc(paperRankRowKey(row))}" role="button" tabindex="0">
      <span class="pp-lb-rank" title="${esc(rankTitle)}">${paperParticipantRankLabel(row,index)}</span>
      ${paperRankMainHtml(row,isMe)}
      ${paperRankMoneyHtml(row,row.returnPct,dir,{weekly:paperRankScope!=="week"})}
    </div>${paperRankDetailHtml(row)}`}).join(""):'<div class="pp-empty">이 기간에 활동한 참가자가 아직 없어요.</div>',more=hasMore?`<button class="pp-btn pp-btn-ghost pp-participant-more" type="button" data-pp-act="participants-more" ${paperParticipantsLoadingMore?"disabled":""}>${paperParticipantsLoadingMore?"불러오는 중...":"더 보기"}</button>`:"";return`<div class="pp-participants pp-participants-expanded">
    ${inModal?`<div class="pp-participants-head"><em>${esc(headText)}</em></div>`:`<div class="pp-participants-head"><span class="pp-card-title">참가자 전체 목록</span><em>${esc(headText)} · <button class="pp-link-btn" type="button" data-pp-act="toggle-participants">접기</button></em></div>`}
    ${paperParticipantFiltersHtml()}
    <div class="pp-participant-list">${body}</div>
    ${more}
  </div>`}function paperRankAllTriggerHtml(lb=paperLeaderboard){const total=Number(lb?.total)||0;return`<button class="pp-btn pp-btn-ghost pp-rank-all-btn" type="button" data-pp-act="rank-modal-open">${total?`참가자 ${total.toLocaleString("ko-KR")}명 전체`:"전체 참가자"} · 정렬·포트폴리오 보기 ›</button>`}function paperRankModalHtml(){if(!paperRankModalOpen)return"";const lb=paperLeaderboard,total=Number(lb?.total)||0,body=Array.isArray(lb?.participants)?paperParticipantsInlineHtml(lb,{modal:!0}):`<div class="pp-rank-modal-loading">${paperParticipantsError?esc(paperParticipantsError):'<span class="news-loading-spin"></span>전체 참가자 목록을 불러오는 중...'}</div>`;return`<div class="pp-modal-backdrop pp-rank-modal-backdrop" data-pp-act="rank-modal-close">
    <div class="pp-modal pp-rank-modal" role="dialog" aria-modal="true" aria-label="전체 참가자 랭킹" data-pp-stop>
      <div class="pp-modal-head">
        <strong>전체 참가자 랭킹</strong>${total?`<em>${total.toLocaleString("ko-KR")}명</em>`:""}
        <button class="pp-modal-x" type="button" data-pp-act="rank-modal-close" aria-label="닫기">✕</button>
      </div>
      <div class="pp-rank-modal-body">${body}</div>
    </div>
  </div>`}function paperParticipantListScrollState(){const list=document.querySelector(".pp-participant-list");return list?{top:list.scrollTop||0,left:list.scrollLeft||0}:null}function paperRestoreParticipantListScroll(state){if(!state)return;const list=document.querySelector(".pp-participant-list");list&&(list.scrollTop=state.top||0,list.scrollLeft=state.left||0)}function renderPaperTabKeepParticipantScroll(){const scrollState=paperParticipantListScrollState();renderPaperTab(),paperRestoreParticipantListScroll(scrollState)}function paperRecentTradeRows(limit=50){return(paperLeaderboard?.stats?.recentTrades||[]).slice(0,Math.max(0,Math.min(50,Number(limit)||50)))}function paperLeagueTradeStats(){const trades=paperRecentTradeRows(),nickMap=new Map,symbolMap=new Map;for(const trade of trades){const nickname=String(trade?.nickname||"참가자").trim()||"참가자",nick=nickMap.get(nickname)||{nickname,count:0};nick.count+=1,nickMap.set(nickname,nick);const name=String(trade?.name||trade?.code||"").trim();if(!name)continue;const market=String(trade?.market||"").toUpperCase(),key=`${name}|${market}`,symbol=symbolMap.get(key)||{name,market,count:0};symbol.count+=1,symbolMap.set(key,symbol)}const byCount=(a,b)=>b.count-a.count||String(a.nickname||a.name||"").localeCompare(String(b.nickname||b.name||""),"ko");return{trades,tradeKings:[...nickMap.values()].sort(byCount),hotSymbols:[...symbolMap.values()].sort(byCount)}}function paperSpotlightItemHtml(kind,kicker,title,meta,body=""){return`<div class="pp-spotlight-item pp-spotlight-${esc(kind)}">
    <span class="pp-spot-kicker">${esc(kicker)}</span>
    <b class="pp-spot-title">${esc(title)}</b>
    <em class="pp-spot-meta">${esc(meta)}</em>
    ${body}
  </div>`}function paperTradeKingSpotlightHtml(stats){const king=stats.tradeKings[0];return king?paperSpotlightItemHtml("trade-king","🏆 거래왕",king.nickname,`최근 체결 ${king.count.toLocaleString("ko-KR")}건`):paperSpotlightItemHtml("quiet","🏆 거래왕","집계 대기","최근 체결이 쌓이면 표시돼요")}function paperRankRiserSpotlightHtml(){let row=(paperLeaderboard?.stats?.rankRisers||[]).filter(row2=>Number(row2?.dailyRankRise)>0||Number(row2?.weeklyRankRise)>0)[0]||null,meta="";if(row){const daily=Number(row.dailyRankRise)||0,weekly=Number(row.weeklyRankRise)||0;meta=daily>0?`오늘 ${daily.toLocaleString("ko-KR")}계단 상승 · ${Number(row.rank)||"-"}위`:`주간 ${weekly.toLocaleString("ko-KR")}계단 상승 · ${Number(row.rank)||"-"}위`}else row=paperParticipantRows(paperLeaderboard).filter(item=>Number(item?.weeklyReturnPct)>0).sort((a,b)=>(Number(b.weeklyReturnPct)||0)-(Number(a.weeklyReturnPct)||0))[0]||null,row&&(meta=`주간 ${paperSignedPct(row.weeklyReturnPct,1)} · ${Number(row.rank)||"-"}위`);return row?paperSpotlightItemHtml("riser","🚀 역전왕",row.nickname||"참가자",meta):paperSpotlightItemHtml("quiet","🚀 역전왕","순위 변동 대기","일·주 시작 순위와 비교해요")}function paperHotSymbolSpotlightHtml(stats){const v2=(paperLeaderboard?.stats?.hotSymbolsV2||[]).slice(0,3);if(v2.length){const top2=v2[0],market2=paperTradeMarketLabel(top2.market),tags2=`<span class="pp-spot-tags">${v2.map(item=>`<i>${esc(item.name)} <b>+${paperWonShort(item.net)}</b></i>`).join("")}</span>`;return paperSpotlightItemHtml("hot-symbol","사람들이 오늘 많이 담은 종목",top2.name,`${market2?`${market2} · `:""}순매수 +${paperWonShort(top2.net)}`,tags2)}const popular=(paperLeaderboard?.stats?.popular||[]).slice(0,3);if(popular.length){const top2=popular[0],tags2=`<span class="pp-spot-tags">${popular.map(item=>`<i>${esc(item.name)} ${(Number(item.holders)||0).toLocaleString("ko-KR")}명</i>`).join("")}</span>`;return paperSpotlightItemHtml("hot-symbol","인기 보유 종목",top2.name,`${(Number(top2.holders)||0).toLocaleString("ko-KR")}명 보유`,tags2)}const symbols=(stats?.hotSymbols||[]).slice(0,3);if(!symbols.length)return paperSpotlightItemHtml("quiet","사람들이 오늘 많이 담은 종목","집계 대기","오늘 거래가 쌓이면 인기 종목이 떠요");const top=symbols[0],market=paperTradeMarketLabel(top.market),tags=`<span class="pp-spot-tags">${symbols.map(item=>`<i>${esc(item.name)} ${item.count.toLocaleString("ko-KR")}</i>`).join("")}</span>`;return paperSpotlightItemHtml("hot-symbol","사람들이 오늘 많이 담은 종목",top.name,`${market?`${market} · `:""}최근 ${top.count.toLocaleString("ko-KR")}건`,tags)}function paperWeeklyMissionItems(){const wm=paperData?.account?.weekMissions;if(!paperData?.account||!wm)return[{key:"trades",label:"이번 주 거래 5회",cur:0,target:5,done:!1,value:"0/5"},{key:"markets",label:"3개 시장 거래",cur:0,target:3,done:!1,value:"0/3"},{key:"wins",label:"익절 1회",cur:0,target:1,done:!1,value:"대기"}];const orders=Math.max(0,Number(wm.orders)||0),mask=Math.max(0,Number(wm.markets)||0),marketCount=(mask&1?1:0)+(mask&2?1:0)+(mask&4?1:0),wins=Math.max(0,Number(wm.wins)||0);return[{key:"trades",label:"이번 주 거래 5회",cur:Math.min(orders,5),target:5,done:orders>=5,value:`${Math.min(orders,5)}/5`},{key:"markets",label:"3개 시장 거래",cur:marketCount,target:3,done:marketCount>=3,value:`${marketCount}/3`},{key:"wins",label:"익절 1회",cur:Math.min(wins,1),target:1,done:wins>=1,value:wins>=1?"완료":"대기"}]}function paperMissionCountdownText(resetAt){const end=Date.parse(String(resetAt||""));if(!Number.isFinite(end))return"이번 주 목표";const ms=end-Date.now();if(ms<=0)return"곧 초기화";const hours=Math.floor(ms/36e5);return hours>=24?`${Math.floor(hours/24)}일 남음`:hours>=1?`${hours}시간 남음`:`${Math.max(1,Math.round(ms/6e4))}분 남음`}function paperWeeklyMissionSpotlightHtml(){const missions=paperWeeklyMissionItems(),hasAccount=!!paperData?.account,done=missions.filter(item=>item.done).length,allDone=hasAccount&&done===missions.length,reward=paperData?.policy?.drawTicketsEvent?"뽑기권 2매":"",title=hasAccount?allDone?`이번 주 미션 완료! 🎉${reward?` ${reward} 지급`:""}`:`${done}/${missions.length} 완료`:"계좌 만들면 시작",countdown=hasAccount?paperMissionCountdownText(paperData?.account?.weekMissions?.resetAt):"이번 주 가벼운 목표",meta=!allDone&&reward?`완료 시 ${reward}${countdown?` · ${countdown}`:""}`:countdown,list=`<span class="pp-mission-list">${missions.map(item=>{const pct=item.target>0?Math.min(100,Math.round(item.cur/item.target*100)):item.done?100:0;return`<i class="pp-mission-row ${item.done?"done":""}"><b>${esc(item.label)}</b><span class="pp-mission-bar"><span style="width:${pct}%"></span></span><em>${esc(item.done?"완료":item.value)}</em></i>`}).join("")}</span>`;return paperSpotlightItemHtml("mission","주간 미션",title,meta,list)}function paperPopularHoldingSpotlightHtml(){const popular=(paperLeaderboard?.stats?.popular||[]).slice(0,3);if(!popular.length)return"";const top=popular[0],tags=`<span class="pp-spot-tags">${popular.map(item=>`<i>${esc(item.name)} <b>${(Number(item.holders)||0).toLocaleString("ko-KR")}명</b></i>`).join("")}</span>`;return paperSpotlightItemHtml("popular-hold","🔥 인기 보유 종목",top.name,`${(Number(top.holders)||0).toLocaleString("ko-KR")}명이 보유 중`,tags)}function paperTodayWinnerSpotlightHtml(){const rows=paperLeaderboardRowsForScope(paperLeaderboard,"day"),top=Array.isArray(rows)?rows[0]:null;return!top||!Number.isFinite(Number(top.returnPct))?"":paperSpotlightItemHtml("today-1st","🥇 오늘의 1위",top.nickname||"참가자",`오늘 수익률 ${paperSignedPct(Number(top.returnPct),1)}`)}function paperBigWinLossSpotlightHtml(){const g=paperLeaderboard?.stats?.topGain,l=paperLeaderboard?.stats?.topLoss;if(!g&&!l)return"";const gMag=g?Math.abs(Number(g.realized)||0):-1,lMag=l?Math.abs(Number(l.realized)||0):-1,showGain=gMag>=lMag,e=showGain?g:l;if(!e)return"";const amt=`${showGain?"+":"-"}${paperWonShort(Math.abs(Number(e.realized)||0))}`,body=`<span class="pp-spot-amt ${showGain?"num-pos":"num-neg"}">${esc(amt)} 실현</span>`;return paperSpotlightItemHtml("big-winloss",showGain?"💥 오늘의 대박":"😮‍💨 오늘의 한숨",e.nickname||"참가자",e.name||"",body)}function paperShareKingSpotlightHtml(){const top=(Array.isArray(paperLeaderboard?.sharingKing?.top)?paperLeaderboard.sharingKing.top:[])[0];return!top||!(Number(top.count)>0)?paperSpotlightItemHtml("quiet","🎁 공유왕","집계 대기","친구를 초대하면 공유왕에 올라요"):paperSpotlightItemHtml("share-king","🎁 공유왕",top.nickname||"참가자",`친구 ${(Number(top.count)||0).toLocaleString("ko-KR")}명 초대`)}function paperLeagueSpotlightHtml(){const stats=paperLeagueTradeStats(),items=[paperTradeKingSpotlightHtml(stats),paperBigWinLossSpotlightHtml(),paperShareKingSpotlightHtml(),paperRankRiserSpotlightHtml()].filter(Boolean).slice(0,3);return items.length?`<div class="pp-spotlight">${items.join("")}</div>`:""}function paperWeeklyMissionCardHtml(){if(!paperData?.account)return"";const missions=paperWeeklyMissionItems();if(!missions.length)return"";const done=missions.filter(m=>m.done).length,allDone=done===missions.length,reward=paperData?.policy?.drawTicketsEvent?"뽑기권 2매":"",countdown=paperMissionCountdownText(paperData?.account?.weekMissions?.resetAt),note=allDone?`완료! 🎉${reward?` ${reward} 지급`:""}`:reward?`다 모으면 ${reward}${countdown?` · ${countdown}`:""}`:countdown,list=missions.map(item=>{const pct=item.target>0?Math.min(100,Math.round(item.cur/item.target*100)):item.done?100:0;return`<i class="pp-mission-row ${item.done?"done":""}"><b>${esc(item.label)}</b><span class="pp-mission-bar"><span style="width:${pct}%"></span></span><em>${esc(item.done?"완료":item.value)}</em></i>`}).join("");return`<div class="pp-card pp-mission-card pp-mission-compact">
    <div class="pp-card-head"><span class="pp-card-title">이번 주 목표</span><em>${done}/${missions.length} 달성${note?` · ${note}`:""}</em></div>
    <span class="pp-mission-list">${list}</span>
  </div>`}function paperRecentBadgeFeedHtml(compact=!1){const feed=(paperLeaderboard?.badgeFeed||[]).slice(0,compact?4:6);return feed.length?`<div class="pp-card pp-badge-feed">
    <div class="pp-card-head"><span class="pp-card-title">최근 뱃지 소식</span><em>리더보드 캐시 기준</em></div>
    <div class="pp-badge-feed-list">${feed.map(item=>{const time=item?.at&&typeof relativeTimeKR=="function"?relativeTimeKR(item.at):"",emoji=String(item?.badgeEmoji||"").trim(),badge=String(item?.badgeName||"뱃지").trim(),reason=String(item?.reason||"조건을 달성해서").trim(),badgeId=String(item?.badgeId||"").trim();return`<button type="button" class="pp-badge-feed-row" data-pp-act="badge-feed-open" data-badge="${esc(badgeId)}" title="${esc(badge)} 조건 보기">
      <span class="pp-badge-feed-emoji" aria-hidden="true">${esc(emoji||"★")}</span>
      <span class="pp-badge-feed-copy"><b>${esc(item?.nickname||"월급루팡")}님</b>이 ${esc(reason)} <strong>${esc(badge)}</strong> 뱃지를 획득하셨어요.</span>
      ${time?`<em>${esc(time)}</em>`:""}
    </button>`}).join("")}</div>
  </div>`:""}function paperInviteCardHtml(){if(!paperLeaderboard)return"";const hasAccount=!!paperData?.account,ticketsMode=!!paperData?.policy?.drawTicketsEvent,tickets=Math.max(0,Number(paperData?.account?.drawTickets)||0),myCount=Number(paperLeaderboard?.me?.referralCount)||0,top=Array.isArray(paperLeaderboard.sharingKing?.top)?paperLeaderboard.sharingKing.top:[],leadCount=top.length&&Number(top[0].count)||0,drawBusy=paperBusy||paperDrawInFlight;let tix="";ticketsMode&&(tix=hasAccount?`<div class="pp-ic-tix">
        <span class="pp-ic-em" aria-hidden="true">🎟️</span>
        <div class="pp-ic-n"><b>${tickets}장</b><span class="pp-ic-k">내 주식 뽑기권</span></div>
        <button type="button" class="pp-ic-draw" data-pp-act="draw-ticket" ${drawBusy||tickets<=0?"disabled":""} title="${tickets<=0?"친구가 내 링크로 시작하면 뽑기권이 쌓여요":"뽑기권 1장으로 랜덤주식 1주"}">${paperDrawInFlight?"뽑는 중…":"뽑기 ▶"}</button>
      </div>`:`<div class="pp-ic-tix pp-ic-tix-guest">
        <span class="pp-ic-em" aria-hidden="true">🎟️</span>
        <div class="pp-ic-n"><b>주식 뽑기권</b><span class="pp-ic-k">가입하면 받을 수 있어요</span></div>
      </div>`);const reward=ticketsMode?"<b>주식 뽑기권 +1장</b>":"<b>내 시드 +100만원</b>",midText=hasAccount?`친구가 내 링크로 시작하면 ${reward}`:`계좌를 만들고 친구를 초대하면 ${reward}`,midSub=ticketsMode?"주 20장까지 · 누적 3·10·30명마다 공유 메달 뱃지 🥉🥈🥇":"주 5명까지 · 누적 3·10·30명마다 공유 메달 뱃지 🥉🥈🥇",rankBlock=hasAccount?`<div class="pp-ic-rank">
      <div class="pp-ic-rank-h">이번 주 공유 순위</div>
      <div class="pp-ic-rank-me">내 초대 <b>${myCount}명</b>${myCount>0&&myCount<leadCount?` · 1위까지 ${leadCount-myCount+1}명`:myCount>0?" · 선두권!":""}</div>
    </div>`:"",shareBtn=hasAccount?`<button type="button" class="pp-ic-share" data-pp-act="share-contest" ${paperBusy||paperShareInFlight?"disabled":""}><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><circle cx="18" cy="5" r="3"/><circle cx="6" cy="12" r="3"/><circle cx="18" cy="19" r="3"/><path d="M8.6 13.5 15.4 17.5M15.4 6.5 8.6 10.5"/></svg>${paperShareInFlight?"링크 만드는 중…":"초대 링크 공유"}</button>`:`<button type="button" class="pp-ic-share pp-ic-join" data-pp-act="signup-start" ${paperBusy?"disabled":""}>${ticketsMode?"가입하고 뽑기권 받기":"가입하고 시작하기"}</button>`;return`${ticketsMode&&hasAccount&&paperDrawResult?paperDrawRevealHtml(paperDrawResult):""}<div class="pp-invite-card">
    ${tix}
    <div class="pp-ic-mid"><div class="pp-ic-t">${midText}</div><div class="pp-ic-d">${midSub}</div></div>
    ${shareBtn}
    ${rankBlock}
  </div>`}function paperSharingKingHtml(){if(!paperLeaderboard)return"";const top=Array.isArray(paperLeaderboard.sharingKing?.top)?paperLeaderboard.sharingKing.top:[];if(!top.length)return"";const medals=["🥇","🥈","🥉"],myNick=String(paperData?.account?.nickname||"").trim(),isMeRow=r=>!!r.me||myNick&&String(r.nickname||"").trim()===myNick,rankOf=(r,i)=>Number(r.rank)>0?Number(r.rank):i+1,rowHtml=(r,rk,isMe,sep)=>`<div class="pp-sk-row${isMe?" is-me":""}${sep?" pp-sk-row-sep":""}">
      <span class="pp-sk-rank">${rk&&rk<=3?medals[rk-1]:rk?`${rk}위`:"나"}</span>
      <span class="pp-sk-nick">${esc(r.nickname||"월급루팡")}${isMe?' <i class="pp-sk-me">나</i>':""}</span>
      <span class="pp-sk-count">${Number(r.count)||0}명</span>
    </div>`,top4=top.slice(0,4);let rows=top4.map((r,i)=>rowHtml(r,rankOf(r,i),isMeRow(r),!1)).join("");if(myNick&&!top4.some(isMeRow)){const myIdx=top.findIndex(isMeRow);if(myIdx>=0)rows+=rowHtml(top[myIdx],rankOf(top[myIdx],myIdx),!0,!0);else{const myCount=Math.max(0,Number(paperLeaderboard?.me?.referralCount)||0);rows+=rowHtml({nickname:myNick,count:myCount},null,!0,!0)}}return`<div class="pp-card pp-sharing-king">
    <div class="pp-card-head"><span class="pp-card-title">링크 공유 순위</span><em>초대 누적</em></div>
    <div class="pp-sk-board">${rows}</div>
  </div>`}function paperDrawBlockHtml(tickets){const busy=paperBusy||paperDrawInFlight;return tickets>0?`<div class="pp-draw">
      <span class="pp-draw-count">🎟️ 랜덤주식 1주 뽑기권 <b>${tickets}장</b></span>
      <button type="button" class="pp-btn pp-btn-primary pp-draw-btn" data-pp-act="draw-ticket" ${busy?"disabled":""}>${paperDrawInFlight?"뽑는 중…":"1주 뽑기권 열기"}</button>
    </div>`:'<div class="pp-draw pp-draw-empty"><span class="pp-draw-count">🎟️ 랜덤주식 1주 뽑기권 <b>0장</b></span><em>친구가 내 링크로 시작할 때마다 랜덤주식 1주 뽑기권이 쌓여요</em></div>'}function paperDrawRevealHtml(prize){if(!prize)return"";if(prize.type==="cash")return`<div class="pp-draw-reveal pp-draw-reveal-cash" role="status">
      <div class="pp-draw-reveal-head">🎁 랜덤주식 1주 뽑기 보너스 <b>${Math.round((Number(prize.amount)||0)/1e4)}만원</b> 지급!</div>
      <button type="button" class="pp-btn pp-btn-sm" data-pp-act="draw-dismiss">확인</button>
    </div>`;const name=esc(prize.name||prize.code||"종목"),price=paperWon(Number(prize.price)||0),g=paperDrawGradeMeta(prize.grade);return`<div class="pp-draw-reveal" role="status">
    ${g?`<div class="pp-draw-grade-badge ${g.cls}">${g.emoji?`${g.emoji} `:""}${esc(g.label)} 당첨!</div>`:""}
    <div class="pp-draw-reveal-head">🎉 <b>${name}</b> 1주 ${g?"획득":"당첨"}!</div>
    <div class="pp-draw-reveal-sub">현재가 ${price} · 내 계좌 보유종목에 담겼어요</div>
    <p class="pp-draw-reveal-note">받은 1주는 실제로 내 모의투자 계좌의 보유 종목이에요. 오르면 내 수익률↑, 내리면↓. 바로 팔아 현금화도 가능해요.</p>
    <button type="button" class="pp-btn pp-btn-sm" data-pp-act="draw-dismiss">확인</button>
  </div>`}async function paperDrawTicket(){if(!(paperBusy||paperDrawInFlight)){paperDrawInFlight=!0,paperDrawResult=null,paperTabActive()&&renderPaperTab();try{const data=await paperPost("draw-ticket");data?.ok&&data.prize?(paperDrawResult=data.prize,await loadPaperTab({silent:!0}).catch(()=>{})):showToast?.(data?.error||"랜덤주식 1주 뽑기에 실패했어요","err")}catch(e){showToast?.(e?.message||"랜덤주식 1주 뽑기에 실패했어요. 잠시 후 다시 시도해 주세요.","err")}finally{paperDrawInFlight=!1,paperTabActive()&&renderPaperTab()}}}function paperHotSymbolCardHtml(){const net=(paperLeaderboard?.stats?.hotSymbolsV2||[]).slice(0,5),hold=(paperLeaderboard?.stats?.popular||[]).slice(0,5);if(!net.length&&!hold.length)return"";let mode=paperHotMode==="hold"?"hold":"net";paperHotModeUserSet||(mode==="net"&&!net.length&&hold.length&&(mode="hold"),mode==="hold"&&!hold.length&&net.length&&(mode="net"));const rows=mode==="hold"?hold:net,title=mode==="hold"?"인기 보유 종목":"오늘 많이 담은 종목",toggle=`<span class="pp-hot-toggle" role="group" aria-label="정렬 기준">
    <button class="pp-hot-tab${mode==="net"?" on":""}" type="button" data-pp-act="hot-mode" data-mode="net" ${paperBusy?"disabled":""}>순매수</button>
    <button class="pp-hot-tab${mode==="hold"?" on":""}" type="button" data-pp-act="hot-mode" data-mode="hold" ${paperBusy?"disabled":""}>인기 보유</button>
  </span>`;let body;return rows.length?body=`<div class="pp-hot-list">${rows.map((item,i)=>{const holders=Number(item.holders)||0,valMain=mode==="hold"?`${holders.toLocaleString("ko-KR")}명`:`+${paperWonShort(item.net)}`,valSub=mode==="hold"?"":holders?`${holders.toLocaleString("ko-KR")}명 보유`:"";return`<div class="pp-hot-row">
        <span class="pp-hot-rank">${i+1}</span>
        <div class="pp-hot-id"><b>${esc(item.name||item.code||"종목")}</b>${paperMarketBadgeHtml(item.market)}</div>
        <div class="pp-hot-val"><b>${esc(valMain)}</b>${valSub?`<em>${esc(valSub)}</em>`:""}</div>
      </div>`}).join("")}</div>`:body=`<div class="pp-hot-empty">${mode==="net"?"오늘 순매수는 집계 전이에요 — 장중 거래가 쌓이면 떠요":"인기 보유 집계 전이에요"}</div>`,`<div class="pp-card pp-hot-card">
    <div class="pp-card-head"><span class="pp-card-title">${title}</span>${toggle}</div>
    ${body}
  </div>`}const PAPER_SEASON1_START_MS=Date.parse("2026-07-01T00:00:00+09:00"),PAPER_SEASON_SEEN_KEY="kg_paper_season_seen_v1";function paperSeasonDday(){if(!Number.isFinite(PAPER_SEASON1_START_MS))return null;const ms=PAPER_SEASON1_START_MS-Date.now();return ms<=0?0:Math.ceil(ms/864e5)}function paperSeasonContext(){const lb=paperLeaderboard,season=lb?.season;if(!season||!(Number(season.no)>0))return null;const now=Date.now(),startMs=Date.parse(season.startIso||""),endMs=Date.parse(season.endIso||""),dleft=Number.isFinite(endMs)?Math.max(0,Math.ceil((endMs-now)/864e5)):null;let progPct=null,dayNum=null;Number.isFinite(startMs)&&Number.isFinite(endMs)&&endMs>startMs&&(progPct=Math.max(0,Math.min(100,Math.round((now-startMs)/(endMs-startMs)*100))),dayNum=Math.max(1,Math.min(14,Math.ceil((now-startMs)/864e5))));const meSeason=lb?.meSeason,myRank=Number(meSeason?.rank)||0,myPct=meSeason&&Number.isFinite(Number(meSeason.returnPct))?Number(meSeason.returnPct):null,total=Number(lb?.total)||0,seasonTop=Array.isArray(lb?.seasonTop)?lb.seasonTop:[],leader=seasonTop[0]||null,leaderPct=leader?Number(leader.returnPct)||0:null,emptyThrone=!leader||leaderPct!=null&&leaderPct<=.01;let throneText="";myRank===1?throneText="👑 시즌 선두 유지 중":myRank>=2&&myPct!=null&&leaderPct!=null&&(throneText=`👑 1위까지 <b>+${Math.max(0,leaderPct-myPct).toFixed(2)}%p</b>`);let rivalLine="",rivalLineShort="";if(myRank>=2&&myRank<=seasonTop.length&&myPct!=null){const above=seasonTop[myRank-2],below=seasonTop[myRank],gAbove=above?Math.max(0,(Number(above.returnPct)||0)-myPct).toFixed(2):null,gBelow=below?Math.max(0,myPct-(Number(below.returnPct)||0)).toFixed(2):null,buildRival=nickFn=>{const parts=[];return above&&parts.push(`↑${esc(nickFn(above.nickname))} <b>+${gAbove}%p</b>`),below&&parts.push(`↓${esc(nickFn(below.nickname))} <b>${gBelow}%p</b>`),parts.join(" · ")};rivalLine=buildRival(s=>String(s||"참가자")),rivalLineShort=buildRival(s=>{const t=String(s||"참가자");return t.length>4?`${t.slice(0,4)}…`:t})}const meDivision=lb?.meDivision||null,divMeta=meDivision&&Array.isArray(lb?.divisions)?lb.divisions.find(d=>d.key===meDivision):null,meDivName=divMeta?.name||null,meDivEmoji=divMeta?.emoji||"",meDivRank=Number(lb?.meDivisionRank)||0,meDivCount=Number(lb?.meDivisionCount)||0;return{season,dleft,progPct,dayNum,myRank,myPct,total,seasonTop,leader,leaderPct,emptyThrone,throneText,rivalLine,rivalLineShort,meDivision,meDivName,meDivEmoji,meDivRank,meDivCount}}function paperSeasonLiveHtml(season,opts={}){const ctx=paperSeasonContext();if(!ctx)return"";const{dleft,progPct,dayNum,myRank,myPct,total,seasonTop,leader,leaderPct,emptyThrone,rivalLineShort,meDivision,meDivName,meDivEmoji,meDivRank,meDivCount}=ctx,no=esc(String(season.no)),hdr=progPct!=null?`<div class="pp-season-hdr"><b class="ttl">시즌 ${no} <span class="ing">진행 중</span></b><span class="pp-mission-bar pp-season-hbar"><span style="width:${progPct}%"></span></span><em>${dayNum}일차<span class="pp-season-prog-tot">/14</span></em></div>`:`<div class="pp-season-hdr"><b class="ttl">시즌 ${no} 진행 중</b></div>`,leadRow=emptyThrone?'<div class="pp-season-lead pp-season-lead-empty">👑 1번 자리가 비어 있어요 — 먼저 수익 내면 잠정 1위</div>':`<div class="pp-season-lead">🥇 <span class="lb">현재 1위</span> <span class="nm">${esc(leader.nickname||"참가자")}</span> <b class="${paperDirClass(leaderPct)}">${paperSignedPct(leaderPct,1)}</b></div>`,inDiv=!!(meDivName&&meDivRank),myStat=myPct!=null?` · 시즌 <b class="${paperDirClass(myPct)}">${paperSignedPct(myPct,1)}</b>`:"",meRow=myRank?inDiv?`<div class="pp-season-mine">나는 <b>${esc(meDivName)} ${meDivRank}위</b>${meDivCount?` / ${meDivCount.toLocaleString("ko-KR")}명`:""}</div>`:`<div class="pp-season-mine">나는 <b>${myRank}위</b>${total?` / ${total.toLocaleString("ko-KR")}`:""}${myStat}</div>`:'<div class="pp-season-mine pp-season-mine-none">아직 시즌 순위 집계 전 — 거래하면 진입해요</div>',rivalRow=rivalLineShort?`<div class="pp-season-rival">${rivalLineShort}</div>`:"",foot=inDiv?`<div class="pp-season-foot"><div class="pp-season-league ${meDivision==="whale"?"pp-season-league-whale":meDivision==="shark"?"pp-season-league-shark":""}">${meDivEmoji} ${esc(meDivName)} 리그</div>${myPct!=null?`<span class="pp-season-myret">시즌 <b class="${paperDirClass(myPct)}">${paperSignedPct(myPct,1)}</b></span>`:""}</div>`:"",asideMedals=["🥇","🥈","🥉"],asideTop=Array.isArray(seasonTop)?seasonTop.slice(0,3):[],seasonAside=asideTop.length?`<div class="pp-season-aside" aria-hidden="true"><div class="pp-season-aside-t">시즌 TOP 3</div>${asideTop.map((r,i)=>`<div class="pp-season-aside-row"><span class="m">${asideMedals[i]}</span><span class="nm">${esc(r.nickname||"참가자")}</span><b class="${paperDirClass(Number(r.returnPct)||0)}">${paperSignedPct(Number(r.returnPct)||0,1)}</b></div>`).join("")}</div>`:"";return`<div class="${`pp-season pp-season-v2 pp-season-tappable${opts.openOnce?" pp-season-open-once":""}`}" data-pp-act="league-modal-open" role="button" tabindex="0" aria-label="시즌 ${no} 전체 랭킹 보기">
    <div class="pp-season-badge"><span class="s">시즌</span><span class="n">${no}</span></div>
    <div class="pp-season-mid">
      ${hdr}
      <div class="pp-season-zone">${leadRow}${meRow}${rivalRow}</div>
      ${foot}
    </div>
    ${seasonAside}
    <div class="pp-season-cd"><div class="d">${dleft!=null?`D-${dleft}`:"진행 중"}</div><div class="l pp-season-more">전체 랭킹 ›</div></div>
  </div>`}function paperLeagueModalHtml(){if(!paperLeagueModalOpen)return"";const ctx=paperSeasonContext();if(!ctx)return"";const{season,dleft,progPct,dayNum,myRank,myPct,total,seasonTop,leader,leaderPct,emptyThrone,throneText,rivalLine}=ctx,leaderLine=emptyThrone?"👑 <b>1번 자리가 비어 있어요</b>":`👑 현재 선두 <b>${esc(leader.nickname||"참가자")}</b> <b class="${paperDirClass(leaderPct)}">${paperSignedPct(leaderPct)}</b>`,header=`<div class="pp-league-hd">
    <div class="pp-league-hd-row"><span class="pp-league-hd-dday">${dleft!=null?`D-${dleft}`:"진행 중"}</span><span class="pp-league-hd-l">시즌 종료까지${progPct!=null?` · ${dayNum}일차/14`:""}</span></div>
    ${progPct!=null?`<span class="pp-mission-bar pp-league-bar"><span style="width:${progPct}%"></span></span>`:""}
    <div class="pp-league-leader">${leaderLine}</div>
  </div>`,divisions=Array.isArray(paperLeaderboard?.divisions)?paperLeaderboard.divisions:null,meDiv=paperLeaderboard?.meDivision||null,divMeta=divisions&&meDiv?divisions.find(d=>d.key===meDiv):null,divView=divisions&&["whale","shark","ant"].includes(paperLeagueDivView)?paperLeagueDivView:"all",meDivChip=divMeta?` <span class="pp-ldiv-badge">${divMeta.emoji} ${esc(divMeta.name)} 리그</span>`:"",meCard=myRank?`<div class="pp-league-me">
        <div class="pp-league-me-rank"><b>${myRank}</b><span>위 / ${total.toLocaleString("ko-KR")}</span></div>
        <div class="pp-league-me-body">
          <div class="pp-league-me-pct">시즌 수익률 <b class="${paperDirClass(myPct||0)}">${myPct!=null?paperSignedPct(myPct,1):"—"}</b>${throneText?` · ${throneText}`:""}${meDivChip}</div>
          ${rivalLine?`<div class="pp-league-me-rival">${rivalLine}</div>`:""}
        </div>
      </div>`:`<div class="pp-league-me pp-league-me-empty">아직 시즌 순위 집계 전 — 거래하면 진입해요${meDivChip}</div>`,divTabs=divisions&&divisions.length?`<div class="pp-ldiv-tabs"><button class="pp-ldiv-tab${divView==="all"?" on":""}" type="button" data-pp-act="league-div" data-div="all">전체</button>${divisions.map(d=>`<button class="pp-ldiv-tab${divView===d.key?" on":""}${meDiv===d.key?" mine":""}" type="button" data-pp-act="league-div" data-div="${esc(d.key)}">${d.emoji} ${esc(d.name)}<em>${(Number(d.count)||0).toLocaleString("ko-KR")}</em></button>`).join("")}</div>`:"",divCritPct={whale:"상위 10%",shark:"상위 30%",ant:"나머지"},divCriteria=divisions&&divisions.length?`<div class="pp-ldiv-criteria">체급은 <b>시즌 시작 평가자산</b> 순 — ${divisions.map(d=>`${d.emoji} ${esc(d.name)} ${divCritPct[d.key]||""}`).join(" · ")}</div>`:"",medals=["🥇","🥈","🥉"],medalRow=(row,i)=>{const pct=Number(row.returnPct)||0,isMe=paperRankRowIsMe(row,paperLeaderboard),rk=i<3?`<span class="pp-league-medal">${medals[i]}</span>`:`<span class="pp-league-rk">${i+1}</span>`;return`<div class="pp-league-row${isMe?" is-me":""}">${rk}<span class="pp-league-nick">${esc(row.nickname||"참가자")}${isMe?" <em>나</em>":""}</span><b class="pp-league-pct ${paperDirClass(pct)}">${paperSignedPct(pct)}</b></div>`},allBtn=total>0?`<button class="pp-btn pp-btn-ghost pp-league-all" type="button" data-pp-act="rank-modal-open" data-scope="season">전체 ${total.toLocaleString("ko-KR")}명 시즌 순위 ›</button>`:"";let rankBlock;if(divView==="all"&&divisions&&divisions.length)rankBlock=`${divisions.map(d=>{const tops=(paperLeaderboard?.divisionTops?.[d.key]||[]).slice(0,3),inner=tops.length?tops.map((row,i)=>medalRow(row,i)).join(""):'<div class="pp-leaguepod-empty">아직 거래 전이에요</div>';return`<div class="pp-leaguepod"><div class="pp-leaguepod-cap">${d.emoji} <b>${esc(d.name)}</b><em>${(Number(d.count)||0).toLocaleString("ko-KR")}명</em></div>${inner}</div>`}).join("")}${allBtn}`;else if(divView==="all")rankBlock=seasonTop.length?`<div class="pp-league-list">${seasonTop.map((row,i)=>medalRow(row,i)).join("")}</div>${allBtn}`:'<div class="pp-empty">아직 랭커가 없어요. 지금 거래하면 시즌 1위!</div>';else{const dt=paperLeaderboard?.divisionTops?.[divView],viewRows=Array.isArray(dt)?dt:[],dm=divisions.find(d=>d.key===divView),divNote=dm?dm.leader?`<div class="pp-ldiv-note">${dm.emoji} <b>${esc(dm.name)}</b> 리그 · ${(Number(dm.count)||0).toLocaleString("ko-KR")}명 · 선두 <b>${esc(dm.leader.nickname||"참가자")}</b> <b class="${paperDirClass(Number(dm.leader.returnPct)||0)}">${paperSignedPct(Number(dm.leader.returnPct)||0)}</b></div>`:`<div class="pp-ldiv-note">${dm.emoji} <b>${esc(dm.name)}</b> 리그 · ${(Number(dm.count)||0).toLocaleString("ko-KR")}명 · 아직 거래 전</div>`:"";rankBlock=viewRows.length?`${divNote}<div class="pp-league-list">${viewRows.map((row,i)=>medalRow(row,i)).join("")}</div>`:'<div class="pp-empty">이 리그는 아직 거래 전이에요.</div>'}const winners=Array.isArray(paperLeaderboard?.seasonWinners)?paperLeaderboard.seasonWinners:[],hofMedals=["🥇","🥈","🥉"],hof=winners.length?winners.map(w=>{const label=Number(w.seasonNo)===0?"원년":`시즌 ${esc(String(w.seasonNo))}`,prows=(Array.isArray(w.podium)&&w.podium.length?w.podium:[{rank:1,nickname:w.nickname,returnPct:w.returnPct}]).slice(0,3).map((p,i)=>`<div class="pp-league-hof-row"><span class="pp-league-hof-medal">${hofMedals[i]||""}</span><span class="pp-league-hof-nick">${esc(p.nickname||"참가자")}</span><b class="${paperDirClass(Number(p.returnPct)||0)}">${paperSignedPct(Number(p.returnPct)||0)}</b></div>`).join("");return`<div class="pp-league-hof"><div class="pp-league-hof-cap">👑 ${label} 챔피언</div>${prows}</div>`}).join(""):`<div class="pp-league-hof-empty">아직 박제된 왕이 없어요. 시즌 ${esc(String(season.no))} 종료(<b>${dleft!=null?`D-${dleft}`:"곧"}</b>)에 첫 👑 왕이 여기 영구 박제됩니다.</div>`;return`<div class="pp-modal-backdrop pp-league-modal-backdrop" data-pp-act="league-modal-close">
    <div class="pp-modal pp-league-modal" role="dialog" aria-modal="true" aria-label="시즌 ${esc(String(season.no))} 리그" data-pp-stop>
      <div class="pp-modal-head"><strong>🏆 리그 · 시즌 ${esc(String(season.no))}</strong><button class="pp-modal-x" type="button" data-pp-act="league-modal-close" aria-label="닫기">✕</button></div>
      <div class="pp-league-modal-body">
        ${header}${meCard}
        <div class="pp-league-sec-t">시즌 순위${divisions?" · 체급 리그":""}</div>${divTabs}${divCriteria}${rankBlock}
        <div class="pp-league-sec-t">👑 명예의 전당</div>${hof}
      </div>
    </div>
  </div>`}function paperSeasonPlaceholderHtml(){const season=paperLeaderboard?.season;if(season&&Number(season.no)>0){let openOnce=!1;try{typeof sessionStorage<"u"&&sessionStorage.getItem(PAPER_SEASON_SEEN_KEY)!==String(season.no)&&(sessionStorage.setItem(PAPER_SEASON_SEEN_KEY,String(season.no)),openOnce=!0)}catch{openOnce=!1}return paperSeasonLiveHtml(season,{openOnce})}const dday=paperSeasonDday();return dday&&dday>0?`<div class="pp-season pp-season-teaser" aria-label="시즌 1 오픈 카운트다운">
      <div class="pp-season-badge pp-season-badge-soon"><span class="s">시즌</span><span class="n">1</span></div>
      <div class="pp-season-mid">
        <div class="t">🏁 시즌 1 — <b>7월 1일</b> 오픈</div>
        <div class="d">2주 수익률로 첫 <b>왕</b>을 가려 👑 <b>명예의 전당</b>에 영구 박제 + ‘시즌 1 왕’ 칭호. 자산은 그대로·순위만 새 출발 — <b>1번 자리가 비어 있어요</b>.</div>
      </div>
      <div class="pp-season-cd pp-season-cd-soon"><div class="d">D-${dday}</div><div class="l">오픈까지</div></div>
    </div>`:`<div class="pp-season" aria-label="시즌 안내">
    <div class="pp-season-badge"><span class="s">시즌</span><span class="n">1</span></div>
    <div class="pp-season-mid">
      <div class="t">🏁 시즌 1 시작!</div>
      <div class="d">2주 수익률로 첫 왕을 가려요. 곧 <b>시즌 순위·내 성적</b>이 여기에 표시됩니다.</div>
    </div>
    <div class="pp-season-cd"><div class="d">진행 중</div><div class="l">시즌 1</div></div>
  </div>`}function paperLeagueDeckHtml(options={}){const spotlight=paperLeagueSpotlightHtml(),badges=paperRecentBadgeFeedHtml(!!options.rankingCompact),ranking=paperLeaderboardHtml(!!options.rankingCompact),trades=options.includeTrades===!1?"":paperPublicRecentTradesHtml(!!options.tradesCompact),hot=options.includeHot===!1?"":paperHotSymbolCardHtml(),invite=options.includeInvite===!1?"":paperInviteCardHtml();return`<div class="pp-league-wrap">${paperTradedTodayStripHtml()}${paperSeasonPlaceholderHtml()}${invite}${hot}${spotlight}<div class="pp-league-cols"><div class="pp-lc-main">${ranking}</div><div class="pp-lc-side">${paperSharingKingHtml()}${trades}${badges}</div></div></div>`}function paperLeaderboardHtml(compact=!1){const lb=paperLeaderboard,scope=(paperSeasonScopeActive()?["day","week","all","season"]:["day","week","all"]).includes(paperRankScope)?paperRankScope:"day",scopedRows=paperLeaderboardRowsForScope(lb,scope);if(!scopedRows.length)return!lb&&(paperLoadingText||paperLoadInFlight||paperPublicLeaderboardInFlight)?paperLeaderboardLoadingCardHtml():`<div class="pp-card pp-rank-card" data-pp-act="rank-modal-open" role="button" tabindex="0" aria-label="전체 참가자 랭킹 열기"><div class="pp-card-head"><span class="pp-card-title">수익률 랭킹</span></div>
      <div class="pp-empty">아직 랭커가 없어요. 지금 시작하면 1위!</div>
      ${compact?"":paperRankAllTriggerHtml(lb)}</div>`;const medals=["🥇","🥈","🥉"],visibleCount=compact?5:10,rows=scopedRows.slice(0,visibleCount).map((row,index)=>{const pct=paperRankReturnPct(row,scope),dir=paperDirClass(pct),isMe=paperRankRowIsMe(row,lb),rankNum=Number(row.rank)||index+1;return`<div class="pp-lb-row ${isMe?"is-me":""} ${paperRankMotionClass(row)}" data-pp-act="rank-expand" data-rankkey="${esc(paperRankRowKey(row))}" role="button" tabindex="0" title="클릭하면 포트폴리오 비중을 볼 수 있어요">
      <span class="pp-lb-rank">${rankNum<=3?medals[rankNum-1]:`${rankNum}위`}</span>
      ${paperRankMainHtml(row,isMe)}
      ${paperRankMoneyHtml(row,pct,dir)}
    </div>${paperRankDetailHtml(row)}`}).join(""),scopedMe=paperLeaderboardMeForScope(lb,scope),mePct=paperRankReturnPct(scopedMe,scope),meRow=scopedMe?.rank&&scopedMe.rank>visibleCount?`<div class="pp-lb-row is-me ${paperRankMotionClass(scopedMe)}" data-pp-act="rank-expand" data-rankkey="${esc(paperRankRowKey(scopedMe))}" role="button" tabindex="0"><span class="pp-lb-rank">${scopedMe.rank}위</span>${paperRankMainHtml(scopedMe,!0)}${paperRankMoneyHtml(scopedMe,mePct,paperDirClass(mePct))}</div>${paperRankDetailHtml(scopedMe)}`:"",updated=typeof relativeTimeKR=="function"&&lb.updatedAt?relativeTimeKR(lb.updatedAt):"",seasonTab=paperSeasonScopeActive()?`<button class="pp-btn pp-btn-season ${scope==="season"?"on":""}" type="button" data-pp-act="rank-scope" data-scope="season">시즌</button>`:"";return`<div class="pp-card pp-rank-card" data-pp-act="rank-modal-open" role="button" tabindex="0" aria-label="전체 참가자 랭킹 열기">
    <div class="pp-card-head"><span class="pp-card-title">수익률 랭킹</span><em>${compact?"":`<span class="pp-scope-toggle" role="group" aria-label="랭킹 기간">
      <button class="pp-btn ${scope==="day"?"on":""}" type="button" data-pp-act="rank-scope" data-scope="day">일간</button>
      <button class="pp-btn ${scope==="week"?"on":""}" type="button" data-pp-act="rank-scope" data-scope="week">주간</button>
      <button class="pp-btn ${scope==="all"?"on":""}" type="button" data-pp-act="rank-scope" data-scope="all">누적</button>
      ${seasonTab}
    </span>`}참가 ${lb.total}명${updated?` · ${esc(updated)} 집계`:""}</em></div>
    ${rows}${meRow}
    <p class="pp-lb-note">순위는 약 1분마다 자동으로 다시 계산돼요. 참가자를 클릭하면 보유 종목 비중을 볼 수 있어요.</p>
    ${compact?"":paperRankAllTriggerHtml(lb)}
  </div>`}function paperOrdersHtml(){const orders=paperData?.orders||[];return orders.length?`<div class="pp-card"><div class="pp-card-head"><span class="pp-card-title">최근 체결</span></div>${orders.slice(0,8).map(order=>{const isBonus=order.side==="bonus",sideText=isBonus?"보너스":order.side==="sell"?"매도":"매수",realized=Number(order.realized),time=typeof relativeTimeKR=="function"?relativeTimeKR(order.created_at):"",detail=isBonus?"공유 링크 방문":`${paperQtyText(order.market,order.qty)} × ${Math.round(order.price).toLocaleString("ko-KR")}`,amount=isBonus?paperWon(order.amount,!0):paperWon(order.amount);return`<div class="pp-order-row ${esc(isBonus?"bonus":order.side)}">
      <span class="pp-order-side">${sideText}</span>
      <b>${esc(order.name||order.code)}</b>
      <em>${esc(detail)}</em>
      <span class="pp-order-amt">${amount}${Number.isFinite(realized)&&order.side==="sell"?` <i class="${paperDirClass(realized)}">${paperWon(realized,!0)}</i>`:""}</span>
      <small>${esc(time)}</small>
    </div>`}).join("")}</div>`:""}function paperTradeMarketLabel(market){return market==="COIN"?"코인":market==="US"?"해외":market==="KR"?"국내":""}const PAPER_TRADES_PER_PAGE=8;function paperPublicRecentTradesHtml(compact=!1){const all=paperLeaderboard?.stats?.recentTrades||[];if(!all.length)return"";let pager="",trades;if(compact)trades=all.slice(0,PAPER_TRADES_PER_PAGE);else{const pageCount=Math.max(1,Math.ceil(all.length/PAPER_TRADES_PER_PAGE)),page=Math.min(Math.max(0,paperTradesPage),pageCount-1),start=page*PAPER_TRADES_PER_PAGE;trades=all.slice(start,start+PAPER_TRADES_PER_PAGE),pageCount>1&&(pager=`<div class="pp-trades-pager">
        <button class="pp-pager-btn" type="button" data-pp-act="trades-page" data-dir="prev" ${page<=0?"disabled":""} aria-label="이전 체결">‹</button>
        <span class="pp-pager-info">${start+1}–${Math.min(start+PAPER_TRADES_PER_PAGE,all.length)} / ${all.length}</span>
        <button class="pp-pager-btn" type="button" data-pp-act="trades-page" data-dir="next" ${page>=pageCount-1?"disabled":""} aria-label="다음 체결">›</button>
      </div>`)}return`<div class="pp-card pp-public-trades">
    <div class="pp-card-head"><span class="pp-card-title">전체 참가자 최근 체결</span><em>리더보드 캐시</em></div>
    <div class="pp-public-trade-list">${trades.map(trade=>{const side=trade.side==="sell"?"sell":"buy",sideText=side==="sell"?"매도":"매수",market=String(trade.market||"").toUpperCase(),marketText=paperTradeMarketLabel(market),code=String(trade.code||"").toUpperCase(),name=String(trade.name||trade.code||"종목").trim(),qty=Number(trade.qty),qtyText=Number.isFinite(qty)&&qty>0?` · ${paperQtyText(market,qty)}${market==="COIN"?"":"주"}`:"",time=typeof relativeTimeKR=="function"&&trade.createdAt?relativeTimeKR(trade.createdAt):"";return`<div class="pp-public-trade-row ${side}">
      <span class="pp-public-trade-side">${sideText}</span>
      <button class="pp-public-trade-symbol" type="button" data-pp-act="pill-quote" data-code="${esc(code)}" data-market="${esc(market)}" data-name="${esc(name)}" data-side="${esc(side)}" title="주문창에 ${esc(name)} 채우기"><b>${esc(name)}</b><i>주문</i></button>
      <em>${esc(trade.nickname||"참가자")} · ${esc(marketText)}${esc(qtyText)}</em>
      <small>${esc(time)}</small>
    </div>`}).join("")}</div>
    ${pager}
  </div>`}function paperFootHtml(){return`<div class="pp-foot">
    <button class="pp-btn" type="button" data-pp-act="refresh" ${paperLoadInFlight||paperBusy?"disabled":""}>${paperLoadingText?"새로고침 중...":"새로고침"}</button>
  </div>`}function paperTableHtml(){return paperError&&!paperData?paperLoadingShellHtml("error"):paperData?!paperData.account&&paperData.memberLocked&&!paperIsLoggedIn()?paperDashWrap(`<div class="pp">${paperPageHeaderHtml()}${paperMemberLockedHtml()}${paperLeagueDeckHtml({tradesCompact:!1})}${paperRankModalHtml()}${paperLeagueModalHtml()}${paperHelpModalHtml()}</div>`):paperData.account?paperDashWrap(`<div class="pp">
    ${paperPageHeaderHtml()}
    ${paperModNoticeHtml()}
    ${paperProgressHtml()}
    ${paperCodeRevealHtml()}
    ${paperRecoveryBannerHtml()}
    <section class="pp-zone pp-zone-account" aria-label="내 계좌">
      ${paperSummaryHtml()}
      <div class="pp-acct-grid">
        <div class="pp-acct-order">${paperTicketHtml()}${paperOrderAdBannerHtml()}${paperWeeklyMissionCardHtml()}${paperInviteCardHtml()}</div>
        <div class="pp-acct-hold">${paperPositionsHtml()}${paperOrdersHtml()}${paperHotSymbolCardHtml()}</div>
      </div>
    </section>
    <section class="pp-zone pp-zone-plaza" aria-label="광장">
      <div class="pp-plaza">${paperLeagueDeckHtml({tradesCompact:!1,includeTrades:!0,includeHot:!1,includeInvite:!1})}</div>
    </section>
    ${paperLinkHtml()}
    ${paperFootHtml()}
    ${paperBadgeModalHtml()}
    ${paperWelcomeBonusModalHtml()}
    ${paperRankModalHtml()}${paperLeagueModalHtml()}
    ${paperHelpModalHtml()}
    ${paperSettingsModalHtml()}
  </div>`):paperDashWrap(`<div class="pp">${paperPageHeaderHtml()}${paperProgressHtml()}${paperCodeRevealHtml()}${paperOnboardingHtml()}${paperLeagueDeckHtml({tradesCompact:!1})}${paperRankModalHtml()}${paperLeagueModalHtml()}${paperHelpModalHtml()}</div>`):paperLoadingShellHtml("loading")}let paperPendingBackgroundRepaint=!1,paperBackgroundRetryTimer=null;function paperPanelEditingInputId(){const el=document.activeElement;if(!el)return"";const tag=(el.tagName||"").toLowerCase();return tag!=="input"&&tag!=="textarea"?"":el.closest?.("#timelineTable")?el.id||"anon":""}function paperFlushBackgroundRepaintWhenIdle(){paperBackgroundRetryTimer||(paperBackgroundRetryTimer=setTimeout(()=>{if(paperBackgroundRetryTimer=null,!!paperPendingBackgroundRepaint){if(paperPanelEditingInputId()){paperFlushBackgroundRepaintWhenIdle();return}paperPendingBackgroundRepaint=!1,paperTabActive()&&renderPaperTab()}},1e3))}function renderPaperTab(opts){const table=document.getElementById("timelineTable");if(!table)return;if(opts&&opts.background&&paperPanelEditingInputId()){paperPendingBackgroundRepaint=!0,paperFlushBackgroundRepaintWhenIdle();return}const prevWinY=window.scrollY,prevWinX=window.scrollX||0,participantScroll=paperParticipantListScrollState();paperSyncOnboardDraftFromDom();const onboardFocus=paperOnboardFocusState();setTimelineTableMode(table,"forecast");try{table.innerHTML=paperTableHtml()}catch(error){try{debugWarn("paper render failed",error)}catch{}table.innerHTML=paperDashWrap('<div class="pp pp-state">화면 표시 중 오류가 났어요. 잠시 후 자동으로 다시 시도해요.</div>');return}const hint=document.getElementById("timelineHint");hint&&paperTabActive()&&(hint.textContent=""),paperInitSymbolAutocomplete(),paperLinkInfo&&paperRenderLinkQr(),paperRestoreOnboardFocus(onboardFocus),paperRestoreParticipantListScroll(participantScroll),typeof window.updatePapercardAd=="function"&&window.updatePapercardAd(),paperBalanceAccountCards(),Math.abs(window.scrollY-prevWinY)>1&&window.scrollTo({top:prevWinY,left:prevWinX,behavior:"auto"})}function paperBalanceAccountCards(){const grid=document.querySelector("#timelineTable .pp-acct-grid");if(!grid)return;const orderCol=grid.querySelector(".pp-acct-order"),holdCol=grid.querySelector(".pp-acct-hold");if(!orderCol||!holdCol)return;const floaters=[grid.querySelector(".pp-mission-card"),grid.querySelector(".pp-invite-card")].filter(Boolean);if(!floaters.length)return;const GAP=8;if(!(Math.abs(orderCol.offsetTop-holdCol.offsetTop)<6)){floaters.forEach(f=>orderCol.appendChild(f));return}const fH=floaters.map(f=>f.offsetHeight+GAP),leftFixed=orderCol.offsetHeight-floaters.reduce((s,f,i)=>s+(orderCol.contains(f)?fH[i]:0),0),rightFixed=holdCol.offsetHeight-floaters.reduce((s,f,i)=>s+(orderCol.contains(f)?0:fH[i]),0);let best=null;for(let mask=0;mask<1<<floaters.length;mask++){let L=leftFixed,R=rightFixed;for(let i=0;i<floaters.length;i++)mask&1<<i?L+=fH[i]:R+=fH[i];const score=Math.max(L,R)*1e4+Math.abs(L-R);(!best||score<best.score)&&(best={mask,score})}floaters.forEach((f,i)=>(best.mask&1<<i?orderCol:holdCol).appendChild(f))}let paperQrLibPromise=null;function paperLoadQrLib(){return typeof window.qrcode=="function"?Promise.resolve(window.qrcode):paperQrLibPromise||(paperQrLibPromise=new Promise((resolve,reject)=>{const script=document.createElement("script");script.src="/assets/vendor/qrcode.js?v=20260703-3240",script.async=!0,script.onload=()=>{typeof window.qrcode=="function"?resolve(window.qrcode):reject(new Error("QR unavailable"))},script.onerror=reject,document.head.appendChild(script)}).catch(error=>{throw paperQrLibPromise=null,error}),paperQrLibPromise)}function paperRenderLinkQr(){const box=document.getElementById("ppLinkQr");if(!box||!paperLinkInfo)return;const url=paperLinkInfo.url;paperLoadQrLib().then(qrFactory=>{const qr=qrFactory(0,"M");qr.addData(url),qr.make(),paperLinkInfo?.url===url&&(box.innerHTML=qr.createSvgTag(4,2,"모의투자 기기 연결 QR","excelkospi 모의투자 기기 연결"))}).catch(()=>{box.innerHTML='<span class="pp-muted">QR 생성 실패 — 아래 주소를 복사해 주세요.</span>'})}function paperInitSymbolAutocomplete(){paperSymbolAutocomplete?.destroy?.(),paperSymbolAutocomplete=null;const input=document.getElementById("ppSymbolInput"),panel=input?.closest?.(".pp-ticket");!input||!panel||!window.StockAutocomplete?.create||(paperSymbolAutocomplete=window.StockAutocomplete.create({input,panel,id:"paperStockAcList",sources:{KR:"/data/kr-stocks.json?v=20260703-3240",US:"/data/us-stocks.json?v=20260703-3240",COIN:"/data/coin-stocks.json?v=20260703-3240"},getActiveMarket:()=>"KR+US+COIN",filterItem:item=>item.market==="KR"||item.market==="US"||item.market==="COIN",onPick:paperPickSymbolSuggestion}))}function paperPickSymbolSuggestion(code,market,item={}){const nextCode=String(code||"").toUpperCase(),nextMarket=String(market||"KR").toUpperCase();nextCode&&(paperReadTicketInputs(),paperTicket={...paperTicket,code:nextCode,market:nextMarket,name:String(item?.name||nextCode),price:null,changePct:null,session:"",maxCashOrder:!1},paperLookupSymbol(nextCode,nextMarket).catch(e=>debugWarn("paper autocomplete lookup failed",e)))}function paperRankStripHtml(){const lb=paperLeaderboard;if(!lb?.top?.length)return"";const medals=["🥇","🥈","🥉"],top=lb.top.slice(0,3).map(row=>`<em>${medals[row.rank-1]||`${row.rank}위`} ${esc(row.nickname)} <i class="${paperDirClass(row.returnPct)}">${paperSignedPct(row.returnPct,1)}</i></em>`).join(""),me=lb.me?.rank?`<em class="pp-strip-me">나 ${lb.me.rank}위</em>`:"";return`<button type="button" class="pp-strip" data-pp-act="open-tab" aria-label="모의투자 랭킹 보기">
    <b>모의투자 랭킹</b>${top}${me}<span>참여하기 ›</span>
  </button>`}document.addEventListener("timeline-tab-updated",ev=>{String(ev?.detail?.key||"")==="forecast"&&!paperLeaderboard&&paperFetchLeaderboard(!1,{publicOnly:!0}).then(()=>{paperLeaderboard?.top?.length&&typeof timelineIsForecast=="function"&&timelineIsForecast()&&typeof renderMarketForecast=="function"&&renderMarketForecast()}).catch(()=>null)});function paperReadTicketInputs(){const symbolInput=document.getElementById("ppSymbolInput"),qtyInput=document.getElementById("ppQtyInput");return qtyInput&&(paperTicket.qty=qtyInput.value),{symbol:symbolInput?.value?.trim()||"",qty:qtyInput?.value||""}}async function paperLookupSymbol(symbolOverride="",marketOverride=""){if(paperBusy)return;const{symbol:typedSymbol}=paperReadTicketInputs(),symbol=String(symbolOverride||typedSymbol||"").trim();if(!symbol){showToast?.("종목명이나 코드를 입력해 주세요","err");return}let focusQty=!1;paperSetBusy("시세 확인 중...");try{const marketParam=String(marketOverride||"AUTO").toUpperCase(),res=await paperFetch(paperApiUrl(`/api/quote?code=${encodeURIComponent(symbol)}&market=${encodeURIComponent(marketParam)}&ttl=60&coinSource=upbit`)),data=await res.json();if(!res.ok||!data?.ok||!Number.isFinite(Number(data.price))){const error=new Error(data?.error||"시세를 찾지 못했어요");throw error.status=res.status,error.payload=data,error.api={...paperResponseApi(res),responseError:paperResponseErrorText(data)},paperReportError("lookup_symbol",error,{phase:"response",market:marketParam}),error}const market=String(data.market||"").toUpperCase();if(market!=="KR"&&market!=="COIN"&&market!=="US"){showToast?.("지금은 국내주식·해외주식·코인만 모의투자할 수 있어요","err");return}return paperTicket={...paperTicket,code:String(data.code||symbol).toUpperCase(),market,name:String(data.name||symbol),price:Number(data.price),changePct:Number(data.changePct??data.changePercent),session:String(data.marketState||""),maxCashOrder:!1},paperQuoteMap[`${paperTicket.code}:${market}`]={price:Number(data.price),changePct:paperTicket.changePct},market==="US"&&!paperFx()&&await paperEnsureFxQuote(),focusQty=!0,!0}catch(e){return paperReportError("lookup_symbol",e,{phase:"catch"}),showToast?.(e?.message||"시세 조회에 실패했어요","err"),!1}finally{paperClearBusy(),focusQty&&setTimeout(()=>document.getElementById("ppQtyInput")?.focus(),0)}}async function paperSubmitOrder(){if(paperBusy)return;paperReadTicketInputs();const disabledReason=paperTicketDisabledReason();if(disabledReason){showToast?.(disabledReason.detail||disabledReason.title||"주문 정보를 확인해 주세요",disabledReason.type==="closed"?"warn":"err");return}const qty=Number(paperTicket.qty);if(!paperTicket.code||!Number.isFinite(qty)||qty<=0){showToast?.("종목과 수량을 확인해 주세요","err");return}paperSetBusy(`${paperTicket.side==="buy"?"매수":"매도"} 주문 처리 중...`);try{const data=await paperPost("order",{code:paperTicket.code,market:paperTicket.market,side:paperTicket.side,qty,...paperTicket.side==="buy"&&paperTicket.maxCashOrder?{maxCashOrder:!0}:{}}),order=data?.order;showToast?.(`${order?.name||paperTicket.code} ${paperTicket.side==="buy"?"매수":"매도"} 체결 — ${paperWon(order?.amount)}`,"info"),paperTicket.qty="",paperNoteInteraction(),paperLeaderboardAt=0,await loadPaperTab({forceLb:!0,silent:!0}).catch(()=>null),paperApplyApproxRank(data?.rank),paperTabActive()&&renderPaperTab(),paperScheduleRankCatchUp()}catch(e){paperReportError("submit_order",e,{phase:"catch",market:paperTicket.market,side:paperTicket.side}),showToast?.(e?.message||"주문에 실패했어요","err")}finally{paperClearBusy()}}async function paperCopyText(text,options={}){let copied=!1;try{navigator.clipboard?.writeText&&window.isSecureContext!==!1&&(await navigator.clipboard.writeText(text),copied=!0)}catch{}if(!copied)try{const ta=document.createElement("textarea");ta.value=text,ta.setAttribute("readonly",""),ta.style.position="fixed",ta.style.left="-9999px",document.body.appendChild(ta),ta.select(),copied=document.execCommand("copy"),ta.remove()}catch{}if(!copied&&options.prompt!==!1)try{window.prompt(options.promptLabel||"복사할 내용을 선택하세요",text)}catch{}return options.silent||showToast?.(copied?"복사했어요":"복사창을 열었어요","info"),copied}async function paperShareLink(){const url=paperLinkInfo?.url||"";if(url){if(paperUseNativeShare())try{await navigator.share({title:"excelkospi 모의투자 계좌 연결",text:"이 링크로 모의투자 계좌를 연결하세요. 5분 뒤 만료됩니다.",url});return}catch(e){if(e?.name==="AbortError")return}await paperCopyText(url,{promptLabel:"계좌 연결 주소를 복사하세요"})}}async function paperCreateContestShare(snapshot=paperShareSnapshot()){const eligibility=paperContestShareEligibility(snapshot);if(!eligibility.ok){const err=new Error(eligibility.reason==="no-order"?"share_no_order":"share_snapshot_not_ready");throw err.userMessage=eligibility.message,err}const data=await paperPostJsonWithIdentity(paperApiUrl("/api/paper-share"),{user_id:paperUserIdSafe(),origin:location.origin,snapshot}),token=paperShareToken(data.token)||paperShareUrlToken(data.url),url=paperContestShareUrl(token,data.url);if(!token||!url)throw new Error("share_token_missing");return{url,title:`${paperProfitText(snapshot.profit)} · 엑셀코스피 모의투자`,text:paperShareText(snapshot),image:paperContestShareImageUrl(token,data.image)}}function paperShareContest(){if(paperBusy||paperShareInFlight)return;const snapshot=paperShareSnapshot(),eligibility=paperContestShareEligibility(snapshot);if(!eligibility.ok){paperRememberContestSharePreview({url:"",image:"",title:"수익 자랑 미리보기",text:eligibility.message,status:"error"}),showToast?.(eligibility.message,"warn");return}paperShareInFlight=!0,paperRememberContestSharePreview({url:"",image:"",title:"수익 자랑 미리보기",text:paperContestShareText(snapshot),status:"loading"}),setTimeout(()=>document.querySelector(".pp-share-preview")?.scrollIntoView?.({behavior:"smooth",block:"nearest"}),30),paperFinishContestShare(snapshot)}async function paperFinishContestShare(snapshot){let share;try{share=await paperCreateContestShare(snapshot)}catch(e){debugWarn("paper share link failed",e);const message=e?.userMessage||(e?.message==="share_snapshot_not_ready"?"수익 집계가 화면에 뜬 뒤 다시 공유해 주세요":"공유 이미지 링크를 만들지 못했어요. 잠시 후 다시 눌러 주세요");paperRememberContestSharePreview({url:"",image:"",title:"수익 자랑 미리보기",text:message,status:"error"}),showToast?.(message,"warn");return}finally{paperShareInFlight=!1,paperTabActive()&&renderPaperTab()}const text=share.text||paperShareText(),url=paperContestShareUrl("",share.url);if(!url){paperRememberContestSharePreview({url:"",image:"",title:"수익 자랑 미리보기",text:"공유 이미지 링크를 만들지 못했어요. 잠시 후 다시 눌러 주세요",status:"error"}),showToast?.("공유 이미지 링크를 만들지 못했어요. 잠시 후 다시 눌러 주세요","warn");return}const shareText=`${text}
${PAPER_SHARE_BONUS_COPY}`,copyText=`${shareText}
${url}`;paperRememberContestSharePreview({...share,url,copyText});const nativeShare=paperUseNativeShare(),copied=await paperCopyText(copyText,{silent:!0,prompt:!nativeShare,promptLabel:"공유 문구를 복사하세요"});if(nativeShare)try{await navigator.share({title:share.title||"엑셀코스피 모의투자 대회",text:shareText,url}),showToast?.("공유 문구도 복사해 뒀어요","info");return}catch(e){if(e?.name==="AbortError")return}showToast?.(copied?"공유 문구와 주소를 복사했어요":"복사창을 열었어요","info")}async function paperRenameNickname(){if(paperBusy)return;const input=document.getElementById("ppRenameInput"),nickname=String(input?.value||"").trim();if(!nickname){showToast?.("닉네임을 입력해 주세요","err"),input?.focus?.();return}paperSetBusy("닉네임을 변경하는 중...");try{const previous=paperData?.account?.nickname||"",data=await paperPost("rename",{nickname});if(data?.account&&paperData?.account&&(paperData.account={...paperData.account,...data.account}),paperLeaderboard?.me&&data?.account?.nickname&&(paperLeaderboard.me.nickname=data.account.nickname),previous&&data?.account?.nickname)for(const key of["top","dailyTop","weeklyTop","allTop","participants"])Array.isArray(paperLeaderboard?.[key])&&(paperLeaderboard[key]=paperLeaderboard[key].map(row=>row?.nickname===previous&&(row?.rank===paperLeaderboard?.me?.rank||row?.allRank===paperLeaderboard?.me?.rank)?{...row,nickname:data.account.nickname}:row));paperLeaderboardAt=0,paperNicknameEditing=!1,showToast?.(data?.unchanged?"이미 같은 닉네임이에요":"모의투자 닉네임을 변경했어요","info"),await loadPaperTab({forceLb:!0,silent:!0}).catch(()=>null)}catch(e){showToast?.(e?.message||"닉네임 변경에 실패했어요","err"),setTimeout(()=>document.getElementById("ppRenameInput")?.focus?.(),0)}finally{paperClearBusy()}}const PAPER_SHARE_WINDOW_MS=60*60*1e3,PAPER_SHARE_LIMIT=2,PAPER_BOAST_AT_KEY="kg_paper_boast_at_v1",PAPER_BOARD_SHARE_AT_KEY="kg_paper_board_share_at_v1";function paperShareReadStamps(key){try{const raw=JSON.parse(localStorage.getItem(key)||"[]"),now=Date.now();return(Array.isArray(raw)?raw:[]).map(Number).filter(t=>Number.isFinite(t)&&t<=now&&t>now-PAPER_SHARE_WINDOW_MS).sort((a,b)=>a-b)}catch{return[]}}function paperShareWriteStamps(key,stamps){try{localStorage.setItem(key,JSON.stringify(stamps.slice(-PAPER_SHARE_LIMIT)))}catch{}}function paperShareRemainMs(key){const stamps=paperShareReadStamps(key);return stamps.length<PAPER_SHARE_LIMIT?0:Math.max(0,stamps[0]+PAPER_SHARE_WINDOW_MS-Date.now())}function paperShareMark(key){const stamps=paperShareReadStamps(key);stamps.push(Date.now()),paperShareWriteStamps(key,stamps)}function paperShareForce(key,ms){const remain=Math.max(0,Math.min(PAPER_SHARE_WINDOW_MS,Number(ms)||0)),base=Date.now()-(PAPER_SHARE_WINDOW_MS-remain);paperShareWriteStamps(key,Array.from({length:PAPER_SHARE_LIMIT},()=>base))}function paperBoardShareCooldownRemainMs(){return paperShareRemainMs(PAPER_BOARD_SHARE_AT_KEY)}function paperBoardShareMarkSent(){paperShareMark(PAPER_BOARD_SHARE_AT_KEY)}function paperBoastCooldownRemainMs(){return paperShareRemainMs(PAPER_BOAST_AT_KEY)}function paperBoastMarkSent(){paperShareMark(PAPER_BOAST_AT_KEY),paperTabActive()&&renderPaperTab()}function paperBoastMarkRetryAfter(seconds){paperShareForce(PAPER_BOAST_AT_KEY,Math.max(0,Number(seconds)||0)*1e3),paperTabActive()&&renderPaperTab()}function paperBoastClearCooldown(){try{localStorage.removeItem(PAPER_BOAST_AT_KEY)}catch{}paperTabActive()&&renderPaperTab()}async function paperBoastToChat(){if(typeof sendChatMessage!="function"){showToast?.("채팅이 아직 준비되지 않았어요","warn");return}const eligibility=paperBoastEligibility();if(!eligibility.ok){eligibility.reason==="zero-profit"?showToast?.("손익이 0원일 때는 채팅에 자랑할 수 없어요","warn"):showToast?.("모의투자 주문 내역이 있어야 채팅에 자랑할 수 있어요","warn");return}const remain=paperBoastCooldownRemainMs();if(remain>0){const minutes=Math.ceil(remain/6e4);showToast?.(`채팅 자랑은 1시간에 2번까지예요. ${minutes>=60?`${Math.ceil(minutes/60)}시간`:`${minutes}분`} 뒤에 다시 가능해요.`,"warn");return}if(typeof chatSendCooldownMs=="function"){const cooldownMs=chatSendCooldownMs();if(cooldownMs>0){showToast?.(`채팅은 ${Math.ceil(cooldownMs/1e3)}초에 한 번만 보낼 수 있어요`,"warn");return}}typeof setChatOpen=="function"&&setChatOpen(!0,{connect:!0,persist:!1}),promotePaperNicknameToChatIdentity(),sendChatMessage(paperBoastMessage())}function promotePaperNicknameToChatIdentity(){try{if(typeof saveUnifiedNickname!="function"||typeof isInlineAdmin=="function"&&isInlineAdmin()||typeof memberAccountNickname=="function"&&memberAccountNickname())return;const rawPaperNick=paperData?.account?.nickname||"",paperNick=typeof sanitizeLocalNickname=="function"?sanitizeLocalNickname(rawPaperNick):String(rawPaperNick).trim();if(!paperNick||paperNick==="월급루팡")return;const current=typeof preferredUnifiedNickname=="function"?preferredUnifiedNickname():"";if(current&&!/^월급루팡_/.test(current))return;saveUnifiedNickname(paperNick)}catch{}}function paperBoardShareEligibility(){return paperData?.account?(Number(paperOrderBreakdown(paperData.account)?.total)||0)<=0?{ok:!1,reason:"no-order"}:{ok:!0}:{ok:!1,reason:"no-account"}}async function paperEnsureBoardShareEligibility(options={}){const current=paperBoardShareEligibility(),hasSellWithoutId=!!(options.withTrades&&paperData?.account&&(paperData?.orders||[]).some(order=>order?.side==="sell"&&Number.isFinite(Number(order.realized))&&!String(order.id||"").startsWith("o_")));if(current.ok&&!hasSellWithoutId)return current;let refreshFailed=!1;return(!paperData||hasSellWithoutId)&&await loadPaperTab({silent:!0}).catch(e=>{refreshFailed=!0;try{typeof debugWarn=="function"&&debugWarn("paper eligibility refresh failed",e)}catch{}}),refreshFailed&&!paperData?.account?{ok:!1,reason:"load-failed"}:paperBoardShareEligibility()}window.paperEnsureBoardShareEligibility=paperEnsureBoardShareEligibility;function paperReadBoardShareDraft(){if(!paperBoardShare)return;const titleEl=document.getElementById("ppBoardShareTitle"),bodyEl=document.getElementById("ppBoardShareComment"),tradeEl=document.getElementById("ppBoardShareTrade");titleEl&&(paperBoardShare.title=String(titleEl.value||"")),bodyEl&&(paperBoardShare.body=String(bodyEl.value||"")),tradeEl&&(paperBoardShare.tradeId=String(tradeEl.value||""))}async function paperShareToBoard(){if(paperBusy)return;paperReadBoardShareDraft();const elig=await paperEnsureBoardShareEligibility({withTrades:!0});if(!elig.ok){showToast?.(elig.reason==="no-order"?"모의투자 주문 내역이 있어야 게시판에 공유할 수 있어요":elig.reason==="load-failed"?"모의투자 기록을 확인하지 못했어요. 잠시 후 다시 눌러 주세요":"먼저 모의투자 계좌를 만들어 주세요","warn");return}const remain=paperBoardShareCooldownRemainMs();if(remain>0){const minutes=Math.ceil(remain/6e4);showToast?.(`게시판 자랑은 1시간에 2번까지예요. ${minutes>=60?`${Math.ceil(minutes/60)}시간`:`${minutes}분`} 뒤에 다시 가능해요.`,"warn");return}const channel=paperBoardShare?.channel||"free",title=String(paperBoardShare?.title||"").trim().slice(0,60);let comment=String(paperBoardShare?.body||"").trim().slice(0,500);const tradeId=String(paperBoardShare?.tradeId||"").trim(),shareTrade=paperBoastTradeForShare(tradeId),shareTradeId=String(shareTrade?.id||tradeId||"").trim(),sentence=paperBoastTradeSentence(shareTrade);sentence&&!comment.includes(sentence)&&(comment=(comment?`${sentence}
${comment}`:sentence).slice(0,500));const nickname=typeof communityNicknameForSend=="function"?communityNicknameForSend(""):paperData?.account?.nickname||"";if(!nickname){showToast?.("닉네임을 확인할 수 없어요. 잠시 후 다시 시도해 주세요","warn");return}const guardText=[title,comment].filter(Boolean).join(`
`);if(!(guardText&&typeof guardChatMessage=="function"&&await guardChatMessage(guardText,"게시판 글").catch(()=>!0)===!1)){paperSetBusy("게시판에 자랑하는 중...");try{const attempt=async(fresh,forceIdentity=!1)=>{const identityToken=forceIdentity?await paperIdentityToken(!0):paperCachedIdentityToken(),ttok=typeof chatTurnstileToken=="function"?await chatTurnstileToken(2e4,fresh?{fresh:!0}:{nonBlocking:!0}).catch(()=>""):"",res2=await paperFetch(paperApiUrl("/api/community"),{method:"POST",headers:{"content-type":"application/json",...identityToken?{"x-chat-identity-token":identityToken}:{},...paperAuthHeader()},body:JSON.stringify({action:"create",app_build:document.querySelector('meta[name="build-id"]')?.getAttribute("content")||"",paper_share:!0,...ttok?{turnstile_token:ttok}:{},...identityToken?{identity_token:identityToken}:{},user_id:paperUserIdSafe(),nickname,channel,paper_badges:paperMyNickBadgeIds(),...title?{title}:{},...shareTradeId?{paper_trade_id:shareTradeId}:{},body:comment})}),data2=await res2.json().catch(()=>({}));return{res:res2,data:data2}};let{res,data}=await attempt(!1),usedFreshTurnstile=!1;if(res.status===403&&data?.error==="turnstile_required"&&(usedFreshTurnstile=!0,{res,data}=await attempt(!0)),(!res.ok||data?.ok===!1)&&paperIdentityError(data)&&(paperClearIdentityToken(),{res,data}=await attempt(usedFreshTurnstile,!0)),res.status===403&&data?.error==="turnstile_required"&&({res,data}=await attempt(!0)),(!res.ok||data?.ok===!1)&&paperIdentityError(data)&&(paperClearIdentityToken(),{res,data}=await attempt(!0,!0)),!res.ok||data?.ok===!1||!data?.post){const err=data?.error,msg=err==="no_paper_record"?"공유할 모의투자 기록이 없어요 (첫 주문 후 가능해요)":err==="turnstile_required"?"사람 확인을 마치지 못했어요. 다시 한 번 눌러 주세요":err==="rate_limited"||err==="first_write_cooldown"?"잠시 후 다시 시도해 주세요":data?.banned?"지금은 글을 쓸 수 없는 상태예요":"게시판 자랑에 실패했어요. 잠시 후 다시 시도해 주세요";showToast?.(msg,"warn");return}paperBoardShare=null,paperBoardShareMarkSent();try{const post=data.post,activeCh=typeof communityActiveChannel=="function"?communityActiveChannel():"",dedupe=list=>{const seen=new Set;return list.filter(p=>{const pid=p?.id?String(p.id):"";return!pid||seen.has(pid)?!1:(seen.add(pid),!0)})};if(activeCh===channel&&typeof communityPosts<"u")communityPosts=dedupe([post,...communityPosts]),typeof writeCommunityPostsCache=="function"&&writeCommunityPostsCache(channel,communityPosts,{hasMore:typeof communityHasMore<"u"?communityHasMore:!1,nextCursor:typeof communityNextCursor<"u"?communityNextCursor:""}),typeof markCommunityPostInserted=="function"&&markCommunityPostInserted(post.id),typeof renderCommunityTable=="function"&&(typeof timelineIsCommunity!="function"||timelineIsCommunity())&&renderCommunityTable();else if(typeof readCommunityPostsCacheEntry=="function"&&typeof writeCommunityPostsCache=="function"){const entry=readCommunityPostsCacheEntry(channel,{allowStale:!0});entry&&Array.isArray(entry.posts)&&writeCommunityPostsCache(channel,dedupe([post,...entry.posts]),{hasMore:entry.hasMore,nextCursor:entry.nextCursor,mentionSummary:entry.mentionSummary})}}catch(e){try{debugWarn?.("paper share board cache refresh failed",e)}catch{}}const label=PAPER_SHARE_CHANNELS.find(c=>c.key===channel)?.label||"게시판";showToast?.(`${label} 게시판에 자랑했어요`,"info"),renderPaperTab()}catch(e){debugWarn("paper board share failed",e),showToast?.("게시판 자랑에 실패했어요. 잠시 후 다시 시도해 주세요","warn")}finally{paperClearBusy()}}}function paperTogglePinDraft(id){if(!PAPER_BADGE_BY_ID[id])return;const alreadyPinned=paperPinnedBadgeIds().includes(id);if(!paperEarnedBadgeIds().includes(id)&&!(paperPreservePinnedBadges()&&alreadyPinned))return;const base=Array.isArray(paperPinDraft)?paperPinDraft.slice():paperPinnedBadgeIds().slice(),idx=base.indexOf(id);if(idx>=0)base.splice(idx,1);else if(base.length>=PAPER_MAX_PINNED_BADGES){showToast?.(`닉네임 뱃지는 최대 ${PAPER_MAX_PINNED_BADGES}개까지예요`,"warn"),paperPinDraft=base;return}else base.push(id);paperPinDraft=base}async function paperSavePinnedBadges(){if(paperBusy)return;const draft=Array.isArray(paperPinDraft)?paperPinDraft.slice(0,PAPER_MAX_PINNED_BADGES):paperPinnedBadgeIds(),earned=paperEarnedBadgeIds(),storedPinned=paperStoredPinnedBadgeIds(),requested=draft.filter(id=>PAPER_BADGE_BY_ID[id]&&(earned.includes(id)||paperPreservePinnedBadges()&&storedPinned.includes(id))).slice(0,PAPER_MAX_PINNED_BADGES);paperSetBusy("뱃지를 저장하는 중...");try{const data=await paperPost("pin-badges",{badges:requested,nickCardMode:paperNickCardModeDraft==="div"||paperNickCardModeDraft==="badge"?paperNickCardModeDraft:paperNickCardMode});(data?.nickCardMode==="div"||data?.nickCardMode==="badge")&&(paperNickCardMode=data.nickCardMode);const earned2=Array.isArray(data?.badges?.earned)?data.badges.earned:paperEarnedBadgeIds(),cleanPinned=(Array.isArray(data?.badges?.pinned)?data.badges.pinned:requested).filter(id=>PAPER_BADGE_BY_ID[id]).slice(0,PAPER_MAX_PINNED_BADGES);paperSessionBadgeIds=paperPreservePinnedBadges()?cleanPinned:cleanPinned.filter(id=>earned2.includes(id)),paperData&&(paperData.badges={...paperData.badges||{},earned:earned2,pinned:paperSessionBadgeIds},paperData.nickCardMode=paperNickCardMode),paperPinDraft=null,paperNickCardModeDraft=null,paperBadgeModalOpen=!1,showToast?.("닉네임 뱃지를 저장했어요","info"),renderPaperTab()}catch(e){try{debugWarn?.("paper pin badges failed",e)}catch{}showToast?.("뱃지를 저장하지 못했어요. 잠시 후 다시 눌러 주세요","warn")}finally{paperClearBusy()}}function paperWarmTurnstile(){typeof primeTurnstile=="function"&&primeTurnstile().catch(()=>{})}async function paperHandleAction(act,btn){if(paperNoteInteraction(),(paperHelpOpen||paperSettingsOpen)&&act!=="help-open"&&act!=="settings-open"&&(paperHelpOpen=!1,paperSettingsOpen=!1),act==="help-open"){paperHelpOpen=!0,paperSettingsOpen=!1,renderPaperTab();return}if(act==="help-close"){paperHelpOpen=!1,renderPaperTab();return}if(act==="settings-open"){paperSettingsOpen=!0,paperHelpOpen=!1,renderPaperTab();return}if(act==="settings-close"){paperSettingsOpen=!1,renderPaperTab();return}if(act==="open-tab"){typeof setTimelineTab=="function"&&setTimelineTab("paper");return}if(act==="signup-start"){try{if(window.__nativeShell?.openAccount?.("login","/login?mode=register"))return}catch{}try{location.assign("/login?mode=register")}catch{}return}if(act==="guest-start"){paperGuestOnboardStarted=!0,renderPaperTab(),paperWarmTurnstile(),setTimeout(()=>document.getElementById("ppNickInput")?.focus?.(),0);return}if(act==="member-import"||act==="member-start"){if(paperBusy)return;paperSetBusy("계좌를 가져오는 중...");try{const r=typeof authLinkPaperAccount=="function"?await authLinkPaperAccount():{ok:!1};r.ok&&r.absorbed?(paperMemberStage="",paperMemberScenario=r.already?"already":"linked",paperOnboardDraft={nickname:"",recovery:""},showToast?.(r.already?"이미 이 계정에 연결된 계좌예요":"이 기기의 모의투자 계좌를 계정으로 가져왔어요","info"),await loadPaperTab({forceLb:!0,silent:!0}).catch(()=>null),await ensurePaperPerformanceNotificationDefault({silent:!0}).catch(()=>null)):r.status===409||r.error==="paper_conflict"?(paperMemberStage="",paperMemberScenario="conflict",showToast?.("이미 이 계정에 모의투자 계좌가 있어요","info"),await loadPaperTab({forceLb:!0,silent:!0}).catch(()=>null)):(paperMemberStage="",paperMemberScenario="no-source",showToast?.("이 브라우저에 가져올 모의투자 계좌가 없어요","info"),renderPaperTab())}catch(e){showToast?.(e?.message||"확인에 실패했어요. 잠시 후 다시 시도해 주세요.","err")}finally{paperClearBusy()}return}if(act==="member-cancel"){paperMemberStage="",renderPaperTab();return}if(act==="create"||act==="member-create"||act==="member-new"){if(paperBusy)return;const nickname=act==="create"?String(document.getElementById("ppNickInput")?.value||"").trim():paperMemberNickname();if(paperOnboardDraft.nickname=nickname,!nickname){showToast?.("닉네임을 입력해야 계좌를 만들 수 있어요","err"),document.getElementById("ppNickInput")?.focus();return}act==="create"&&paperWarmTurnstile(),paperSetBusy("계좌를 만드는 중...");let clearedBusy=!1;try{const data=await paperPost("create",{nickname});data.recoveryCode&&!paperIsLoggedIn()&&(paperRevealCode=data.recoveryCode),data?.account&&(paperData={account:data.account,positions:Array.isArray(data.positions)?data.positions:[],orders:Array.isArray(data.orders)?data.orders:[],policy:data.policy||paperData?.policy||{}},paperLeaderboard=data.leaderboard?{...paperLeaderboard||{},...data.leaderboard}:paperLeaderboard,paperError="",writePaperSnapshot()),paperOnboardDraft={nickname:"",recovery:""},paperGuestOnboardStarted=!1,paperMemberStage="",paperMemberScenario="",paperNicknameEditing=!1,showToast?.("모의투자 계좌가 생겼어요! 가상 5,000만원 입금 완료","info"),paperClearBusy(),clearedBusy=!0,data?.account&&paperTabActive()&&renderPaperTab(),setTimeout(()=>{paperLeaderboardAt=0,loadPaperTab({forceLb:!0,silent:!0}).catch(e=>{try{debugWarn?.("paper create refresh failed",e)}catch{}}),ensurePaperPerformanceNotificationDefault({silent:!0}).catch(()=>null)},0)}catch(e){showToast?.(e?.message||"계좌 생성에 실패했어요","err")}finally{clearedBusy||paperClearBusy()}return}if(act==="recover"){if(paperBusy)return;paperSetBusy("계좌를 복구하는 중...");try{const code=document.getElementById("ppRecoverInput")?.value||"";paperOnboardDraft.recovery=code;const data=await paperPost("recover",{code});data?.recoveryCode&&!paperIsLoggedIn()&&(paperRevealCode=data.recoveryCode),paperOnboardDraft={nickname:"",recovery:""},paperGuestOnboardStarted=!1,paperNicknameEditing=!1,showToast?.("계좌를 되찾았어요!","info"),await loadPaperTab({forceLb:!0,silent:!0}).catch(()=>null),await ensurePaperPerformanceNotificationDefault({silent:!0}).catch(()=>null)}catch(e){showToast?.(e?.message||"복구에 실패했어요","err"),document.getElementById("ppRecoverInput")?.select?.()}finally{paperClearBusy()}return}if(act==="copy-code")return paperCopyText(btn.dataset.code||"");if(act==="copy-share-text")return paperCopyText(paperContestSharePreview?.copyText||paperContestSharePreview?.url||"",{promptLabel:"공유 문구를 복사하세요"});if(act==="nickname-edit"){if(!paperNicknameCanChange()){showToast?.(`${paperNicknameChangeButtonText()} 가능해요`,"warn");return}paperSettingsOpen=!1,paperNicknameEditing=!0,renderPaperTab(),setTimeout(()=>document.getElementById("ppRenameInput")?.select?.(),0);return}if(act==="nickname-cancel"){paperNicknameEditing=!1,renderPaperTab();return}if(act==="nickname-save")return paperRenameNickname();if(act==="code-done"){paperRevealCode="",renderPaperTab();return}if(act==="ack-notice"){if(paperBusy)return;paperSetBusy("확인 중...");try{await paperPost("ack-notice"),await loadPaperTab({silent:!0}).catch(()=>null)}catch(e){showToast?.(e?.message||"처리하지 못했어요","err")}finally{paperClearBusy()}return}if(act==="link-close"){paperLinkInfo=null,renderPaperTab();return}if(act==="link-share")return paperShareLink();if(act==="side-buy"||act==="side-sell"){paperReadTicketInputs(),paperTicket.side=act==="side-buy"?"buy":"sell",paperTicket.maxCashOrder=!1,renderPaperTab();return}if(act==="lookup")return paperLookupSymbol();if(act==="qty-add"){paperReadTicketInputs();const add=Number(btn.dataset.n)||1,current=Number(paperTicket.qty)||0;paperTicket.qty=String(current+add),paperTicket.maxCashOrder=!1,renderPaperTab();return}if(act==="qty-clear"){paperTicket.qty="",paperTicket.maxCashOrder=!1,renderPaperTab();return}if(act==="qty-percent"||act==="qty-max"){paperReadTicketInputs();const pct=act==="qty-max"?1:Math.max(0,Math.min(1,Number(btn.dataset.pct)||0));if(paperTicket.side==="buy"&&paperTicket.market==="US"&&!paperFx()){paperSetBusy("최대 수량 계산 중...");try{await paperEnsureFxQuote()}finally{paperClearBusy()}}const nextQty=paperQtyForPct(pct);nextQty?(paperTicket.qty=nextQty,paperTicket.maxCashOrder=paperTicket.side==="buy"&&pct>=.999):(paperTicket.maxCashOrder=!1,showToast?.(paperTicket.side==="sell"?"매도할 보유 수량이 없어요":"살 수 있는 수량을 계산하지 못했어요","err")),renderPaperTab();return}if(act==="prefill"){paperTicket={...paperTicket,code:String(btn.dataset.code||"").toUpperCase(),market:String(btn.dataset.market||"KR").toUpperCase(),side:btn.dataset.side==="sell"?"sell":"buy",qty:btn.dataset.qty||"",session:"",maxCashOrder:!1};const pos=(paperData?.positions||[]).find(p=>p.code===paperTicket.code&&p.market===paperTicket.market);paperTicket.name=pos?.name||paperTicket.code;const rawQuote=Number(paperQuoteMap[`${paperTicket.code}:${paperTicket.market}`]?.price);paperTicket.market==="US"?paperTicket.price=Number.isFinite(rawQuote)&&rawQuote>0?rawQuote:null:paperTicket.price=paperPositionMark(pos||{code:paperTicket.code,market:paperTicket.market,avg_price:null}),renderPaperTab();return}if(act==="submit-order")return paperSubmitOrder();if(act==="reissue"){if(paperBusy)return;paperSetBusy("복구코드를 재발급하는 중...");try{paperRevealCode=(await paperPost("reissue")).recoveryCode||"",renderPaperTab(),setTimeout(()=>document.querySelector(".pp-code")?.scrollIntoView?.({behavior:"smooth",block:"center"}),50),showToast?.("새 복구코드를 발급했어요. 이전 코드는 무효가 됐어요.","info")}catch(e){showToast?.(e?.message||"재발급에 실패했어요","err")}finally{paperClearBusy()}return}if(act==="qr"){if(paperBusy)return;paperSetBusy("기기 연결 링크를 만드는 중...");try{const data=await paperPost("link-issue");paperLinkInfo={token:data.token,url:`${paperShareUrl()}?paperlink=${data.token}`},renderPaperTab(),setTimeout(()=>document.querySelector(".pp-link")?.scrollIntoView?.({behavior:"smooth",block:"center"}),50)}catch(e){showToast?.(e?.message||"QR 생성에 실패했어요","err")}finally{paperClearBusy()}return}if(act==="refresh"){paperLeaderboardAt=0,paperQuoteRecoveryAttempt=0,loadPaperTab({forceLb:!0}).catch(()=>null);return}if(act==="rank-jump"||act==="rank-modal-open"){const reqScope=String(btn.dataset.scope||"");(reqScope==="season"&&paperSeasonScopeActive()||reqScope==="week"||reqScope==="all"||reqScope==="day")&&(paperRankScope=reqScope),paperLeagueModalOpen=!1,paperRankModalOpen=!0,Array.isArray(paperLeaderboard?.participants)||(paperParticipantsError="",renderPaperTab(),await paperFetchLeaderboard(!0,{participants:!0}).catch(()=>(paperParticipantsError="전체 목록을 불러오지 못했어요",null))),renderPaperTab();return}if(act==="rank-modal-close"){paperRankModalOpen=!1,renderPaperTab();return}if(act==="league-modal-open"){if(!paperSeasonScopeActive())return;paperLeagueModalOpen=!0,renderPaperTab();return}if(act==="league-modal-close"){paperLeagueModalOpen=!1,renderPaperTab();return}if(act==="league-div"){const div=String(btn.dataset.div||"all");paperLeagueDivView=["whale","shark","ant"].includes(div)?div:"all",renderPaperTab();return}if(act==="rank-expand"){const key=String(btn.dataset.rankkey||"");paperExpandedRankKey=paperExpandedRankKey===key?"":key,renderPaperTabKeepParticipantScroll();return}if(act==="rank-scope"){const rawScope=String(btn.dataset.scope||"");paperRankScope=rawScope==="season"&&paperSeasonScopeActive()||rawScope==="week"||rawScope==="all"?rawScope:"day",renderPaperTab();return}if(act==="hot-mode"){paperHotMode=String(btn.dataset.mode||"")==="hold"?"hold":"net",paperHotModeUserSet=!0,renderPaperTab();return}if(act==="trades-page"){paperTradesPage=Math.max(0,paperTradesPage+(String(btn.dataset.dir||"")==="next"?1:-1)),renderPaperTab();return}if(act==="pill-quote"){const code=String(btn.dataset.code||"").toUpperCase(),market=String(btn.dataset.market||"").toUpperCase(),side=btn.dataset.side==="sell"?"sell":"buy",name=String(btn.dataset.name||code||"").trim();if(!code)return;if(!paperData?.account){showToast?.("계좌를 만들면 바로 시세 확인·주문까지 할 수 있어요","info");return}if(paperTicket={...paperTicket,code,market:market||"KR",side,qty:"",session:"",maxCashOrder:!1},!await paperLookupSymbol(code,market||"AUTO"))return;document.querySelector(".pp-ticket")?.scrollIntoView?.({behavior:"smooth",block:"center"}),setTimeout(()=>document.getElementById("ppQtyInput")?.focus?.(),80),showToast?.(`${name||paperTicket.name||code} ${side==="sell"?"매도":"매수"} 주문창을 채웠어요`,"info");return}if(act==="reset-account"||act==="close-account"){if(paperBusy)return;const nickname=paperData?.account?.nickname||"내 계좌";if(!confirm(`'${nickname}' 계좌를 처음부터 다시 시작할까요?
보유 종목과 거래내역이 모두 지워지고 5천만원으로 새롭게 시작합니다.
초기화는 7일에 한 번만 가능해요.`)||!confirm("정말 초기화할까요? 기존 거래내역은 복구할 수 없어요."))return;paperSetBusy("계좌를 초기화하는 중...");try{const data=await paperPost("reset",{confirm:"RESET"});paperRevealCode=data.recoveryCode||"",data?.account&&(paperData={account:data.account,positions:[],orders:[],policy:data.policy||paperData?.policy||{},leaderboard:paperData?.leaderboard||null}),paperLinkInfo=null,paperExpandedRankKey="",paperNicknameEditing=!1;try{localStorage.removeItem(PAPER_RECOVERY_ACK_KEY)}catch{}try{localStorage.removeItem(PAPER_SNAPSHOT_KEY)}catch{}showToast?.("5천만원으로 새롭게 시작했어요. 초기화는 7일에 한 번만 가능해요.","info"),await loadPaperTab({forceLb:!0,silent:!0}).catch(()=>null),setTimeout(()=>document.querySelector(".pp-code")?.scrollIntoView?.({behavior:"smooth",block:"center"}),50)}catch(e){showToast?.(e?.message||"계좌 초기화에 실패했어요","err")}finally{paperClearBusy()}return}if(act==="recovery-ack"){try{localStorage.setItem(PAPER_RECOVERY_ACK_KEY,"1")}catch{}renderPaperTab();return}if(act==="toggle-participants"){const willOpen=!paperShowParticipants;paperShowParticipants=willOpen,willOpen||(paperParticipantsError=""),paperShowParticipants&&!Array.isArray(paperLeaderboard?.participants)&&(paperParticipantsError="",renderPaperTab(),setTimeout(()=>document.querySelector(".pp-participants-expanded")?.scrollIntoView?.({behavior:"smooth",block:"nearest"}),30),await paperFetchLeaderboard(!0,{participants:!0}).catch(()=>(paperParticipantsError="전체 목록을 불러오지 못했어요",null))),renderPaperTab(),willOpen&&setTimeout(()=>document.querySelector(".pp-participants-expanded")?.scrollIntoView?.({behavior:"smooth",block:"nearest"}),30);return}if(act==="participants-more"){if(paperParticipantsLoadingMore||!paperLeaderboard?.participantsHasMore)return;paperParticipantsLoadingMore=!0,renderPaperTab(),await paperFetchLeaderboard(!0,{participants:!0,append:!0}).catch(()=>(paperParticipantsError="추가 목록을 불러오지 못했어요",null)),paperParticipantsLoadingMore=!1,renderPaperTab();return}if(act==="rank-window"){const key=String(btn.dataset.window||"all");PAPER_RANK_WINDOWS.some(item=>item.key===key)&&(paperRankWindow=key,renderPaperTabKeepParticipantScroll());return}if(act==="participant-sort"){paperParticipantSort=btn.dataset.sort==="orders"?"orders":"rank",renderPaperTabKeepParticipantScroll();return}if(act==="share-contest")return paperShareContest();if(act==="draw-ticket")return paperDrawTicket();if(act==="draw-dismiss"){paperDrawResult=null,paperTabActive()&&renderPaperTab();return}if(act==="boast-chat")return paperBoastToChat();if(act==="share-board-open"){const elig=await paperEnsureBoardShareEligibility({withTrades:!0});if(!elig.ok){showToast?.(elig.reason==="no-order"?"모의투자 주문 내역이 있어야 게시판에 공유할 수 있어요":elig.reason==="load-failed"?"모의투자 기록을 확인하지 못했어요. 잠시 후 다시 눌러 주세요":"먼저 모의투자 계좌를 만들어 주세요","warn");return}paperBoardShare=paperBoardShare||{channel:"free",title:"",body:"",tradeId:""},renderPaperTab(),setTimeout(()=>document.getElementById("ppBoardShareTitle")?.focus({preventScroll:!0}),0);return}if(act==="share-board-channel"){paperReadBoardShareDraft(),paperBoardShare&&(paperBoardShare.channel=PAPER_SHARE_CHANNELS.some(c=>c.key===btn.dataset.ch)?btn.dataset.ch:"free"),renderPaperTab();return}if(act==="share-board-trade"){paperReadBoardShareDraft(),renderPaperTab();return}if(act==="share-board-cancel"){paperBoardShare=null,renderPaperTab();return}if(act==="share-board-submit")return paperShareToBoard();if(act==="badge-feed-open"){paperBadgeModalOpen=!0,paperBadgeFocusId=String(btn.dataset.badge||""),paperPinDraft=null,renderPaperTab(),paperScrollFocusedBadge();return}if(act==="badge-modal-open"){paperBadgeModalOpen=!0,paperBadgeFocusId="",paperPinDraft=null,renderPaperTab();return}if(act==="badge-modal-close"){paperBadgeModalOpen=!1,paperBadgeFocusId="",paperPinDraft=null,renderPaperTab();return}if(act==="welcome-bonus-close"){paperWelcomeBonusAmount=0,paperTabActive()&&renderPaperTab();return}if(act==="badge-pin-toggle"){paperTogglePinDraft(btn.dataset.badge),renderPaperTab();return}if(act==="nickcard-mode"){paperNickCardModeDraft=btn.dataset.mode==="div"?"div":"badge",renderPaperTab();return}if(act==="badge-pin-save")return paperSavePinnedBadges()}document.addEventListener("click",ev=>{const summary=ev.target?.closest?.(".pp-share-menu > summary");if(summary){ev.preventDefault();const details=summary.closest("details");details&&(details.open=!details.open,summary.setAttribute("aria-expanded",details.open?"true":"false"));return}const btn=ev.target?.closest?.("[data-pp-act]");!btn||btn.disabled||btn.classList?.contains("pp-modal-backdrop")&&ev.target?.closest?.("[data-pp-stop]")||(ev.preventDefault(),paperHandleAction(btn.dataset.ppAct,btn).catch(e=>debugWarn("paper action failed",e)))}),document.addEventListener("change",async ev=>{if(ev.target?.id==="ppBoardShareTrade"){paperReadBoardShareDraft(),renderPaperTab();return}if(ev.target?.id!=="ppNotifySelect")return;const threshold=Number(ev.target.value)||0;try{if(threshold>0){if(typeof Notification<"u"&&Notification.permission!=="granted"&&await Notification.requestPermission()!=="granted"){showToast?.("브라우저 알림 권한을 허용해야 받을 수 있어요","warn"),renderPaperTab();return}writePaperPerformanceNotificationSetting(!0)}else writePaperPerformanceNotificationSetting(!1);try{const pending=window.syncChatPushSubscription?.();pending&&typeof pending.catch=="function"&&pending.catch(()=>{})}catch{}await paperPost("notify-settings",{threshold}),paperData?.account&&(paperData.account.notifyThreshold=threshold),showToast?.(threshold>0?`수익률이 ±${threshold}%p 움직이면 알려드릴게요`:"수익률 알림을 껐어요","info")}catch(e){showToast?.(e?.message||"알림 설정에 실패했어요","err")}}),document.addEventListener("input",ev=>{if(ev.target?.id==="ppQtyInput"){paperTicket.qty=ev.target.value,paperTicket.maxCashOrder=!1,paperUpdateTicketFoot();return}if(ev.target?.id==="ppNickInput"){paperOnboardDraft.nickname=ev.target.value;return}if(ev.target?.id==="ppRecoverInput"){paperOnboardDraft.recovery=ev.target.value;return}(ev.target?.id==="ppBoardShareTitle"||ev.target?.id==="ppBoardShareComment")&&paperReadBoardShareDraft()}),document.addEventListener("keydown",ev=>{if(ev.key==="Escape"&&(paperHelpOpen||paperSettingsOpen||paperBadgeModalOpen||paperRankModalOpen)){if(typeof paperTabActive=="function"&&!paperTabActive())return;paperHelpOpen=!1,paperSettingsOpen=!1,paperBadgeModalOpen=!1,paperRankModalOpen=!1,paperLeagueModalOpen=!1,ev.preventDefault(),renderPaperTab();return}if(ev.target?.id==="ppSymbolInput"){if(paperSymbolAutocomplete?.handleKeydown?.(ev))return;ev.key==="Enter"&&(ev.preventDefault(),paperLookupSymbol());return}ev.key==="Enter"&&(ev.target?.id==="ppQtyInput"&&(ev.preventDefault(),paperSubmitOrder()),ev.target?.id==="ppRenameInput"&&(ev.preventDefault(),paperRenameNickname()),ev.target?.id==="ppRecoverInput"&&(ev.preventDefault(),paperHandleAction("recover",ev.target)),ev.target?.id==="ppNickInput"&&(ev.preventDefault(),paperHandleAction("create",ev.target)))}),window.ensurePaperPerformanceNotificationDefault=ensurePaperPerformanceNotificationDefault;let paperBalanceResizeTimer=null;window.addEventListener("resize",()=>{paperBalanceResizeTimer&&clearTimeout(paperBalanceResizeTimer),paperBalanceResizeTimer=setTimeout(()=>{paperBalanceResizeTimer=null,typeof paperTabActive=="function"&&paperTabActive()&&paperBalanceAccountCards()},150)}),document.addEventListener("DOMContentLoaded",()=>{let token="";try{token=new URLSearchParams(location.search).get("paperlink")||""}catch{}if(token){try{const url=new URL(location.href);url.searchParams.delete("paperlink"),history.replaceState(history.state,document.title,url.toString())}catch{}paperPost("link-claim",{token}).then(()=>{showToast?.("모의투자 계좌가 이 기기에 연결됐어요!","info"),typeof setTimelineTab=="function"?setTimelineTab("paper"):loadPaperTab().catch(()=>null),setTimeout(()=>{ensurePaperPerformanceNotificationDefault({silent:!0}).catch(()=>null)},0)}).catch(e=>{showToast?.(e?.message||"기기 연결에 실패했어요","err")})}});
